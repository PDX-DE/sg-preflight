# CMake Helpers

Project-local CMake helpers live here once the C++ track needs reusable modules. C-1 intentionally keeps all build logic in `cpp/CMakeLists.txt` until the first real duplication appears.
