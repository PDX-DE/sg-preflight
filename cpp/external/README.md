# External Dependencies

No vendored dependencies are committed here for C-1. Ramses is built from a separate source checkout with submodules, then consumed through its installed `ramses-shared-lib` CMake package.
