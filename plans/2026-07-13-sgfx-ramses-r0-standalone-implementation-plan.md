# SGFX Ramses R0 Standalone Probe Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (\`- [ ]\`) syntax for tracking.

**Goal:** Build and accept a standalone, noninteractive Ramses R0 probe that produces bounded metadata, validation, inventory, logic, lifecycle, authored-frame, and classified non-render evidence without changing the existing four-pack action, Control Center truth, packaged default, or external systems.

**Architecture:** The existing sgfx::cine library remains the only Ramses implementation boundary. Focused native units collect typed SDK facts and reuse the accepted authored preview renderer/lifecycle path for one optional, explicitly configured perspective and at most one frame; the new sgfx_ramses_probe executable only parses strict arguments and writes an atomic native report. Python resolves canonical inputs, proves helper/runtime/source provenance, owns generation and the external 60-second process boundary, validates the native facts, builds the policy-bearing final RamsesProbeReport, and keeps every path-bearing artifact protected below the SGFX output root; native work reserves finalization at 58 seconds and bounds the complete authored perspective/update/Rendered/readback phase to 30 seconds within that work deadline.

**Tech Stack:** C++17, CMake/CTest, Ramses 28.16.0 public SDK, SDL3, existing nlohmann_json 3.12.0 target, Python 3.10+ standard library, unittest, PowerShell, and the existing SGFX bundle/evidence code. No new production dependency is introduced.

## Global Constraints

- Native-build operations use one exact two-stage outer-Job protocol. While the direct snapshot-Python coordinator is itself assigned to Task 1's no-breakaway outer Job, it cannot observe or claim whole-Job active-zero. After every registered descendant has signaled and exited, its drains are joined, its direct thread/process/pipe handles are closed, and its audit/module records are reconciled, the still-running coordinator must query exact `ActiveProcesses == 1` and prove the sole remaining process ID is itself; this is `coordinator_self_only_quiescence`. It retains the publication journal/mutex/root handles through that proof, finalizes the protected receipt/publication, emits its bounded result, and exits. A native-build receipt binds ordered descendant cleanup plus `coordinator_self_only_quiescence`, never post-exit active-zero. Only the outer PowerShell parent may then capture the coordinator signal/exit, join its drains, close its direct resources, query exact `ActiveProcesses == 0`, close the Job, and accept stdout/stderr/exit. Every later native-build phrase such as “through active-zero”, “active-zero cleanup”, or “CMake/MSBuild/CTest active-zero” is shorthand for this split protocol and must not be implemented as a coordinator-side zero query. Tests reject zero claimed before coordinator exit, a receipt containing post-exit zero, any second live PID at self-only quiescence, or outer acceptance before the final zero query and Job closure.
- For the `recover-publication` write exception, the one authorization-bound transaction's `journal` always means its authenticated final journal and its distinct same-parent journal-bootstrap leaf. The exact write set is therefore journal/bootstrap plus the authorization-bound staging/final/completion/pointer/tombstone identities; `repair_one_journal_bootstrap` may mutate and no-replace rename only that bound bootstrap identity under the common recovery contract.
- Execute in the clean linked worktree C:\Users\DavidErikGarciaArena\Downloads\sg-preflight-v02-qt-integration on branch feature/sgfx-v02-qt-integration-20260710, starting from bd4867a809a6c02612f8449562940e29215ecfa6.
- After Task 1 creates it, run every workspace Python module, test, inspector, verifier, package builder, and Task 13 regression only through tracked `scripts/run_sgfx_isolated_python.ps1` or the separately reviewer-bound acceptance launcher frozen in Task 12. No acceptance command launches the sibling venv redirector or the user-local base installation in place.
- Every append-only journal in this plan uses canonical UTF-8/no-BOM JSONL with one terminal LF per complete record. The header is sequence 0; every later record has exact top-level keys `sequence`, `previousRecordSha256`, `kind`, and `payload`, where `sequence` is the prior value plus one and `previousRecordSha256` hashes the complete prior canonical record including LF. No protected-tree mutation is legal until the complete canonical header has been flushed, reopened, byte-compared, and atomically published. Header publication first derives an exact same-parent bootstrap leaf `.<journal-leaf>.bootstrap-<32hex>` from the already authenticated transaction/bootstrap binding, creates it with no replace, records its held identity in memory, writes, flushes, and reparses the complete header, and renames that same identity with no replace to the final journal leaf. A crash can therefore leave only the authenticated before-state plus an absent journal, one bootstrap leaf, or the exact final header—never a protected-tree mutation.
- Journal-bootstrap recovery is explicit. Cache and primary-snapshot bootstrap recovery launches no Python/generated code and remains inside Task 1's reviewed PowerShell launcher. Task 10 publication bootstrap recovery instead runs only through the held `scripts/recover_sgfx_r0_publication.ps1` wrapper and Task 1's exact isolated `sg_preflight.bundle_manifest_cli recover-publication` operation, so the one Python `SecurePublisher` implementation owns both forward and recovery byte/state transitions. The applicable fixed director-approved recovery authorization binds the expected canonical header bytes/digest, bootstrap/final names, observed bootstrap identity/bytes, held parent/mutex, and a fresh complete proof that every protected mutation root still equals its pre-header inventory. Recovery may resume only the same bootstrap identity: on first entry its bytes equal the authorization; after an interrupted repair they must be an exact prefix of the one expected header. It truncates/re-writes that same held leaf, flushes/reparses the header, and performs the no-replace rename; original-absent/exact-final-present is accepted only with the same identity and header. Publication recovery action `repair_one_journal_bootstrap` then appends canonical `aborted_before_mutation` and `completed` records so a fresh transaction may start; cache/primary recovery instead continues its already authorized state machine. Any target drift, second bootstrap/final leaf, replacement, non-prefix, ADS/link, fork, or mutation before the final header stops. Tests crash before/after bootstrap create, every partial-write offset, flush, reopen, rename, and final-header reopen for fixed and nonce journals.
- A recovery authorization that binds a journal prefix always records both byte length and SHA-256. On every later recovery—including two or more successive recovery crashes—the journal must start byte-for-byte with that exact prefix. Every complete suffix record must form one contiguous sequence/previous-digest chain and be the unique next record allowed by the full transaction state machine and retained identities. A non-LF torn tail is recoverable only when it is a byte prefix of the single canonical next record recomputed from the last complete intent and current retained state; recovery truncates only that tail before appending the recomputed record. Truncation of a complete record, replay, duplicate sequence, fork, alternate tail, reordered/unknown state, digest mismatch, or suffix that authorizes a different identity stops without mutation. The same fixed authorization remains valid only under these exact extension rules.
- Before the first ordinary `SecurePublisher` transaction, Task 10 performs exactly one `publication_journal_root` initialization under the native-build mutex. Its fixed journal `.superpowers/sdd/r0-publication-journal-root-init.jsonl` is bootstrapped in the already existing, authenticated, and retained `.superpowers/sdd` parent; its bootstrap leaf is exactly `.superpowers/sdd/.r0-publication-journal-root-init.jsonl.bootstrap-<32hex>`. The header binds the held parent identity/DACL/inventory, absent proof for exact child `r0-publication-journals-v1`, expected protected DACL, tooling/source/primary-receipt bindings, the fixed external recovery-authority-root identity/DACL, and mutex. It appends and flushes `journal_root_create_intent` before no-replace creation, pins/reopens the empty directory, appends and flushes `journal_root_created` with its observed volume/file ID and DACL, re-enumerates the original parent inventory plus the exact fixed initialization journal and that sole root child, and appends `reconciled` then `completed`. An existing root before the durable intent, any planted sibling/entry, or any identity/DACL/inventory disagreement stops. After an intent without its result, absent resumes creation and exact present may become a recovered result only when it is the sole new child besides the exact fixed initialization journal, remains empty, and matches every authorization-bound identity/ancestry/DACL fact. Publication recovery action `resume_publication_journal_root_initialization` handles only that fixed state machine; the common prefix/suffix rules make repeated recovery crashes deterministic. A completed initialization journal and empty-or-valid-journal-only root are revalidated read-only before every later transaction and are never recreated or replaced. Tests cover absent, planted, pre-intent insertion, every header-bootstrap offset, intent, create-before-result, result, reconciliation, repeated-recovery, completed-parent, and later root-identity/inventory drift.
- The primary snapshot root is exactly the held-worktree-relative `.superpowers/sdd/r0-python-primary-v1`, with exact children `runtime` and `dependencies`. The wrapper derives that root only from the authenticated worktree identity; no environment value, receipt field, registry value, current directory, or caller path can redirect it. Task 1 first requires the existing repository `.superpowers/` ignore rule, securely creates one sibling `r0-python-primary-v1.next-<32-lowercase-hex>` with inheritance disabled and a protected DACL limited to the current user SID, SYSTEM, and Administrators, pins every directory identity, builds into it, flushes and revalidates the complete tree, applies the final read/execute-only current-user DACL, and renames it with no replace. An existing final root is reused only when every identity, ACL, receipt, and byte already passes. An unexpected pre-binding final root, partial `.next` root, or unmatched tracked receipt is a rejected orphan: the initializer never adopts, replaces, or automatically deletes it. It stops before Python and records exact ancestry/ACL/identity/inventory/tree-digest facts in the ignored ledger; cleanup of that unbound state is outside this plan and requires a separate director-approved recovery plan with its own restartable journal. The known-clean entry condition for this plan is absence of all such roots. After the clean primary-snapshot binding commit, a missing or mismatched root is terminal until the explicit restartable recovery below, byte-identical reconstruction, fresh independent receipt review, a new binding commit, and a new external primary review record all pass; there is no fallback to the base installation.
- Post-binding snapshot recovery is the explicit PowerShell-only mode `-RecoverBoundPrimarySnapshot -DevelopmentTask 1 -ExpectedRecoveryAuthorizationSha256 <64-lowercase-hex>` and is permanently distinct from initial `-InitializePrimarySnapshot`. It is legal only when the launcher/config bytes still equal the latest binding commit, the tracked receipt fails solely because the protected final root is missing/mismatched, and fixed protected `.superpowers/sdd/r0-primary-recovery-authorization.json` exists. That canonical authorization is created only after explicit director approval and independent identity review; it is capped at 512 MiB and binds schema, latest binding commit, old receipt digest, action `reconstruct_primary_snapshot`, at most two observed final/partial cleanup roots, their ancestry/DACL plus complete leaf/directory volume/file-ID inventories and tree digests, deterministic same-parent tombstone names, the exact journal header/bootstrap binding, and approval timestamp. Each cleanup root and the reconstruction root obey the receipt's maximum 100,000 files, 25,000 parent-closure directories, 768-byte relative path, no-empty-directory, and 8 GiB combined-tree rules. Before the first destructive operation, the launcher publishes fixed append-only `.superpowers/sdd/r0-primary-recovery-journal.jsonl` through the common recoverable header-bootstrap contract. The journal is capped at 800,000 canonical records and 2 GiB, and each post-header record is at most 2 KiB. Its constructive maximum is two cleanup roots times 125,000 entries times intent/result = 500,000 records, plus one 125,000-entry reconstruction times intent/result = 250,000 records, plus at most 512 root/state/rename/receipt/review/archive/finalization records; 750,512 is below both declared caps even when every path is maximum length. The header binds the retained authorization bytes/digest, held parent identity, cleanup-root identities, tombstone names, mutex identity, and a cryptographically generated reconstruction-root name. For each authorized top-level cleanup root it flushes a rename intent, atomically renames that exact reopened identity without following links to its authorization-bound same-parent tombstone, then flushes the observed rename result before advancing. On restart, original-present/tombstone-absent resumes the rename; original-absent/tombstone-present is accepted only when the tombstone has the authorized identity and ancestry and is recorded as a recovered rename result; any other combination stops. Only after a durable rename result may leaf-first disposition begin. Every descendant leaf and then empty directory has a flushed delete intent and result; after interruption, a missing descendant is accepted as a recovered deletion only when its durable intent follows the durable top-level rename, the still-present held tombstone ancestry/inventory contains no unknown identity, and authorization/mutex remain unchanged. The top-level tombstone itself has a distinct final delete intent that binds its empty-tree proof and retained parent identity; if deletion succeeds but its result does not flush, original and tombstone both absent may be recorded as a recovered root deletion only when every descendant result is terminal, the parent/authorization/journal/mutex identities are unchanged, and no unknown sibling exists. Replaced, extra, ambiguous, cap overflow, or unrecorded state stops. Maximum-shaped tests instantiate both full cleanup roots plus a full reconstruction and require the complete finalizer to stay below 800,000 records and 2 GiB without relaxing any per-entry flush.

- After cleanup reconciliation, the same journal advances through exact states `reconstruction_creating`, `reconstruction_complete`, `snapshot_rename_intent`, `snapshot_renamed`, `recovery_receipt_intent`, `recovery_receipt_published`, and `awaiting_new_binding`. Every reconstruction directory/leaf has a flushed create intent/result containing expected relative path, size/digest, parent identity, and observed identity. On restart, an intent with no result either creates the absent object or records a recovered result only when the existing object has the exact expected identity/bytes/ancestry; every completed result is reopened and revalidated. After the complete tree/DACL/receipt preimage passes, flush `snapshot_rename_intent`, rename the exact `.next` root with no replace, and flush the result. A crash after rename but before result is recovered only as reconstruction-root-absent/exact-final-root-present with the complete expected identity/tree/DACL. Publish fixed `.superpowers/sdd/r0-python-primary-recovery-receipt.json` through its own flushed intent/result and secure no-replace temp; an exact existing receipt after intent is revalidated and recorded as recovered, while any unjournaled or mismatched receipt stops. No Python execution/refresh/floor mode is enabled in `awaiting_new_binding`. Independent Python/security and scope/release reviewers verify the new snapshot and candidate receipt; root updates only the tracked receipt and frozen launcher digest in a new two-path binding commit, then creates and validates the new fixed external `python313` review record. Only that clean commit plus record permits the retirement phase or later execution.

- `-FinalizeBoundPrimaryRecovery -DevelopmentTask 1 -ExpectedRecoveryAuthorizationSha256 <64hex> -ExpectedNewBindingCommit <40hex> -ExpectedExternalPrimaryReviewRecordSha256 <64hex>` is the sole no-Python retirement mode. It derives the new primary receipt/external-record names, requires the journal in `awaiting_new_binding`, and proves the exact new two-path binding commit and external review record before history mutation. It first revalidates fixed parent `.superpowers/sdd`; if `.superpowers/sdd/r0-primary-recovery-history-v1` is absent, it appends and flushes `history_parent_create_intent` binding the exact name, held parent identity, absent proof, and protected DACL, creates/pins it with no replace, and appends and flushes `history_parent_created` with observed identity. If that parent already exists, every prior child must be a complete immutable digest-addressed history and it appends a read-only validation result; an unknown/partial child stops. Next it appends and flushes `history_root_create_intent` for exact child `recovery-<authorization-sha256>`, binding the retained history-parent identity, absent proof, empty initial inventory, and protected DACL before `CreateDirectoryW`; only then does it create/pin the child with no replace and append and flush `history_root_created` with observed identity. After an intent-without-result crash, absent resumes creation; exact present may become a recovered result only when it is the sole new child, has the expected name/parent/ancestry/DACL, no reparse/ADS/alias, and is empty before any later archive intent. Once later intents exist, its inventory must equal only their exact recorded results. Any present child before the intent or any identity/inventory disagreement stops.
- Under later journaled archive intents/results the finalizer atomically moves the fixed authorization and recovery receipt into that authenticated child, writes and flushes canonical `archive-manifest.json` binding old/new commits, every receipt/record/authorization digest and history identity, and the pre-archive journal-prefix digest. It then appends and flushes a final `journal_archive_intent` binding that exact manifest digest, prefix digest, history-root identity, and proof that every prior archive result is terminal; hashes the resulting terminal journal; and atomically moves the journal itself as the last operation to derived history leaf `recovery-journal-<terminal-journal-sha256>.jsonl`. The destination name is derived only after the terminal hash and is not part of the journal preimage, avoiding a circular digest. A crash after any creation or move reconciles original/history identity only from the prior intent and common prefix-extension rules; a crash after the final journal move is complete only when the sole current-history journal's filename matches its bytes, its terminal event binds the manifest/prefix/history root, every manifest-bound artifact revalidates, and every fixed live name is absent. The digest-addressed history is immutable and ignored by later recovery selection, so future fixed authorization/journal names are unambiguous. Tests crash before/after history-parent intent, create, and result; history-child intent, create, and result; every cleanup root/leaf/directory intent/result; reconstruction create/result; snapshot rename; recovery-receipt publication; new-binding/external-record check; authorization/receipt archive; archive-manifest publication; final-event flush; and terminal journal move. They cover deletion-before-result, rename-before-result, planted/pre-intent/partial history roots, forged/stale authorization, replacement, reconstruction drift, fixed/history ambiguity, digest-name mismatch, and the mandatory disabled interval.
- Live CPython 3.13.13 has no source `python313.zip` or `python313._pth`, so Task 1 never claims to copy either. Before the first Python instruction, PowerShell-only .NET code holds and authenticates the signed base `python.exe`, stable-ABI `python3.dll`, versioned `python313.dll`, Microsoft-signed `VCRUNTIME140.dll` and `VCRUNTIME140_1.dll`, the held sibling venv's `pyvenv.cfg` whose exact `home`/`executable`/version must resolve to that same signed base, base `Lib` excluding `site-packages`/`__pycache__`/`*.pyc`/`*.pyo`, and base `DLLs`. It copies the five signed runtime files and the complete held `DLLs` tree, then constructs `runtime/python313.zip` from every selected `Lib` regular file in canonical UTF-8 relative-path byte order using ZIP stored entries, UTF-8 names, no duplicate/case/NFC collision, fixed DOS timestamp `1980-01-01T00:00:00`, no directory entries, and exact source bytes. It writes `runtime/python313._pth` as exactly the ASCII bytes `python313.zip\nDLLs\n..\..\..\..\n..\dependencies\n` with no BOM and no `import site` line. At process creation the third entry must resolve to the already authenticated and retained worktree root and the fourth to the protected receipt-owned dependency root. The startup path is exactly protected standard-library ZIP, protected `DLLs`, held worktree, then protected dependencies; no absent, registry, environment, user-site, or other slot exists.
- The dependency selection policy is exactly `third_party_no_site_v1`. From the held sibling-venv `Lib/site-packages` tree, copy every regular local file except any `__pycache__` component, `*.pyc`, `*.pyo`, `*.pth`, `*.egg-link`, `direct_url.json`, top-level `__editable__*` entry, and every top-level `.dist-info` or `.egg-info` directory whose PEP-503-normalized distribution name is `sg-preflight`, independent of version or case. Reject rather than copy any reparse/ADS/hardlink/case/NFC collision, `sitecustomize.py`, `usercustomize.py`, or importable top-level `sg_preflight`, `tests`, or `scripts` entry. The fixed `_pth` order places the receipted dependency root only after the authenticated worktree root; the bootstrap verifies that order without adding another path, never invokes `site`, never processes a `.pth` file, and rejects any dependency import that would resolve a worktree-owned top-level name. Tests use both `sg_preflight-0.1.0.dist-info` and `sg_preflight-0.1.1.dist-info`, mixed-case/version variants, the observed editable `.pth`, executable `distutils-precedence.pth`, and `direct_url.json` shapes and prove that none enters the snapshot, import graph, or package.
- `config/sgfx-primary-python-3.13-provenance.json` is canonical schema 1 with exactly the top-level keys `schemaVersion`, `snapshotId`, `bootstrapCommit`, `bootstrapFiles`, `implementation`, `version`, `architecture`, `sourceSignedFiles`, `snapshotSignedFiles`, `snapshotGeneratedFiles`, `pyvenvCfgSha256`, `sourceStandardLibrary`, `sourceDependencies`, `runtimeTree`, `dependencyTree`, `startupPath`, and `dependencySelectionPolicy`. Values are exactly `1`, `r0-python-primary-v1`, the independently reviewed clean bootstrap commit, its exact four-file binding described below, `CPython`, `3.13.13`, `amd64`, the fixed arrays/records below, the lowercase held sibling-venv `pyvenv.cfg` SHA-256, four tree records, `["python313.zip","DLLs","..\\..\\..\\..","..\\dependencies"]`, and `third_party_no_site_v1`. `bootstrapFiles` is UTF-8-path-byte ordered and contains exactly `.gitattributes`, `scripts/run_sgfx_isolated_python.ps1`, `scripts/sgfx_isolated_python_bootstrap.py`, and `scripts/test_sgfx_isolated_python_trust.ps1` as objects `{path,size,sha256}`. Each signed-file array is ordered `python`, `python_abi_dll`, `python_dll`, `vcruntime`, `vcruntime_1` and each object is exactly `{role,fileName,size,sha256,volumeSerialNumber,fileId,signerSubject,signerThumbprint}`; source and snapshot names are respectively the observed canonical leaf and fixed snapshot leaf. `snapshotGeneratedFiles` is ordered `python_zip` then `python_pth` with exact objects `{role,fileName,size,sha256,volumeSerialNumber,fileId}`. Every tree object is exactly `{fileCount,totalBytes,treeSha256}`; its digest preimage is UTF-8 canonical JSON of `["sgfx-python-tree-v1",[[relativePath,size,sha256],...]]` with NFC `/`-separated paths sorted by UTF-8 bytes. Receipt JSON uses ASCII keys, `ensure_ascii=true`, keys sorted by ordinal byte order, arrays in the prescribed order, separators `,`/`:`, UTF-8 without BOM, and one terminal LF; the reviewed receipt SHA-256 covers those exact bytes including LF. The receipt is capped at 1 MiB, each source/snapshot tree at 50,000 files and 4 GiB, and the combined runtime/dependency snapshot at 100,000 files and 8 GiB. The copied snapshot contains no empty directory; its exact parent closure is capped at 25,000 directories, and every NFC relative path is capped at 768 UTF-8 bytes. File, directory, path, or byte overflow is rejected before allocation or copy and is exercised at each boundary.
- Require valid PSF subject/thumbprints for both source and snapshot `python.exe`/`python3.dll`/`python313.dll` and valid Microsoft subject/thumbprints plus digests for both source and snapshot `VCRUNTIME140.dll`/`VCRUNTIME140_1.dll`. Construction and independent binding review open and retain every source, snapshot, dependency, and component leaf; reject remote/reparse/ADS/case/NFC collision or unaccounted hardlink state. After the binding commit, execution deliberately does not reopen or depend on the mutable user-local base installation or sibling venv: every launch uses only the tracked receipt plus protected snapshot/runtime/dependency leaves for interpreter/dependency provenance, separately retains the authenticated worktree leaves named by the source proof, verifies all applicable namespaces/tree records before launch and after exit, and treats the source records as frozen construction provenance. Tests mutate or remove a source fixture after binding and prove the intact protected snapshot remains authoritative, while any snapshot/receipt drift fails. The expected receipt digest is mandatory and must match the independently reviewer-bound caller, the wrapper's frozen value, and the fixed external `python313-<receipt-sha256>.json` review record derived only from that expected digest. Task 1-12 development commands use exactly the environment key `SGFX_R0_PRIMARY_PYTHON_PROVENANCE_RECEIPT_SHA256`; verifier/acceptance calls pass the same value explicitly. The wrapper accepts exactly one digest-binding source, rejects disagreement or any other provenance environment key, removes the environment binding before child creation, and accepts no review-record path parameter. The verifier-only CPython-3.10 lane follows the same lane-parameterized receipt, snapshot, both-VCRUNTIME, no-site, and no-absent-entry contract under Task 12.
- Before either destructive Task 1 pre-binding mode can execute, two independent reviewers freeze the clean bootstrap commit and exact size/SHA-256 of the same four bootstrap files in a protected review record outside the implementation worktree. Root Lexus copies those values into a caller-held invocation only; the launcher may not discover or substitute them. The caller opens `scripts/run_sgfx_isolated_python.ps1` with read access and no write/delete sharing, hashes that retained byte stream, requires the reviewed launcher digest, keeps the stream open through invocation, and passes `-ExpectedBootstrapCommit` plus all four explicit `-ExpectedBootstrap*Sha256` values. The launcher independently requires its current clean HEAD to equal that commit, each exact HEAD blob/current leaf to equal the supplied size/hash binding, and the retained caller launcher to be the same file identity it reopens. Cleanup and primary receipts record the commit and four-file binding. A same-shaped launcher on any other clean commit, a self-consistent edited launcher, a missing reviewer value, or caller/launcher disagreement fails before deletion, copy, child creation, or generated-root mutation.
- External review bindings live only beneath fixed canonical-checkout root `C:\Users\DavidErikGarciaArena\Downloads\sg-preflight\out\agent-control\r0-review-bindings-v1`, never beneath the implementation worktree or a generated R0 evidence root. Root Lexus creates one protected no-replace canonical record per binding: `bootstrap-<40hex>.json`, `python313-<64hex>.json`, `native-build-<40hex>.json`, `python310-<64hex>.json`, or `implementation-<40hex>.json`. Each is UTF-8/no-BOM/one-LF, at most 64 KiB, and exactly `{schemaVersion,kind,reviewedCommit,reviewedDigest,files,reviewers,reviewedAtUtc}`; schema is 1 and kind matches the filename prefix. `reviewedDigest` always names the reviewed target artifact, never the review record's own digest: it is null for `bootstrap`, the tracked primary receipt SHA-256 for `python313`, the tracked native-build provenance JSON SHA-256 for `native-build`, the fixed protected floor-receipt SHA-256 for `python310`, and the acceptance-launcher SHA-256 for `implementation`. `reviewedCommit` is respectively the reviewed bootstrap commit, primary snapshot-binding commit, native-build provenance-binding commit, null, or reviewed implementation commit. `files` is respectively the exact four bootstrap files; the exact bound launcher and primary receipt; the provenance candidate's exact tracked `toolingFiles`; an empty array for the externally protected `python310` receipt whose canonical digest binds its complete retained tree records; or the exact acceptance launcher. Every nonempty entry is sorted exact `{path,size,sha256}` with repository-relative engineering paths only. `reviewers` is the sorted fixed role set that actually approved it, and time is canonical UTC. The current-user/SYSTEM/Administrators-only DACL, ancestry, no reparse/ADS/hardlink, bytes, identity, and reviewer dispositions are independently checked before use. Records are append-only by unique binding name; no replacement or “latest” lookup is allowed. Bootstrap, native-build, floor, and implementation callers receive their one explicit fixed record identity/digest. The primary launcher and verifier instead derive only `python313-<ExpectedPrimaryPythonProvenanceReceiptSha256>.json` beneath the fixed external root, retain and validate that canonical record together with the tracked receipt and launcher, and accept no caller-supplied review-record path or filename. Canonical STATE/handoff blocks mirror only kind/commit/digests and never replace these records. No implementation, verifier, or product code writes this root.
- Native dependency source authority is a distinct protected local-only binding beneath fixed canonical-checkout root `C:\Users\DavidErikGarciaArena\Downloads\sg-preflight\out\agent-control\r0-native-source-bindings-v1`. After the Task 10 tooling commit, independent native/spec and Python/security reviewers inspect one lawful four-source set and root Lexus no-replace publishes canonical `source-set-<record-sha256>.json`, where the 64-hex filename digest hashes the exact UTF-8/no-BOM/one-LF bytes. The object is at most 64 KiB and exactly `{schemaVersion,kind,toolingCommit,dependencies,reviewers,reviewedAtUtc}`; schema is 1, kind is `native_dependency_sources`, tooling commit is exact, and dependencies are in fixed nlohmann_json, SDL3, FreeType, RmlUi order with exact `{role,canonicalPath,origin,version,rootVolumeSerialNumber,rootFileId,fileCount,totalBytes,treeSha256,licenseRelativePath,licenseSha256}`. Paths are protected local evidence, never committed, copied to a receipt, logged, or emitted. The fixed current-user/SYSTEM/Administrators-only DACL, ancestry, bytes, digest-addressed filename, identities, complete source trees/licenses, and two reviewer roles are revalidated before and after snapshot construction. The initializer accepts only `-ExpectedNativeDependencySourceBindingSha256 <64-lowercase-hex>`, derives that one fixed record name under the fixed root, and accepts no source path or binding-record path. Missing, replaced, remote, linked, ambiguous, wrong-commit, wrong-role/order, drifted, or extra source state stops before publication.
- Publication recovery authority lives outside every implementation-worktree and generated-root inventory beneath fixed canonical-checkout root `C:\Users\DavidErikGarciaArena\Downloads\sg-preflight\out\agent-control\r0-publication-recovery-authorizations-v1`. Before Task 1, root Lexus securely creates that empty protected directory with inheritance disabled, a current-user/SYSTEM/Administrators-only DACL, no reparse/ADS/alias, and a retained identity recorded in canonical coordination; no implementation, verifier, wrapper, product code, or recovery operation creates, replaces, or deletes it. After an interrupted publication, root Lexus alone may no-replace publish one canonical UTF-8/no-BOM/one-LF `authorization-<record-sha256>.json`, where the 64-hex filename digest hashes its exact bytes. The record is at most 512 MiB and has exact top-level keys `{schemaVersion,kind,action,sourceCommit,journal,targets,proof,mutex,authorizedAtUtc}`; schema is 1, kind is `sgfx_r0_publication_recovery_v1`, action is one allowed recovery action, `journal` binds exact relative name/identity/header/prefix size and digest, `targets` is the ordered complete staging/final/completion/pointer/tombstone identity set, `proof` binds fresh target-worktree ancestry/DACL/inventory/tree/relation/tooling/receipt facts, and `mutex` binds the held lock identity. The recovery wrapper accepts only the record SHA-256, derives that exact filename under the fixed external root, and revalidates root/record identity, DACL, bytes, digest, and target facts before mutation. Creating this external authorization never changes `.superpowers/sdd`, the publication-journal root, or any header-bound target inventory.

- Both Python lanes use the same reviewed Microsoft-CRT-compatible quoting routine under absolute Windows PowerShell 5.1 and PowerShell 7 and launch only through Win32 `CreateProcessW`, never `ProcessStartInfo.ArgumentList`, a shell, or an ambient executable. The parent opens NUL for standard input and creates one dedicated parent-to-child capability pipe, one child-to-parent audit pipe, plus separate stdout/stderr pipes. `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` contains exactly the NUL read handle, capability read handle, audit write handle, stdout write handle, and stderr write handle; only NUL/stdout/stderr are standard handles, and the capability/audit handle values enter only the fixed bootstrap argv. The parent retains non-inheritable counterparts and drains at most 4 MiB and 4,096 canonical nonce-framed audit records independently, with overflow terminal. It starts the direct snapshot interpreter suspended, assigns it to a fresh no-breakaway queryable `KILL_ON_JOB_CLOSE` Job before first instruction, starts bounded drains, and resumes it. Each stdout and stderr drain retains at most 16 MiB while continuing to drain; crossing either cap terminates the Job and fails rather than truncating a successful result. The parent creates one fresh protected synthetic profile beneath its protected temp root and exact empty `AppData\Roaming`, `AppData\Local`, and `ProgramData` children. The child environment contains only trusted `SystemRoot`/`WINDIR`; protected `TEMP`/`TMP`; `HOME`/`USERPROFILE` pointing to that synthetic profile; matching derived `HOMEDRIVE`/`HOMEPATH`; protected `APPDATA`/`LOCALAPPDATA`/`PROGRAMDATA`/`ALLUSERSPROFILE`; and `PATH=<held-runtime-directory>;<held-Program-Files-Git-bin>;<System32>`; plus exact `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=NUL`, `GIT_OPTIONAL_LOCKS=0`, `GIT_TERMINAL_PROMPT=0`, and `GIT_NO_REPLACE_OBJECTS=1`. It contains no real-home variable, caller value, or other Git/Python key. Python starts with `-I -B -S -X pycache_prefix=<fresh-cryptographic-nonce-nonexistent-path>` plus one fixed `-c` bootstrap. Before importing any workspace/dependency module, the bootstrap verifies isolated/no-site/safe-path/no-write flags, the exact four-entry startup path and resolved held identities, receipt facts, synthetic `Path.home()`/environment identities, and its own loaded non-SystemRoot set, emits one nonce-bound ready record, and blocks on the parent capability. The parent independently enumerates the live process modules and requires exactly the lane's held snapshot process image, `python313.dll` or `python310.dll`, and held `VCRUNTIME140.dll` outside SystemRoot; receipted `python3.dll` and `VCRUNTIME140_1.dll` are not early modules and may load only later for stable-ABI or standard-library/dependency extensions. Only then does the parent return the capability. Every later non-SystemRoot module, including any `python3.dll` or `VCRUNTIME140_1.dll`, must resolve beneath the retained runtime or dependency snapshot and equal a receipt-owned tree identity; all loaded modules are reconciled after exit. After the direct process signals, the parent captures exit, joins drains, closes thread/process/pipe handles, requires Job `ActiveProcesses == 0`, closes the Job, and only then accepts stdout/stderr/exit; timeout or any descendant/module/namespace/output-cap disagreement terminates the complete Job and fails.
- The bootstrap requires the fixed `_pth` worktree/dependency entries to resolve to the retained authenticated roots in exact order and dispatches through `runpy` only modules `unittest`, `sg_preflight.bundle_manifest_cli`, or `sg_preflight.ramses_probe_runner`, or tracked workspace scripts `scripts/build_sgfx_exe.py` or `scripts/benchmark_qt_quick.py`. The launcher accepts only these target-specific argument languages: `unittest` is either exact `discover -s tests -v` or one or more canonical dotted `tests.test_*` selectors, optionally ending in one `Test*` class and one `test_*` method, followed by terminal `-v`; `bundle_manifest_cli` is exactly `inspect-pair --pointer cpp/runtime-pairs/current.json`, `inspect-v2 --path build/b/sgfx-preflight/bundle-manifest.json`, `recover-publication --expected-authorization-sha256 <64-lowercase-hex>`, `build-native-release --expected-native-build-provenance-sha256 <64-lowercase-hex>`, `build-and-stage-runtime-pair --expected-native-build-provenance-sha256 <64-lowercase-hex>`, or `initialize-native-provenance` followed in this exact order by `--expected-tooling-commit <40-lowercase-hex>`, `--expected-primary-python-provenance-receipt-sha256 <64-lowercase-hex>`, `--expected-native-dependency-source-binding-sha256 <64-lowercase-hex>`, and exact 64-lowercase-hex SHA-256 arguments for the native-build launcher, native-build trust test, publication-recovery wrapper, pair-stage script, `cpp/CMakeLists.txt`, `sg_preflight/ramses_probe_publication.py`, `sg_preflight/bundle_manifest.py`, and `sg_preflight/bundle_manifest_cli.py`; `ramses_probe_runner` is exactly one of the two fixed `--inspect-current` relative pointers or `--request` plus one held canonical request leaf beneath the authenticated current attempt root; `build_sgfx_exe.py` is exactly `--staged-only`; and `benchmark_qt_quick.py` is exactly control-center status with optional `--reference-ready`, or `warm-start`/`first-run` against the held staged bundle. The four mutating `bundle_manifest_cli` forms are legal only under `-ExpectedSourceCommit`, never a development manifest. The launcher binds exact target-specific write exceptions: initialization may touch only the publication-root initializer, publication journals/bootstrap leaves, native dependency `.next`/final roots, and journal-owned provenance-candidate staging/final leaves; recovery may touch only the one external-authorization-bound transaction's journal, staging/final/completion/pointer/tombstone identities; `build-native-release` may touch only its publication journal/bootstrap plus one `build/ramses-r0-release/build-<32hex>.next` and final root; and `build-and-stage-runtime-pair` may touch exactly its `release_build`, `runtime_pair`, and applicable retention journals/bootstrap leaves plus that fresh build root and one `cpp/runtime-pairs/pair-<32hex>.next`/final/current/tombstone set. Each form requires the publisher's complete reconciliation before the ordinary after-source proof. No other source/generated exception is accepted. After capability and before target import, the bootstrap installs one fixed `shell=False` subprocess policy over `Popen`/`run`; every later import observes that same policy. It opens and retains each executable identity before creation and allows only target-declared descendants: the same receipted snapshot Python through a recursive fixed child bootstrap that normalizes `-I -B -S` while preserving the requested `-m`/`-c` argv/stdin/stdout/stderr/exit contract; the held Program-Files Git image and its five DLLs for unittest/benchmark local operations; receipt-owned dependency tools; prelaunch-held plan-named test helpers; an active package builder/benchmark's securely registered held staged executable; or the native-build coordinator's provenance-listed tool images plus securely registered compiler-ID/test/helper images beneath its held build root. Manifest inspectors, recovery, initialization, and the pair-stage phase permit no descendant. `build-native-release` and the build phase of `build-and-stage-runtime-pair` retain the `release_build` journal/mutex/build-root handles in the same Python process while creating only the provenance-listed CMake/toolchain images and pre-registered compiler-ID/test/helper descendants. Before each child, that coordinator sends the nonce-bound held image/DLL identities through the inherited audit pipe; Task 1's outer launcher retains and monitors the one inherited no-breakaway Job through active-zero, and any unregistered process/image/module or registration race fails the whole operation. The protected runner permits only Task 7's separately audited contained-process path. The policy writes nonce-framed start/identity/exit records to the inherited audit pipe, recursively passes that pipe only to snapshot-Python children, rewrites the sole allowed bare `git` token to the held absolute image and rejects shell use, every other path search, environment additions outside the frozen target-specific child grammars, or unlisted handle inheritance, and reconciles every direct/recursive record with Job `TotalProcesses`, active-zero, loaded non-SystemRoot identities, and before/after held namespaces. A bypass through `ctypes`, `QProcess`, an inherited raw `Popen`, or an unregistered grandchild changes Job accounting without an audit record and fails the outer invocation even if tests report green. Fixed parent deadlines from process creation through ordered cleanup are respectively 1,800 seconds for `unittest`, 120 seconds for either manifest inspection or publication recovery, 1,800 seconds for native-provenance initialization, 1,800 seconds for `build-native-release`, 2,100 seconds for `build-and-stage-runtime-pair`, 180 seconds for the protected runner, 1,200 seconds for the package builder, and 600 seconds for the benchmark; no caller timeout override exists. Reject absolute test selectors, separators, `..`, unknown switches, reordered/duplicated tokens, and every other argument before Python starts. Tests cover exact deadline/output-cap boundaries, continued drain without accepted truncation, complete Job termination, and ordered resource cleanup.
- Inventory authorities are separate and never substituted. The held worktree import root must equal authenticated recursive HEAD records under the frozen expected-commit built-in Git-clean transform, with additional raw blob equality for every explicit `-text` security input, plus only the exact current-task manifest exception below during precommit `unittest`. The runtime and dependency roots must equal their receipt identities and complete tree-digest preimages, never HEAD. Reject a dependency top-level collision with a worktree-owned import name, any worktree importable file absent from HEAD/current-task manifest, and any runtime/dependency file absent from its receipt tree. Re-enumerate and rehash all three roots after the child.
- The sole precommit exception is canonical `.superpowers/sdd/r0-task-development.json`. Its exact top-level keys are `schemaVersion`, `task`, `baselineCommit`, and `files`; schema is 1, task is an integer 1-12, baseline is the authenticated current 40-hex HEAD, and `files` is UTF-8-path-byte sorted with at most 256 objects exactly `{path,size,sha256,volumeSerialNumber,fileId}`, at most 8 MiB each and 64 MiB combined. Paths must be a nondeleted Create/Modify path from the selected Task's frozen `Files` list; the launcher contains the exact task-to-path allowlists and accepts no caller extension. Task 1 has a stricter phase split: before its clean snapshot-binding commit, no development manifest or Python execution is permitted; afterward, its allowlist excludes `.gitattributes`, `scripts/run_sgfx_isolated_python.ps1`, `scripts/sgfx_isolated_python_bootstrap.py`, `scripts/test_sgfx_isolated_python_trust.ps1`, and `config/sgfx-primary-python-3.13-provenance.json`, which must remain exact HEAD bytes. PowerShell-only mode `-RefreshDevelopmentManifest -DevelopmentTask <1..12>` runs the real-Git proof, requires every current difference to be a nondeleted allowlisted path, opens and hashes those identities, securely replaces only that fixed manifest, and launches no Python. A dirty `unittest` invocation must pass `-DevelopmentTask <same>`, authenticate the fixed manifest before and after, and permit exactly its records; no `-DevelopmentManifest` path parameter exists. The manifest is forbidden for scripts, builders, inspectors, verifiers, packaging, evidence, Task 13, and `-ExpectedSourceCommit` mode. Missing/stale/wrong-task/wrong-baseline/extra/deleted/drifted records fail before Python; a successful task commit makes the old manifest stale automatically. Every precommit Python command below explicitly refreshes and supplies its Task number.
- Immediately after the clean no-Python bootstrap commit and before primary-snapshot initialization, any Python launch, or any development manifest, Task 1 runs one PowerShell-only `-InitializeIgnoredCacheCleanup -DevelopmentTask 1` mode through the independently held caller binding. It permits only HEAD-untracked `*.pyc`/`*.pyo` one-link regular leaves inside exact source-root `__pycache__` directories, caps them at 1,024 files and 64 MiB, pins and hashes every identity, deletes each without following links, removes only then-empty held cache directories, and never deletes a HEAD entry or any other unknown/importable source. Before the first deletion it publishes fixed append-only `.superpowers/sdd/r0-task1-cache-cleanup-journal.jsonl` through the common recoverable header-bootstrap contract, capped at 256 MiB and 400,000 canonical events. Its header binds schema, the caller-supplied independently reviewed bootstrap commit/four-file `{path,size,sha256}` array, held worktree and cache-parent identities, the complete sorted cache inventory and before-tree digest, fixed receipt names, and mutex identity. Each file deletion and then-empty directory deletion has a flushed intent containing the exact retained identity/parent/inventory fact before mutation and a flushed observed result afterward. A failed or interrupted initialization permanently blocks a new normal initialization, snapshot initialization, manifest refresh, floor preparation, and every Python start until the explicit recovery mode below completes; absence of a result alone never authorizes a second attempt.
- Fixed `.superpowers/sdd/r0-task1-cache-cleanup-recovery-authorization.json` is a protected no-replace canonical director-approved record for exactly one interrupted cleanup or receipt-publication state. It binds action `resume_cache_cleanup`, the exact byte length and SHA-256 of each journal's last durable prefix, remaining exact leaf/directory inventories and held parent identities, reviewed bootstrap binding, both journal/mutex identities when receipt publication has begun, observed ancestry/DACL facts, and approval timestamp. `-RecoverIgnoredCacheCleanup -DevelopmentTask 1 -ExpectedCacheCleanupRecoveryAuthorizationSha256 <64-lowercase-hex>` is the sole recovery mode; it accepts no path and launches no Python. Under the unchanged mutex it reopens the fixed authorization and applicable journal, validates every completed result, and resumes only the unique next durable intent. Present-after-intent is revalidated and deleted; missing-after-intent is recorded as a recovered result only when the exact parent, prior retained inventory, authorization, journal prefix, and mutex remain unchanged and no unknown sibling exists. The same rule applies leaf-first and then to an empty held cache directory. Missing-before-intent, replacement, extra inventory, ancestry drift, wrong bootstrap, or journal/auth ambiguity stops without mutation.
- After every cache leaf and eligible empty directory has a terminal result and the complete ordinary source proof passes, the initializer or recovery mode appends and flushes the terminal `cleanup_complete` event and hashes those exact immutable cleanup-journal bytes. Before touching either receipt leaf it publishes a second fixed append-only `.superpowers/sdd/r0-task1-cache-cleanup-receipt-journal.jsonl` through the common recoverable header-bootstrap contract. That header binds the terminal cleanup-journal size/digest, exact canonical receipt bytes/size/digest, held parent/mutex identity, and fixed `.superpowers/sdd/r0-task1-cache-cleanup.next` and `.superpowers/sdd/r0-task1-cache-cleanup.json` names. It then appends and flushes `receipt_stage_create_intent`, creates the fixed `.next` leaf with `CREATE_NEW`, pins its identity, and appends and flushes `receipt_stage_created` with the observed identity before any receipt byte is written. It appends and flushes `receipt_write_intent`, writes or truncates and rewrites that same held identity from offset zero to the exact expected bytes, flushes/reopens/reparses them, and appends and flushes `receipt_written`. It next appends and flushes `receipt_rename_intent`, atomically renames that identity with no replace to the final receipt, appends and flushes `receipt_renamed`, reopens/revalidates the final bytes and identity, and appends and flushes `completed` before final source/generated-root reconciliation.
- The canonical receipt is capped at 1 MiB and has exact top-level keys `schemaVersion`, `bindingCommit`, `bootstrapFiles`, `removedFiles`, `beforeCacheTreeSha256`, `afterCacheTreeSha256`, and `cleanupJournalSha256`. Schema is 1; removed records are exact `{path,size,sha256,volumeSerialNumber,fileId}` objects with NFC `/`-separated held-worktree-relative paths sorted by UTF-8 bytes; each tree digest is SHA-256 over UTF-8 canonical JSON of `["sgfx-task1-cache-cleanup-v1",[[path,size,sha256,volumeSerialNumber,fileId],...]]`; the before list is the cleanup header inventory and the after list is empty. The receipt uses the primary receipt's canonical ASCII-key/sorted-key/`ensure_ascii=true`/compact-separator encoding plus one terminal LF and binds the terminal cleanup-journal digest.
- Receipt-publication recovery uses only the fixed authorization, both immutable journal prefixes, retained identities, and the common chained-suffix rules. After a create intent without its result, exact final-absent plus a same-parent zero-length one-link regular `.next` leaf may be recorded as the recovered create result only when its identity is authorization-bound; any nonzero, linked, replaced, or alternate leaf stops. After a crash during receipt write, the durable create result authorizes reopening only that same identity and deterministically truncating/rewriting the expected bytes from offset zero before continuing. After a rename intent without its result, `.next`-absent plus final-present is accepted only when the final has that same retained identity and exact expected bytes; it is then recorded as the recovered rename result. The cleanup journal remains terminal and immutable throughout. The receipt journal and any recovery authorization remain immutable retained one-shot audit evidence; a second cleanup is permanently forbidden. Snapshot initialization requires the exact completed receipt journal, final receipt, terminal cleanup journal, external bootstrap binding, and empty after inventory before any source/base/dependency copy. Tests crash before and after every leaf/directory intent/result, cleanup-journal terminalization, receipt-journal bootstrap, receipt create/result, every partial receipt write, flush/reopen/result, rename/result, final reopen/completion, and final proof; they cover repeated recovery crashes and forge arbitrary ignored `.py` plus timestamp/size-valid cache and every journal/authorization/receipt field. They poison every `PYTHON*` variable, current directory, user site, `.pth`, sitecustomize, extension module, selector, and PyInstaller hook seam. No sentinel may execute or enter a package.
- Task 1's Git boundary never trusts caller `PATH`, environment, config, index, ignore, replacement-object, or repository redirection. Resolve with `FOLDERID_ProgramFiles` and launch only held `<ProgramFiles>\Git\mingw64\bin\git.exe`; explicitly reject the `cmd\git.exe` shim, `PATH` candidates, aliases selected by the caller, and wrappers. Require a valid Windows Authenticode chain and frozen signer subject/thumbprint for the real executable. Bind exact held identities and SHA-256 for that image plus `libintl-8.dll`, `libiconv-2.dll`, `libpcre2-8-0.dll`, `libwinpthread-1.dll`, and `zlib1.dll`. For each identity, enumerate every hardlink alias through Win32, require the observed count to equal `nNumberOfLinks`, cap total aliases at 512 and eight per identity, require every alias and non-reparse ancestor to stay beneath the held protected Program Files Git root, reject broad-user write/delete/owner/DACL access and ADS/case collisions, and retain the file object without write/delete sharing through the complete Git session. A fixed stdin-held trust probe must observe exactly the real image and those five Git-root DLL identities plus trusted SystemRoot modules; every repository command must have Job total-process count one and no unexpected non-system image. Launch with `CreateProcessW`, no shell, a three-handle stdin/stdout/stderr allowlist, suspended assignment to a fresh queryable `KILL_ON_JOB_CLOSE` Job, a five-second deadline, and bounded drains. Start from an empty environment and add only trusted Windows/System32/TEMP values plus `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=NUL`, `GIT_OPTIONAL_LOCKS=0`, `GIT_TERMINAL_PROMPT=0`, `GIT_NO_REPLACE_OBJECTS=1`, and `GIT_ATTR_SOURCE=<ExpectedSourceCommit>`; no caller `GIT_*` key survives. Prefix every object-reading command with `--no-replace-objects --no-optional-locks --attr-source=<ExpectedSourceCommit> -c core.fsmonitor=false -c core.hooksPath=NUL -c credential.helper= -c core.attributesFile=NUL -c core.autocrlf=true -c core.eol=native -c core.safecrlf=false`. Before any attribute or object operation, resolve both the per-worktree Git directory and common Git directory by held identities, require each applicable `info/attributes` leaf absent, pin each `info` directory without write/delete sharing for the complete session, and re-enumerate/reject creation, replacement, case alias, ADS, or identity drift after every command. This absence requirement is independent of `core.attributesFile=NUL` and `--attr-source`, because repository-local `info/attributes` otherwise has higher precedence. Reject on-disk alternates, grafts, replacement refs, shallow ambiguity, unsafe local-config includes, index flags, and repository-format ambiguity. Capture NUL-delimited full recursive HEAD records `{mode,type,objectId,size,path}` with caps of 20,000 records, 512 MiB per blob, and 8 GiB total. Use trusted NUL-delimited `check-attr --source=<ExpectedSourceCommit>` with the protected local-attribute layer absent to record each path's exact expected-commit `text` and `eol` states and require `filter`, `ident`, and `working-tree-encoding` to be unspecified. Re-run and byte-compare that complete resolved-attribute map immediately before every `hash-object`, after every command, and at session close; an allowed built-in state is not sufficient unless it exactly equals the frozen expected-commit map. Stream each retained worktree file's exact bytes through the trusted Git process's bounded stdin to `hash-object --stdin --path=<path>` under that frozen expected-commit attribute/EOL policy and require the resulting repository-algorithm object ID to equal the HEAD record; this deliberately proves the current checkout's Git-clean bytes because live `core.autocrlf=true` means raw CRLF/mixed text is not generally byte-equal to the LF blob. Exact `-text` security inputs must also match the raw blob bytes. Compare a fresh protected index constructed from `ExpectedSourceCommit`, and independently enumerate every filesystem entry outside exact generated-root exclusions; neither caller index nor ignore metadata is authority. Hold and revalidate worktree, gitdir, common-dir, both protected `info` directories, object/runtime/file identities, canonical HEAD records, resolved attributes, alias inventory, and tree digests before and after each session. After any Git process signals, capture exit and join drains, close its thread/process/pipe handles, then require Job `ActiveProcesses == 0`, close the Job, and only then accept the command. Timeout, output overflow, runtime/signature/identity/source drift, unexpected child/module/argv/output, local-attribute creation, attribute/filter ambiguity, or any config/index/object/repository ambiguity fails closed before cleanup, Python, packaging, or evidence.
- The only generated-root exclusions from HEAD filesystem equality are exact held roots `.superpowers/`, `build/`, `cpp/bin/`, `cpp/runtime-pairs/`, `demo/good/operator_state/`, `dist/`, `operator_state/`, and `out/`; an absent listed root is valid, but no other untracked entry is. Enumerate and digest every existing excluded tree before and after each source-proof session. Tasks may change only their plan-declared subset: `.superpowers/sdd/r0-progress.md`; `.superpowers/sdd/r0-task-development.json` only through the fixed refresh contract; `.superpowers/sdd/r0-task1-cache-cleanup-journal.jsonl`, `.superpowers/sdd/r0-task1-cache-cleanup-receipt-journal.jsonl`, their exact same-parent `.r0-task1-cache-cleanup-journal.jsonl.bootstrap-<32hex>` and `.r0-task1-cache-cleanup-receipt-journal.jsonl.bootstrap-<32hex>` leaves, `.superpowers/sdd/r0-task1-cache-cleanup-recovery-authorization.json`, `.superpowers/sdd/r0-task1-cache-cleanup.next`, and `.superpowers/sdd/r0-task1-cache-cleanup.json` only through Task 1's one-time cleanup/recovery contract; `.superpowers/sdd/r0-python-primary-v1` and one authenticated sibling `.next-<32hex>` only during Task 1 initialization or authorized bound recovery; `.superpowers/sdd/r0-primary-recovery-authorization.json`, `.superpowers/sdd/r0-primary-recovery-journal.jsonl`, its exact same-parent `.r0-primary-recovery-journal.jsonl.bootstrap-<32hex>` leaf, authorization-bound primary-recovery tombstones, `.superpowers/sdd/r0-python-primary-recovery-receipt.json`, and immutable `.superpowers/sdd/r0-primary-recovery-history-v1` only during that recovery/finalization; `.superpowers/sdd/r0-python-floor-310-v1` and one authenticated sibling `.next-<32hex>` only during Task 12's optional floor preparation; `.superpowers/sdd/r0-native-dependencies-v1` plus one authenticated sibling `.next-<32hex>` only during Task 10 provenance binding; fixed `.superpowers/sdd/r0-publication-journal-root-init.jsonl`, exact same-parent `.r0-publication-journal-root-init.jsonl.bootstrap-<32hex>`, `.superpowers/sdd/r0-publication-journals-v1`, and its transaction-bound journal-bootstrap leaves only through the publication-root/journal contracts; journal-bound publication/retention tombstones only for explicit recovery or locked retention; development-only `build/cine-c0/`; protected `build/ramses-r0-release/`; `build/pyinstaller/`; `build/b/sgfx-preflight/`; `cpp/runtime-pairs/`; and `out/ramses-r0/`. Task 10's isolated `initialize-native-provenance` or authorized `recover-publication` operation alone additionally permits exact source-tree leaves `config/.sgfx-r0-native-build-provenance.json.next-<32hex>` and `config/sgfx-r0-native-build-provenance.json`; both are predeclared in the journal header, and no other `config/` difference is legal. Every other excluded root and every sibling entry inside a writable root must retain exact identity/inventory/digest. No excluded root enters Python search except the two exact receipt-owned runtime/dependency children; `.superpowers` itself is never appended. Before every Python or package operation, independently reject an untracked or mismatched `.py`, `.pyw`, `.pyc`, `.pyo`, `.pyd`, `.pth`, `sitecustomize`, `usercustomize`, or PyInstaller hook that can enter the selected target's actual resolution graph, regardless of generated root or ignore metadata. Tests poison every exclusion and prove only the exact declared write set may change.
- R0 is noninteractive. It adds no Home page, QML control, capability, renderer viewport, guided tour, or continuously interactive 3D claim. Evidence-linked guided 3D review belongs to R4.
- Preserve exactly eight UI capability IDs, 19 surface descriptors, the profile-neutral Control Center, and the existing finite C0 turntable/fallback behavior.
- Do not modify sg_preflight/qa_actions.py, sg_preflight/qa_hub.py, or action/QML wiring in this standalone plan. The four-pack sgfx_preflight action remains exactly anchors, constants, carpaints, and project_sanity.
- The one-button ramses_r0 stage is a separate later integration plan and may begin only after the Standalone Acceptance Gate at the end of this plan passes. Passing that gate does not graduate the R0 program phase. The frozen order is standalone acceptance -> separately reviewed one-button integration implementation and acceptance -> every remaining section-24 OPEN/DEFERRED item adjudicated, including representative timing closed or spec-amended -> teammate pilot of that accepted integrated workflow -> R0 phase graduation. Standalone use, tests, or pre-integration feedback cannot satisfy that pilot. Default cutover and distribution remain separately authorized after phase work.
- Use only the installed Ramses 28.16.0 public headers and existing lawful local SGFX code as implementation authority. Do not copy a proprietary implementation.
- Use one backend only: the CLI token `opengl`, mapped exactly to the already proven Ramses `EDeviceType::GLES_3_0` path. OpenGL/Vulkan comparison and GL 4.x selection belong to R2.
- Use at most one 480 x 270 authored-frame readback when its exact authored contract exists. It is evidence, never visual approval. R0 does not create or mutate a camera, render pass, source scene, perspective, or baseline.
- Drive only the exported `Interface_CameraCrane` contract when an explicitly validated perspective file and perspective ID are supplied. The perspective JSON key `AspectFromResolution_isEnabled` maps only to the proven runtime property `AutoAspect`; do not alias or fall back to the C0 prototype spelling. If the perspective input, exact root, or exact property tree is absent, continue metadata/validation/inventory/logic and report `authored_pass_or_camera_contract_missing` with no renderer/PNG; never fall back to direct camera mutation.
- Each executed real lane has one parent-owned `QueryPerformanceCounter` origin and `QueryPerformanceFrequency`. For receipt, the source-verifier worker takes them immediately before final protected-request serialization, fixed native command construction, and `CreateProcessW`. For product, that same source-verifier worker takes them immediately before final protected-request serialization, fixed frozen-host command construction, and the outer `CreateProcessW`; request serialization, outer process creation, PyInstaller loader/Python/CRT startup, delayed resume, inner command construction, inner native startup, packaged native/frame/log transport validation, outer status emission, direct-process signal and exit capture, drain joins, thread/process/pipe closure, whole-Job active-zero observation, Job closure, and lease release all consume the same contained-process budget in that order. Source-only final-report/evidence construction begins after those contained-process facts and is not mislabeled as child-process timing. The parent writes exact unsigned `parentStartQpc`, `qpcFrequency`, `nativeWorkDeadlineQpc = start + 58 seconds`, and `parentDeadlineQpc = start + 60 seconds` into a parent-generated protected envelope that public verifier parameters cannot select. Whole-second deadlines use checked integer multiplication/addition; every subsecond boundary is `start + floor(qpcFrequency * boundaryMilliseconds / 1000)` with checked wide intermediates, never floating point. Reported elapsed milliseconds are the checked integer ceiling of `elapsedTicks * 1000 / qpcFrequency`, while all pass/fail comparisons use raw ticks. The frozen host, inner runner, and native child each validate and forward the four QPC values unchanged; none may recompute or rebase them.
- The source-verifier worker owns the named Ramses lease, one query-capable `KILL_ON_JOB_CLOSE` Job, process/pipes, and output identities for the complete lane. Receipt assigns the suspended native process to that Job before first instruction. Product assigns the suspended frozen host before first instruction; its no-breakaway native descendant inherits the same Job, and the host neither acquires a second lease nor creates a second Job. Native uses only the supplied absolute deadlines and stops new work at the work deadline. Its authored-render deadline is `min(nativeWorkDeadlineQpc, authoredRenderStartQpc + 30 seconds)` and covers perspective application, update, tag flush acknowledgement, Rendered, and readback. At `parentDeadlineQpc`, any still-active contained process causes `TerminateJobObject`. On normal exit or after termination, the worker first waits for the direct process, captures its exit, joins its drains, and closes thread/process/pipe handles. It retains only the query-capable Job, Ramses lease, and separately owned artifact/input/runtime handles while querying exact `ActiveProcesses == 0`; only then does it close the Job and release the lease. A lane is process-timing PASS only when that complete order finishes no later than the raw deadline. The caller receives timeout failure after at most two reap-only seconds, while the worker keeps the remaining Job/lease barrier until the same ordered cleanup completes; a query/termination failure keeps that barrier owned and prevents another Ramses acquisition. Original non-inheritable artifact handles and immutable input/runtime handles remain live for post-process validation/publication after process cleanup. Strict native/final/evidence re-read, held source/helper/runtime rehash, and the independent after-Git snapshot are acceptance gates outside the design's 60-second contained-child measurement; each remains bounded by its existing byte/cardinality cap and Git's own five-second Job. They are skipped after a process timeout except for safe retained-handle diagnostics, so they cannot delay the two-second timeout notification or be mislabeled as requirement-27 timing. Process abort relies only on emergency `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` and produces no accepted completion. Tests prove `ActiveProcesses` remains nonzero while a signaled direct-process handle is retained, becomes eligible for zero only after that handle closes, delay both outer and inner resume/startup beyond every boundary, crash/terminate the host, keep a descendant alive beyond caller grace, and prove a second lease cannot acquire before exact active-zero and Job closure. These budgets remain OPEN until measured on the named representative profile/source pair; do not relax them without an evidence-backed spec amendment.
- Native JSON is capped at 2 MiB after JSON escaping, final JSON at 4 MiB after JSON escaping, isolated evidence at 512 KiB, each retained native console stream at 256 KiB, the one 480 x 270 PNG at 1 MiB, and each executed `real/(receipt|product)` subtree at 9 MiB including its protected request. The deterministic no-private run executes native/C++ synthetic tests but no fabricated product or receipt scene; real helper execution remains OPEN until the complete approved canonical input set exists. A real attempt executes exactly both `real/receipt` and `real/product`. Root verification/control artifacts are capped at 4 MiB, so the constructive maximum successful attempt is 22 MiB and the absolute complete-attempt cap remains 32 MiB. Tests construct both maximum real subtrees plus root artifacts and prove the aggregate stays within both limits. Input limits are exact: perspective JSON 1 MiB, protected request/receipt JSON 256 KiB each, JSON depth 8 for perspective and 16 for protected control/final/native documents, at most 256 perspective views, 32 properties per selected view object, 128 UTF-8 bytes per key, and 4,096 UTF-8 bytes per string. Inventory preserves exact total counts, at most 256 sorted object records per type, and at most 2,048 records globally, with explicit reported/truncated counts. Each contained Git command retains at most 4 MiB while draining the rest; any overflow fails the exact source proof rather than inventing a clean state. Task 1 assigns serialized section budgets whose sum leaves at least 512 KiB for structural syntax and uncapped scalar fields; every capped list preserves anomaly truth before truncation. Maximum-shaped serialization tests prove both report caps constructively with worst-case JSON escaping.
- The only accepted native-child outputs are `ramses-probe-native.json` and, after a successful readback, `first-frame.png`. Parent Python securely creates the protected `request.json` before launch and may publish only `stdout.log`, `stderr.log`, `ramses-probe-report.json`, and `ramses-r0-evidence.json` for that executed lane after the applicable validation stages; Global Constraints' run-manifest rules freeze which prefix is legal on failure. No other executed-lane leaf is accepted.
- The native report contract and final `RamsesProbeReport` contract are separately versioned as schema 1; probe version is 0.1.0. Unknown fields, enum values, phase order, output files, helper names, profile identities, scenario identities, source fingerprints, or artifact paths fail closed.
- Python owns helper/runtime, scene, and optional perspective hashing, the canonical path-free `sourceFingerprint`, the current generation/run token, and the final source-mutation verdict. The native helper only echoes adapter-supplied identity claims; Python independently verifies every claim before accepting the native facts.
- A packaged helper is trusted only through the one canonical strict adjacent bundle-manifest schema-2 loader. A development helper is unavailable to normal product resolution; the standalone verifier may supply it only with a protected build receipt tied to the current clean commit, build target/configuration, exact schema digests, exact helper/runtime digests, and Task 10's independently reviewed native-build provenance receipt. Development RED/GREEN tests may use `RelWithDebInfo` from the existing configured tree only as nonacceptance TDD evidence. Unit/fixture pair checks run before a task commit; a strict real stage runs only afterward on a clean commit. No Release provenance operation configures, builds, stages, packages, or verifies from `build/cine-c0` or any pre-existing generated project. The receipt and staged acceptance candidate use one freshly configured and built `Release` preview/R0 runtime pair from the exact authenticated source commit, protected dependency snapshots, and held native toolchain described below, with the reviewed MSVC path-remapping/PDB contract applied to every local and dependency target. Both receipt helpers and the four runtime DLLs occupy one authenticated app-local `bin` directory; the packaged copies likewise occupy one `_internal/cpp/bin` directory. The native report records path-free volume/file identities for its process image plus exact SDL3 and three Ramses modules copied while each module is loaded; Python reconciles them to the retained pair/manifest identities and digests before acceptance. Missing, duplicate, ambient, or competing same-name modules fail even if process startup succeeded.
- Exact scene and optional perspective files are hashed before and after through retained file handles. Retain a handle for every validated intermediate directory component and for the runtime namespace until the contained Job completes; enumerate alternate streams before and after. Every distinct source root and the helper runtime root must be disjoint from the output root. A containing worktree's exact HEAD/blob/index/filesystem proof is captured before and after only by Task 7's contained trusted Git lane; a non-Git input or unavailable trusted Git records `exact_hash_only`, never an invented clean state.
- No raw console error, private scene/perspective/source-input path, resolved generated output-artifact path, URL, environment, credential, command, process handle, BMW scene, perspective payload, or derived frame enters QML, a committed fixture, a ticket, a handoff, or a package. Repository-relative engineering paths may appear in plans/tickets. Canonical crash-recovery records may additionally name only the active worktree, canonical coordination files, the fixed ignored ledger `.superpowers/sdd/r0-progress.md`, and the fixed resolver tokens `R0_CURRENT` and `C0_CURRENT`; they store sanitized run IDs, artifact roles, and digests rather than resolved JSON/Markdown/frame/log/private paths. `R0_CURRENT` and `C0_CURRENT` mean that a future recovery session authenticates and resolves the fixed `out/ramses-r0/current.json` or `out/ramses-r0/c0-current.json` contract locally; the literal resolved pointer or descendant path is never copied into a handoff. No exception permits a private probe input, generated evidence descendant, or real lane path. Privacy verification scans every staged regular file and every recursively decoded logical member of the exact supported PyInstaller CArchive, PYZ, and ZIP/base-library containers without importing, unmarshalling to executable code, or executing any member. Both physical and logical streams use a carry of `max(encoded_marker_length) - 1` bytes, computed over every dynamically derived home/worktree/source/build/dependency/install marker and every slash-normalized, URI-encoded, JSON-escaped, case-folded, UTF-8, and UTF-16LE representation; an equivalent proven streaming matcher is permitted. The parser rejects an unknown archive version, malformed/truncated/overlapping/trailing/encrypted member, duplicate or case-colliding logical name, unsupported compression, inconsistent CRC/size, or nested archive deeper than three. Caps are 5,000 physical files and 1 GiB physical bytes, 10,000 logical members and 2 GiB aggregate decompressed bytes, and 64 MiB per logical member; every cap is checked before allocation/decompression and violation fails privacy. Extension allowlists are forbidden. Tests place every longest representation across every possible physical/decompressed chunk split offset, including UTF-16LE code-unit boundaries, and prove a marker visible only after CArchive/PYZ/ZIP decompression fails.
- Synthetic fixtures contain no BMW data or private workstation paths. A real smoke uses an explicitly supplied local scene/perspective; all probe execution/evidence artifacts remain in one unique ignored `out/ramses-r0` root. Development-only native tests may write the authenticated ignored `build/cine-c0` tree. Release provenance may write only one protected `build/ramses-r0-release/build-<32hex>.next` and its authenticated final root, protected dependency/toolchain receipts below `.superpowers/sdd`, `build/pyinstaller`, `cpp/runtime-pairs`, and `build/b/sgfx-preflight` as required below; temporary test outputs are removed and every other write is rejected. No frame enters git or delivery records.
- A validation issue is a finding, not a helper crash. Duplicate or empty object names never define identity; exact source fingerprint plus object type and scene object ID do.
- Missing helper, scene, prelaunch runtime prerequisite, or an explicitly named but missing/unreadable perspective is unavailable with no report. A valid `opengl` request whose public device support check fails completes an exit-0 report with `display_or_offscreen_setup_failed`. A malformed/non-finite supplied perspective is rejected as invalid with no report. No perspective supplied, or a readable exact perspective that lacks the requested ID/property/runtime interface, becomes the complete `authored_pass_or_camera_contract_missing` report after earlier phases. Malformed output, helper/runtime mismatch, source mutation, output escape, timeout, or crash is an execution failure. Neither state changes four-pack evidence.
- The product remains usable without the optional helper. No Jira, Clockodo, SVN, BMW, Git push, CI, rack, vehicle, network, package publication, source export, approval, or external mutation is part of this plan.
- Keep the current no-argument packaged default as dashboard run --ui-mode clean. This plan builds and verifies a staged candidate only.
- Follow RED then minimal GREEN for every production behavior. Independently review each task and record only fresh tool evidence.
- Tasks 1-9 development RED/GREEN native tests may use the already configured `build\cine-c0` tree after confirming its `CMAKE_HOME_DIRECTORY` is this worktree's `cpp` directory; those tests never confer Release provenance. Task 10 creates `scripts/run_sgfx_r0_native_build.ps1`, `scripts/test_sgfx_r0_native_build_trust.ps1`, and canonical tracked `config/sgfx-r0-native-build-provenance.json`. The reviewed schema-1 provenance receipt binds the earlier clean Task 10 native-build-tooling commit and exact launcher/test bytes; held signed CMake image/version; Visual Studio instance/product/version; Windows SDK version; exact signed image/size/SHA-256/volume/file ID/signer for CMake, MSBuild, `cl`, `link`, `lib`, `rc`, `mt`, `cvtres`, `mspdbsrv`, and every other process image observed by the fixture trace; exact Ramses install package/config/header/runtime tree; and protected source-tree receipts for nlohmann_json, SDL3, FreeType, and RmlUi. Each dependency receipt freezes lawful origin/version, complete canonical tree digest, file/byte caps, license digest, and protected fixed-root identity. The bootstrap copies dependency sources once through held handles from independently reviewed local source inventories into `.superpowers/sdd/r0-native-dependencies-v1.next-<32hex>`, then no-replace publishes `.superpowers/sdd/r0-native-dependencies-v1`; it never trusts a generated CMake project or executes code from the source tree during snapshot construction. A second clean Task 10 binding commit adds only the reviewed provenance receipt. Every later build receipt separately binds that current exact source commit and requires the tracked provenance receipt to still name the unchanged tooling commit/bytes.
- Every accepted Release build starts from an absent final build name beneath protected `build/ramses-r0-release`, securely creates one nonce `.next` root, and runs a fresh configure from authenticated `cpp` with absolute held CMake and exact `-G "Visual Studio 17 2022" -A x64`; `-DSGFX_R0_PROTECTED_DEPENDENCIES=ON`; `-DFETCHCONTENT_FULLY_DISCONNECTED=ON`; `-DFETCHCONTENT_TRY_FIND_PACKAGE_MODE=NEVER`; `-DCMAKE_FIND_USE_PACKAGE_REGISTRY=OFF`; `-DCMAKE_FIND_USE_SYSTEM_PACKAGE_REGISTRY=OFF`; `-DCMAKE_EXPORT_NO_PACKAGE_REGISTRY=ON`; exact `FETCHCONTENT_SOURCE_DIR_NLOHMANN_JSON`, `FETCHCONTENT_SOURCE_DIR_SDL3`, `FETCHCONTENT_SOURCE_DIR_FREETYPE`, and `FETCHCONTENT_SOURCE_DIR_RMLUI` values fixed to the four protected receipted roots; and the held exact Ramses config root. In that mandatory production mode `cpp/CMakeLists.txt` does not call `find_package` for SDL3, RmlUi, or Freetype, makes all four local sources available without download/update or package fallback, and fails unless every resulting target's source root equals its retained receipt. Ramses remains a separately validated exact CONFIG package and OpenGL remains the authenticated Windows SDK/system target. This proves offline dependency resolution and that SGFX requests no fetch/update; it does not claim an OS egress sandbox around CMake/MSBuild/compiler processes. Native provenance and build receipts record `osEgressContainment=not_claimed`, the verifier never emits an OS-network-denial PASS, and tests must not treat an unobserved direct socket attempt as blocked. Missing local dependency bytes or any attempted CMake fetch/update fails before compilation.

  Every compiler-visible retained root receives a fixed logical `/pathmap` prefix before any dependency subdirectory or target is configured: project source -> `S:\\sgfx\\src`; protected nlohmann_json/SDL3/FreeType/RmlUi roots -> `S:\\sgfx\\deps\\<role>`; fresh build root -> `S:\\sgfx\\build`; Ramses install root -> `S:\\sgfx\\sdk\\ramses`; Windows SDK root -> `S:\\sgfx\\sdk\\windows`; held Visual Studio instance/toolchain root -> `S:\\sgfx\\toolchain\\msvc`; and held CMake install root -> `S:\\sgfx\\toolchain\\cmake`. Tracked native-build provenance freezes only the static `pathMappingPolicy`: the ordered role and logical-prefix vocabulary, longest-root-first/overlap rules, immutable dependency/SDK/toolchain identity references, and deterministic trace-root classification/token policy. It never contains a current project/build identity, project/build tree digest, or actual future trace-root set. Any additional compiler include/library root observed by a retained capability/process trace must instantiate that deterministic policy beneath an authenticated static root role; an unknown root outside those authenticated roles requires an independently reviewed provenance amendment rather than a wildcard. Per-build facts are derived only from retained identities, canonicalized by held identity, ordered longest-real-root first, and rejected for duplicate identity/logical prefix, alias-equivalent overlap, or ambiguous non-strict overlap; a declared strict ancestor relation is accepted only under that longest-first resolution. Each build receipt freezes the actual ordered mapping instances as `{role,logicalPrefix,rootVolumeSerialNumber,rootFileId,treeSha256Before,treeSha256After,policyRole}`, together with project-root identity/source before-and-after exact-tree digests, build-root identity/empty-before and post-build tree digests, and the retained trace-closure digest/result. Neither receipt exposes a real path. The Release gate scans every produced OBJ/PDB/PE/DLL, staged payload, and sanitized receipt for every real root in raw, alternate-slash, ordinal-case, URI-encoded, JSON-escaped, UTF-8, and UTF-16LE form at every streaming split boundary; any hit or unmapped traced root fails.

  CMakeCache, generated projects, response files, raw configure/build logs, compiler-ID files, and other path-bearing control metadata are protected ephemeral descendants of that one contained invocation. They are authenticated for process/build provenance but never staged, packaged, copied into evidence, or subjected to the public path-free payload claim; their sanitized receipt projection uses only fixed logical root tokens and digests. The absence-of-real-path gate applies to produced OBJ/PDB/PE/DLL payloads, staged artifacts, and sanitized public receipts. None of those control files or any output from `build/cine-c0` is read as a Release input. The isolated Python native-build coordinator uses `CreateProcessW`, an empty reviewed environment, bounded stdout/stderr, suspended creation into Task 1's already inherited no-breakaway outer Job before first instruction, and the nonce audit stream; it creates no nested Job. Before launch it holds every receipt-listed tool image and DLL, registers those exact identities with the outer launcher, and retains the `release_build` journal/mutex/build-root handles through ordered active-zero. The Job may execute only receipt-listed tool images plus explicitly registered compiler-ID/test/helper images created and rehashed inside the protected build root. Unknown image, module, child, response-file escape, source/dependency/toolchain drift, output cap, or timeout terminates the Job and rejects the build.

  The isolated Python native-build coordinator builds exactly the payload targets `sgfx_cine_ramses_preview_cli` and `sgfx_ramses_probe` plus test targets `sgfx_cine_ramses_probe_contract_test`, `sgfx_cine_ramses_probe_scene_test`, `sgfx_cine_ramses_probe_logic_test`, `sgfx_cine_ramses_probe_lifecycle_test`, `sgfx_cine_ramses_probe_synthetic_test`, `sgfx_cine_ramses_link_probe`, `sgfx_cine_ramses_preview_contract_test`, and `sgfx_cine_ramses_probe_cli_test`, then runs the exact corresponding Release CTest names. Only the two payload helpers enter the pair receipt. The canonical build receipt separately binds source commit, native-build-provenance digest, project-root volume/file ID plus source before/after exact-tree digests, build-root volume/file ID plus empty-before/post-build tree digests, the actual ordered mapping instances `{role,logicalPrefix,rootVolumeSerialNumber,rootFileId,treeSha256Before,treeSha256After,policyRole}`, retained trace-closure digest/result, tokenized configure/build argv, protected generated-root tree digest, the ordered eight test-target names/results and complete CTest log digest, both Release helper digests, four runtime DLL digests, schema digests, process audit digest, offline-dependency-resolution state, `osEgressContainment=not_claimed`, and ordered active-zero cleanup. R0 verification always creates a fresh protected build and never fetches; a missing/unusable reviewed toolchain or dependency snapshot is a truthful build-prerequisite failure.
- Treat local source/helper/output paths as user-controlled but not as a hostile-administrator sandbox. Reject Windows device prefixes, alternate data streams, reserved device names, trailing dot/space components, reparse points, non-regular files, output hardlinks, UNC roots, and roots whose `GetDriveTypeW` result is `DRIVE_REMOTE`; revalidate identities through held handles before and after. Enumerate streams with `FindFirstStreamW`/`FindNextStreamW` and accept only the unnamed data stream. Immutable file handles use `GENERIC_READ | FILE_READ_ATTRIBUTES` and `FILE_SHARE_READ` only; directory-component handles use `FILE_LIST_DIRECTORY | FILE_READ_ATTRIBUTES`, `FILE_FLAG_OPEN_REPARSE_POINT | FILE_FLAG_BACKUP_SEMANTICS`, and `FILE_SHARE_READ` only. One Python implementation, `SecurePublisher` in `sg_preflight/ramses_probe_publication.py`, is the sole byte/state owner for ordinary R0/C0 publication, one-time publication-journal-root initialization, the native-dependency snapshot and provenance candidate, and every authorized recovery transition. It creates every generated directory with `CreateDirectoryW` fail-if-exists then opens and pins its identity, and creates every generated leaf through unpredictable same-directory staging plus `CREATE_NEW`, `FILE_FLAG_OPEN_REPARSE_POINT`, regular/link-count-one validation, retained volume/file identity, handle-only writes, `FlushFileBuffers`, strict byte/schema revalidation, and handle-based no-replace rename. `sg_preflight.bundle_manifest_cli` supplies only fixed argument/schema orchestration; PowerShell launchers are held thin wrappers and never parse, encode, append, truncate, rename, delete, or reimplement a publication journal or candidate. `SecurePublisher` retains all accepted leaf and directory identities through manifest/completion/current publication and cleans only by held Windows identity; the POSIX development fallback uses `openat(O_EXCL|O_NOFOLLOW)`/`renameat2(RENAME_NOREPLACE)` and leaves a rejected orphan when safe identity-based removal is unavailable. Current-pointer replacement is a distinct locked unique-temp/flush/reparse/atomic-replace operation owned by the same implementation. Hash immutable content only through held handles and hold every component handle through consumption. Derive the Windows directory and System32 from trusted Win32 APIs rather than caller environment. Never recursively clean a rejected or unproven tree.
- The fixed `publication_journal_root` initializer above is a special `SecurePublisher` transaction outside the ordinary journal root; after it completes, every ordinary transaction owns a canonical bounded append-only journal beneath fixed protected `.superpowers/sdd/r0-publication-journals-v1`, keyed by transaction kind and 32-hex nonce and published through the common recoverable header-bootstrap contract. While holding the applicable pair/evidence/build mutex, it publishes the journal before the first child leaf; the header binds schema, transaction kind (`native_dependency_snapshot|runtime_pair|r0_run|c0_run|pointer_temp|package_stage|release_build|runtime_pair_retention|r0_retention|c0_retention`), protected parent/root identity, the ordered one-or-more staging/final/tombstone names and identities applicable to that kind, source commit, relation facts, and mutex identity. Every external mutation has a flushed intent before it and a flushed observed result after it. Creation results record exact volume/file ID, size/digest for leaves, and parent identity. Publication state advances only by append-and-flush through the applicable ordered states `creating`, `root_renamed`, `completion_published`, `pointer_replaced`, `reconciled`, and `completed`; kinds without a completion or pointer explicitly record the inapplicable transition before reconciliation. The `native_dependency_snapshot` header additionally binds the canonical candidate recipe/schema bytes and digest, every independently supplied tooling/source/receipt authority, exact derivation rules, absent proofs for final `config/sgfx-r0-native-build-provenance.json` and same-parent staging `config/.sgfx-r0-native-build-provenance.json.next-<32hex>`, and their held parent identity; it binds neither future protected dependency file IDs nor final candidate bytes. After `root_renamed`, `SecurePublisher` reopens the exact final dependency root and each fixed-role child, requires their identities and tree/license digests to equal the journaled creation results and recipe authorities, and deterministically derives the complete canonical candidate including the now-observed protected root volume/file IDs. Before candidate staging, it appends and flushes `provenance_preimage_bound` containing the recipe digest, ordered protected dependency identities, and exact candidate bytes/size/SHA-256. Recovery after `root_renamed` but before that record repeats the held-root derivation and appends the same record; after it is durable, every forward/recovery path recomputes and byte-compares it, while a torn suffix follows the common prefix-repair contract. The transaction then rolls forward only through `provenance_stage_create_intent`, `provenance_stage_created`, `provenance_write_intent`, `provenance_written`, `provenance_rename_intent`, `provenance_published`, `reconciled`, and `completed`. A partial write may truncate and rewrite only the journal-owned staging identity from `provenance_preimage_bound`; rename-before-result is accepted only when staging is absent and final is the same retained identity with those exact bytes. Runtime-pair retention advances through `candidate_authenticated`, `tombstone_rename_intent`, `tombstone_renamed`, per-entry `delete_intent`/`delete_result`, `parent_reconciled`, and `completed`; R0/C0 retention instead advances through `candidate_authenticated`, `completion_tombstone_rename_intent`, `run_tombstone_rename_intent`, `completion_tombstone_renamed`, `run_tombstone_renamed`, the per-root delete states, `parent_reconciled`, and `completed`. A final root is committed at `root_renamed`: before that state recovery may only roll back the authenticated staging root; at or after it recovery may only validate and roll forward, including provenance-candidate publication for `native_dependency_snapshot`. Completion and current-pointer operations are idempotently reconciled against their exact journal facts, never inferred from a name. No journal becomes completed before every required final-root/provenance-candidate/completion/pointer or retention-tombstone fact, complete relation/tree reconciliation, and handle closure passes. A completed native-provenance initialization rerun validates the journal, recipe, `provenance_preimage_bound`, dependency root, source binding, tooling bytes, and final candidate read-only and returns only the existing candidate digest. On restart, any noncompleted journal, bootstrap leaf, candidate staging leaf, or unknown `.next`/tombstone entry stops new publication and pruning before mutation.
- `scripts/recover_sgfx_r0_publication.ps1`, introduced and independently reviewed with Task 10's native-build trust tools before the first real dependency snapshot or pair, is the sole operator recovery entrypoint for publication-journal-root initialization, native-dependency-snapshot, pair, R0, C0, pointer-temp, package-stage, release-build, and retention transactions. It is a held thin wrapper around Task 1's isolated launcher and exact `sg_preflight.bundle_manifest_cli recover-publication` operation; it requires expected source commit, expected primary-Python provenance receipt digest, and recovery-authorization digest, accepts no path, and performs no journal parsing or mutation itself. The Python command derives and reads only external `C:\Users\DavidErikGarciaArena\Downloads\sg-preflight\out\agent-control\r0-publication-recovery-authorizations-v1\authorization-<ExpectedRecoveryAuthorizationSha256>.json`, requires its exact filename/content SHA-256 and protected-root identity, and accepts exactly one director-approved action: `resume_publication_journal_root_initialization`, `repair_one_journal_bootstrap`, `clean_one_publication_orphan`, `finalize_one_committed_publication`, or `resume_one_authorized_cleanup`. The record binds exactly one root-initializer/journal/bootstrap plus all of its authenticated staging/final/tombstone identities, fresh complete ancestry/DACL/inventory/tree digests, source/relation/tooling/receipt facts, mutex identity, and action. `resume_publication_journal_root_initialization` is legal only for the fixed special journal/root names and follows that initializer's exact intent/result/reconciliation contract. `repair_one_journal_bootstrap` is legal only before any protected mutation and follows the common bootstrap-repair/abort contract. `clean_one_publication_orphan` is legal only before a committed final-root rename: under the matching mutex it flushes the journal-bound cleanup intent, atomically no-replace renames the exact staging identity to its same-parent tombstone, flushes the result, and then uses the restartable cleanup protocol. `finalize_one_committed_publication` is legal only when the exact journal-owned final root already exists: it reopens and validates the complete final tree, then idempotently validates or publishes the exact missing provenance candidate, completion, and pointer transitions, reconciles their relation/digests, and appends completion; it never deletes or rolls back the committed root. For `native_dependency_snapshot`, finalization after `root_renamed` must derive or revalidate `provenance_preimage_bound`, then recover or publish exactly those candidate bytes before reconciliation/completion. If candidate, completion, or pointer bytes already exist they must exactly equal the journal; if the pointer was already replaced, that new pointer remains authoritative and recovery only rolls forward.
- `resume_one_authorized_cleanup` requires each applicable durable top-level tombstone-rename intent plus either its result or an exact original-absent/tombstone-present identity that can be recorded as a recovered result, in the journal's fixed root order. It flushes a delete intent before each descendant leaf and then empty descendant directory and a result afterward. Missing-after-intent for a descendant is accepted as a recovered deletion only while that root tombstone still exists with exact ancestry/inventory and unchanged authorization/journal/mutex. The top-level tombstone file or directory uses a distinct `tombstone_root_delete_intent` binding its retained parent identity, exact file content identity or empty-tree proof, and every terminal descendant result. If deletion succeeds but its result does not flush, original and tombstone both absent may be recorded as a recovered root deletion only when the parent/authorization/journal/mutex identities remain unchanged, all descendant results are terminal, and no unknown sibling exists; this special rule applies equally to a retention completion-file tombstone and a directory-root tombstone. Original/tombstone ambiguity, missing-before-intent, extra/replaced/live/current/referenced state, out-of-order second-root work, or a nonempty unlisted directory stops recovery. Authorization and journal remain until every bound tombstone root has a terminal root-delete result and a reconciled completion receipt/result exists. Tests crash before and after every creation, final rename, completion publish, pointer replace, each ordered tombstone rename, every descendant deletion, and each top-level root deletion for every transaction kind; they prove pre-commit rollback, post-commit roll-forward, post-pointer preservation of the new pointer, pre-pointer preservation of the prior pointer, deletion-before-result including root-delete-before-result, and rejection once the tombstone ancestry required by an ordinary descendant recovery is gone.
Crash-window classification uses the last durable intent plus exact retained identities, never the absence of a result alone. A final-rename intent followed by original-staging-absent/exact-final-present is recorded as a recovered `root_renamed` result and forces roll-forward; cleanup is forbidden. The same rule applies to the provenance candidate: after `provenance_rename_intent`, staging-absent/exact-final-present with the retained identity and exact `provenance_preimage_bound` bytes becomes recovered `provenance_published`, while an exact held staging identity is rewritten or renamed only according to its last durable state and that same preimage. Likewise, exact completion or pointer bytes present after their durable intent are revalidated and recorded as recovered results, while absent bytes are published by the finalizer. Both old and new pointer bytes being ambiguous, or any identity/digest disagreement, is terminal. A completed journal is revalidated read-only and never replays mutation.

- Its only production parameters are `-ExpectedSourceCommit <40-lowercase-hex>`, `-ExpectedPrimaryPythonProvenanceReceiptSha256 <64-lowercase-hex>`, and `-ExpectedRecoveryAuthorizationSha256 <64-lowercase-hex>`; fixture roots exist only under `-FixtureOnly` in the trust test and are unreachable in production. A recovery invocation is exactly `& scripts\recover_sgfx_r0_publication.ps1 -ExpectedSourceCommit $expectedSourceCommit -ExpectedPrimaryPythonProvenanceReceiptSha256 $expectedPrimaryPythonProvenanceReceiptSha256 -ExpectedRecoveryAuthorizationSha256 $reviewedRecoveryAuthorizationSha256` from a held independently reviewed script identity, with no environment binding or additional argument. The wrapper invokes Task 1's held isolated launcher with those first two bindings and exact module arguments `sg_preflight.bundle_manifest_cli recover-publication --expected-authorization-sha256 <digest>`; no second recovery codec exists.
- Final artifacts use exact `out/ramses-r0/runs/run-<32hex>`, `out/ramses-r0/c0-runs/c0-<32hex>`, `out/ramses-r0/completions`, `current.json`, and `c0-current.json`. Build only in securely created/pinned `.next` directories, publish every leaf with the shared secure publisher, reparse/hash-reconcile the complete tree, and rename the pinned directory by handle with no replace. Under the evidence-publication lock, publish the exact external completion record with no replace and publish the current pointer last by locked atomic replacement; a pre-completion interruption leaves the prior pointer valid. R0 `current.json` is at most 4 KiB and has exactly `schemaVersion`, `state`, `runId`, `sourceCommit`, `relativeJson`, `relativeMarkdown`, `runManifest`, `completionRecord`, `jsonSha256`, `markdownSha256`, `runManifestSha256`, `completionRecordSha256`, and `completedAtUtc`; C0 adds exactly `relatedR0RunId`. Schema is exactly 1; `state` is exactly `PASS`, `PASS_WITH_OPEN`, or `FAIL`; IDs/commit/time/hashes are strict. For R0, the four relative values are exactly `runs/run-<RunId>/ramses-r0-verification.json`, `runs/run-<RunId>/ramses-r0-verification.md`, `runs/run-<RunId>/run-manifest.json`, and `completions/r0-<RunId>.json`. For C0 they are exactly `c0-runs/c0-<RunId>/control-center-c0-verification.json`, `c0-runs/c0-<RunId>/control-center-c0-verification.md`, `c0-runs/c0-<RunId>/run-manifest.json`, and `completions/c0-<RunId>.json`. A completed terminal FAIL replaces current; an interrupted attempt never does.
- A run manifest is at most 256 KiB and has exactly `schemaVersion`, `runId`, `sourceCommit`, `state`, `startedAtUtc`, `completedAtUtc`, `directories`, `files`, and `totalBytes`; C0 adds exactly `relatedR0RunId`. Manifest schema is exactly 1. Canonical paths are nonempty ASCII, `/`-separated, normalized relative paths with no empty, `.`, `..`, backslash, colon/ADS, absolute, drive-relative, UNC/device, reserved, trailing-dot/space, or Windows-case-fold-colliding component. `directories` is the UTF-8-byte-sorted unique parent closure of listed files, excluding the run root and `run-manifest.json`; it contains no unused directory. `files` is sorted by path bytes and each object is exactly `{path,role,case,lane,size,sha256}`; `size` is a non-Boolean integer in the role cap, `sha256` is lowercase, and `totalBytes` is exactly the checked sum of listed file sizes. The manifest never lists or counts itself, a completion record, or a pointer. The external completion `totalBytes` is exactly manifest `totalBytes + byteSize(run-manifest.json)`.
- A non-FAIL R0 manifest contains exactly these root mappings: `ramses-r0-verification.json` -> `verification_json/none/none`, `ramses-r0-verification.md` -> `verification_markdown/none/none`, `runtime-pair-receipt.json` -> `runtime_pair_receipt/none/receipt`, and `product-manifest-inspection.json` -> `product_manifest_inspection/none/product`. When `realInputMode=absent`, no execution directory exists and the verification state is `PASS_WITH_OPEN` with exact real receipt/product execution OPEN codes. When `realInputMode=complete`, both `real/receipt` and `real/product` exist—never only one—and each maps exact filenames `request.json`, `stdout.log`, `stderr.log`, `ramses-probe-native.json`, `ramses-probe-report.json`, and `ramses-r0-evidence.json` to roles `request`, `stdout_log`, `stderr_log`, `native_report`, `final_report`, and `isolated_evidence` with matching `case=real`/lane; `first-frame.png` -> `first_frame` exists iff secure frame publication succeeded. Thus complete real `directories` is exactly `real`, `real/product`, `real/receipt` in sorted order. A terminal FAIL still requires both root verification artifacts; each successfully reached root proof is optional, and an attempted real lane has request/stdout/stderr followed only by the longest actually completed prefix native-report -> final-report -> isolated-evidence, plus frame only with the native report. No synthetic execution payload, other path, role, case, lane, duplicate, or unlisted leaf is valid.
- A C0 manifest contains exactly `control-center-c0-verification.json` -> `c0_verification_json/none/none` and `control-center-c0-verification.md` -> `c0_verification_markdown/none/none`; `directories` is exactly empty and no R0 proof/execution role is permitted. Every external completion record is at most 4 KiB and has exactly `schemaVersion`, `kind`, `runId`, `relatedR0RunId`, `sourceCommit`, `state`, `relativeManifest`, `manifestSha256`, `completedAtUtc`, and `totalBytes`; schema is exactly 1, `kind` is `r0` or `c0`, `relatedR0RunId` is null only for R0, and `relativeManifest` is exactly `runs/run-<id>/run-manifest.json` or `c0-runs/c0-<id>/run-manifest.json`. Verification model, manifest, completion, and pointer must have byte-identical run ID, related R0 ID where present, source commit, state, and completed-at value; pointer/manifest/completion paths, hashes, and checked totals must reconcile exactly. Completion records are the non-circular durable anchors for every retained historical manifest.
- One per-workspace named interprocess evidence mutex, derived from the held output-root volume/file identity rather than a path, serializes R0/C0 directory/completion/current publication and retention. C0 revalidates and pins its related R0 completion/manifest under that lock immediately before renaming its directory and holds the lock through C0 completion/current publication. Retention, under the same lock, revalidates both current pointers and the complete relation graph before selecting each candidate. It keeps at most eight R0 and eight C0 runs, no noncurrent run older than 14 days, and at most 256 MiB combined; never selects either current run or an R0 referenced by retained C0. For each eligible C0 first and then R0, publish and flush the matching `c0_retention` or `r0_retention` journal and bind both the exact external completion leaf and final run root plus their separate held parents/tombstone names. Pin the complete candidate, completion, current pointers, and relation graph and revalidate them immediately before mutation. Append and flush `completion_tombstone_rename_intent` and then `run_tombstone_rename_intent` while both originals and the complete retained relation graph are still present; neither rename is legal until both intents are durable. Then atomically no-replace rename the exact completion file to its same-parent journal-bound tombstone and flush `completion_tombstone_renamed`, followed by the exact final run root to its separate same-parent journal-bound tombstone and `run_tombstone_renamed`. A crash after either intent or between the two renames blocks every publication/retention operation and may only roll forward through authorized recovery; the candidate is considered fully detached only after both rename results are durable. Delete only the run tombstone's manifest-listed leaves, manifest, empty listed directories, and root, then the completion-file tombstone, using the restartable per-entry/root intent-result protocol. `parent_reconciled` is legal only when both tombstones and both originals are absent, the completion/run parents have their held identities and no unknown sibling, current pointers and retained relation graph remain valid, and every root-delete result is terminal. A crash after either rename or any deletion blocks further pruning until `resume_one_authorized_cleanup` completes that exact journal. Any unknown entry/link/incomplete run/digest/graph/concurrent-state disagreement stops before the first intent. Runtime-pair publication/retention uses a distinct pair mutex and the same `runtime_pair_retention` tombstone journal; when both mutexes are ever needed, acquire pair before evidence and never reverse the order. No recursive or re-resolved-path deletion is permitted.
- The declared Python floor is verified only with an actual trusted protected CPython 3.10 lane at exact held-worktree-relative root `.superpowers/sdd/r0-python-floor-310-v1`. The independently receipted protected CPython 3.13 root is the primary lane; the existing venv is only the source of the fixed-filter 3.13 third-party dependency selection and its redirector never executes. PEP 514 is discovery only and never provenance: no unambiguous candidate records `PYTHON_3_10_INTERPRETER_UNAVAILABLE`; a candidate without the separately reviewer-bound fixed-root receipt/runtime/dependency closure records `PYTHON_3_10_PROVENANCE_REQUIRED`; neither can be PASS or inferred from registry/package metadata.

---

## File and Responsibility Map

### Native probe

- Create sg_preflight/data/ramses_r0/ramses_probe_native_report.schema.json and sg_preflight/data/ramses_r0/ramses_probe_report.schema.json: packaged exact versioned native/final keys in one dedicated auditable schema subtree, nested types, enums, limits, and additional-field rejection; consumed with standard-library/manual validators, not a new production dependency.
- Modify cpp/include/sgfx/cine/ramses_probe.h: typed request, phase, validation, inventory, logic, lifecycle, first-frame, and native-report contracts while preserving ramses_link_probe_message().
- Create cpp/src/ramses_probe_contract.cpp: enum conversion, phase ordering, render-outcome classification, bounded record helpers, and stable native subject identity.
- Create cpp/src/ramses_probe_scene.cpp: metadata-first framework creation, scene load, native validation, bounded object inventory, and one default-state update report per sorted LogicEngine in a retained non-movable session.
- Modify cpp/src/ramses_probe.cpp: small orchestrator that combines scene facts with the existing authored renderer and retains the link probe.
- Create cpp/src/ramses_preview_internal.h and modify cpp/src/ramses_preview.cpp: share a source-private renderer/lifecycle implementation for an already loaded scene, accept an optional exact authored perspective, and fill Task 1's `ProbeLifecycleFacts`/`ProbeFrameFacts` without changing installed `ramses_preview.h`, existing preview request/result/function ABI, defaults, or CLI manifest.
- Create cpp/apps/ramses_probe_cli.cpp plus app-private WIC support: strict option parser, exact perspective JSON reader, inherited-handle native-report serialization, bounded in-memory frame encoding, and safe one-token stderr. R0 never asks the pathname-only Ramses PNG helper to write an artifact.
- Modify cpp/CMakeLists.txt: build and test sgfx_ramses_probe with the already pinned dependencies and place required runtime DLLs beside its build output; do not copy the probe into package-source directories until Task 10 updates that package contract.

### Python trust and evidence boundary

- Create scripts/run_sgfx_isolated_python.ps1, scripts/sgfx_isolated_python_bootstrap.py, scripts/test_sgfx_isolated_python_trust.ps1, and config/sgfx-primary-python-3.13-provenance.json: the only workspace-Python launcher after Task 1, its PowerShell-only trust-boundary tests, and its fixed reviewer-bound primary receipt, with protected runtime/dependency snapshots, isolated/no-site/no-absent-entry startup, a fresh unreachable bytecode-cache prefix, exact source/dependency path injection, ignored import-artifact rejection, and a caller-environment-free tracked module/script dispatch.
- Create sg_preflight/ramses_probe_adapter.py: canonical input resolution, component/handle path checks, hashes, helper/runtime trust, and immutable request facts; it does not own process or policy code.
- Create sg_preflight/ramses_probe_process.py and sg_preflight/ramses_probe_publication.py: parent-owned inherited output handles, Windows Job/process/pipe/Git ownership, QPC deadlines, strict PNG decode, secure generated-leaf publication, evidence/pair mutexes, and identity-based retention primitives.
- Create sg_preflight/ramses_probe_report.py: the only native-fact-to-policy mapper and atomic final `RamsesProbeReport` writer, including deterministic findings.
- Create sg_preflight/ramses_probe_runner.py: protected receipt runner, frozen-product host entry, exact verification model/current inspection, and secure R0/C0 publication orchestration; PowerShell never launches the native helper directly.
- Create sg_preflight/ramses_runtime_lease.py and tests/test_ramses_runtime_lease.py: one dependency-free cross-process lease shared by preview and R0.
- Modify sg_preflight/desktop/preview_coordinator.py and tests/test_qt_quick_preview.py: acquire the shared lease around the existing helper lifetime without changing preview output or fallback semantics.
- Create sg_preflight/ramses_probe_evidence.py: project an accepted final report into an isolated standalone R0 evidence document using the existing checker-evidence vocabulary without changing the shared checker-evidence schema before integration.

### Tests and synthetic fixtures

- Create cpp/tests/ramses_probe_contract_test.cpp: all stable outcome codes, phase ordering, bounds, duplicate/empty-name identity, and deadline state-machine tests.
- Create tests/test_ramses_probe_schema.py: exact native/final schema documents, field sets, enum parity, bounds, and no additional fields.
- Create cpp/tests/ramses_probe_scene_test.cpp: generated metadata/inventory/validation/logic scene facts with no private input.
- Create cpp/tests/ramses_probe_lifecycle_test.cpp: fake-clock state reducer for Available, Ready, Rendered, flush acknowledgement, readback, and every timeout/failure branch.
- Create cpp/tests/ramses_probe_synthetic_test.cpp: generated authored pass, camera-crane interface, one visible frame, lifecycle, inherited-handle report/frame staging, and missing-contract cases.
- Create tests/test_ramses_probe_adapter.py and tests/test_ramses_probe_process.py: path, helper/runtime, hash, schema, inherited-handle publication, timeout/reaper, mutation, generation/run-token identity, WIC PNG validation, and process cleanup.
- Create tests/test_ramses_probe_runner.py and tests/test_ramses_probe_report.py: protected request-only entrypoint, final report ownership, finding identity, strict JSON, and atomic output.
- Create tests/test_ramses_probe_evidence.py: exact lossless safe projection, isolated schema, bounded rows, and no approval inference.
- Modify tests/test_qt_quick_preview.py: prove that the extended native library leaves the finite C0 preview contract and opaque fallback path unchanged.
- Modify tests/test_bundle_manifest.py: optional R0 runtime, digest, inventory, privacy, and exact staged-content cases.
- Create tests/test_ramses_r0_verifier.py: static fail-closed contract for the deterministic verification script.

### Package, verifier, and delivery evidence

- Create scripts/run_sgfx_r0_native_build.ps1, scripts/test_sgfx_r0_native_build_trust.ps1, scripts/recover_sgfx_r0_publication.ps1, and config/sgfx-r0-native-build-provenance.json: independently bound fresh-configure Release build/tool/dependency provenance, offline local-only dependency resolution with `osEgressContainment=not_claimed`, restartable journaled generated-root recovery, and exact protected build receipts; no accepted Release artifact comes from the development configured tree.
- Modify scripts/build_sgfx_exe.py, sg_preflight/bundle_manifest.py, and sg_preflight/exe_entry.py, and create sg_preflight/bundle_manifest_cli.py: introduce an initially inert strict `load_bundle_manifest_v2()` typed trust boundary in Task 6, define the inactive schema-2 producer/pair contract in Task 10, then atomically activate schema 2 across every producer and consumer in Task 11. Expose only sanitized inspection output to PowerShell, copy the optional probe independently of preview within one authenticated runtime pair, include separately named native/final/evidence schema digests, record path-free typed runtime digests/state, and reject incomplete or contradictory runtime sets. Keep a separate explicit schema-1 historical reader; schema dispatch never guesses.
- Task 10 makes `sg_preflight/bundle_manifest.py` final for both inactive and active schema-2 behavior before its bytes enter native-build `toolingFiles`. Task 11 activates that already frozen producer/merger only through builders and consumers and must leave `sg_preflight/bundle_manifest.py` byte-identical to the reviewed Task 10 tooling commit; any required change instead triggers the explicit Task 10 provenance-rebind path before a build.
- Modify NOTICE.md: record Ramses 28.16.0, SDL3, and nlohmann_json runtime roles and observed licenses without claiming distribution approval.
- Create scripts/verify_ramses_r0.ps1 and scripts/run_sgfx_r0_acceptance.ps1: exact-tree native/Python/package/source-integrity verifier with optional explicit real-scene smoke, machine-readable JSON/Markdown output, and the separately reviewer-bound sole Task 13 launcher.
- Create docs/tickets/sgfx-ramses-r0-standalone.md: verified local acceptance, risks, and copy-ready wording; no external posting.
- Create docs/worklogs/sgfx-ramses-r0-standalone.md: exact observed intervals only; no Clockodo write.
- Modify CHANGELOG.md only after the corresponding standalone behavior is green.

---

## Planning Approval Checkpoint (must complete before Task 1)

After independent native/spec, Python/security/package, and scope/release reviews report no open Critical or Important findings, stage this plan alone, run `git diff --cached --check`, inspect the entire staged diff, and commit it with `git commit -m "docs: approve standalone Ramses R0 plan"`. Record the plan SHA-256, approval commit, original baseline `bd4867a809a6c02612f8449562940e29215ecfa6`, and post-wording implementation base in ignored `.superpowers/sdd/r0-progress.md`. Tasks 1-12 do not edit this tracked plan; their RED/GREEN commands, observed results, review findings, and task commits go only into that ignored progress ledger. Task 13 reconciles the checked boxes once exact evidence exists. This checkpoint makes every later clean-tree and commit-bound claim executable.

Before Task 1, execute one separate tracked documentation checkpoint:

1. In `docs/tickets/sgfx-control-center-c0.md`, replace “accepted executable” with “staged candidate executable” and replace “The release keeps” with “The staged C0 candidate preserves”; add one sentence that the candidate has not replaced the installed/default bundle.
2. In `CHANGELOG.md` under `[Unreleased - local alpha]`, add one bounded note that the C0 Control Center entries describe a staged local candidate and the installed/default double-click path remains Clean pending a separately approved default-cutover plan.
3. In `plans/2026-07-12-sgfx-qa-control-center-design.md`, replace the stale “separate Task 14” wording with “separately approved default-cutover plan”; change no behavior claim.
4. Run `rg -n "accepted executable|The release keeps|separate Task 14"` over those three files, `git diff --check`, and the exact no-argument default test. Stage only those three files, inspect the entire staged diff, and commit `docs: clarify C0 candidate status`.
5. Append a physically-last Europe/Berlin-stamped block to both canonical coordination files correcting the prior broad statement: R0 is noninteractive; C0 remains the finite turntable; evidence-linked guided 3D review belongs to R4; a continuously embedded interactive viewport is not an accepted goal. Name the plan-approval commit, wording commit, active worktree, and Task 1 as NEXT. End both blocks with the exact Lexus sign-off.

No push occurs. Before Task 1, securely establish and record the empty external publication-recovery-authority root from Global Constraints; this is canonical orchestration state, not an implementation artifact, and it remains empty unless an actual interrupted publication later requires one director-approved digest-addressed record. Create `.superpowers/sdd/r0-progress.md` with plan hash/commits and a task table containing task number, start/end Europe/Berlin timestamps, RED command/result, GREEN command/result, review disposition, task commit, and next task. Task 1 additionally records its distinct no-Python bootstrap commit and primary-snapshot binding commit before its accepted feature commit; neither prerequisite commit is mislabeled as Task 1 acceptance. After every accepted Task 1-12 feature commit, root Lexus appends a signed physically-last canonical beat to both coordination files naming the task commit, fresh gates, review state, ledger path, and exact next task. The ignored ledger supplements but never replaces canonical crash recovery.

---

### Task 1: Define the pure native R0 contract and classifier

**Files:**
- Create: .gitattributes
- Create: scripts/run_sgfx_isolated_python.ps1
- Create: scripts/sgfx_isolated_python_bootstrap.py
- Create: scripts/test_sgfx_isolated_python_trust.ps1
- Create: config/sgfx-primary-python-3.13-provenance.json
- Create: sg_preflight/data/ramses_r0/ramses_probe_native_report.schema.json
- Create: sg_preflight/data/ramses_r0/ramses_probe_report.schema.json
- Modify: cpp/include/sgfx/cine/ramses_probe.h
- Create: cpp/src/ramses_probe_contract.cpp
- Create: cpp/tests/ramses_probe_contract_test.cpp
- Create: tests/test_ramses_probe_schema.py
- Modify: cpp/CMakeLists.txt

**Interfaces:**
- Consumes: the independently reviewed protected CPython 3.13 runtime/dependency snapshots and primary receipt, std::filesystem::path, std::chrono::milliseconds, and existing sgfx::cine library linkage.
- Produces: the sole isolated workspace-Python launcher; ProbePhaseId, ProbePhaseState, RamsesRenderOutcome, AuthoredPerspectiveState, RamsesAuthoredPerspective, RamsesProbeRequest, ProbeSubject, ProbeValidationAggregate, ProbeInventoryBucket, ProbeTransformFacts, ProbeCameraFacts, ProbeViewportFacts, ProbeAuthoredContractFacts, ProbeLogicFacts, ProbeLifecycleFacts, ProbeFrameFacts, ProbeLoadedModuleIdentity, RamsesProbeNativeReport, classify_render_outcome(), render_outcome_code(), and validate_probe_request().

- [ ] **Step 1: Establish the reviewed tracked launcher bootstrap without executing it**

First author `scripts/test_sgfx_isolated_python_trust.ps1` alongside the launcher and bootstrap as a PowerShell-only trust-boundary test. The PowerShell test requires both tracked launcher files and the fixed primary receipt, direct snapshot-process argv transport, the exact five-handle NUL/capability/audit/stdout/stderr allowlist with only three standard handles, `-I -B -S`, a fresh nonexistent `pycache_prefix`, exact `_pth`/startup-path/flags/handshake/module-identity assertions before workspace import, cleared Python/caller environment, an empty protected synthetic home/config tree with exact `Path.home()` identity, no `.pth` processing, the target-specific argument grammars in Global Constraints, exact HEAD/import inventory independent of ignore/index state, and exact exit/stdout/stderr propagation. Test the same `CreateProcessW` CRT quoting implementation under absolute held Windows PowerShell 5.1 and PowerShell 7 when present, including empty tokens, whitespace, non-ASCII, quotes, every run of backslashes before quotes/end, exact `sys.argv`, environment, and exit transport. Test suspended assignment to a no-breakaway Job, nonce ready/capability ordering, recursive snapshot-Python `-m`/`-c` transport, held bare-Git resolution, dependency-tool/test-helper/staged-executable registration, target-specific child environments, the exact future `build-native-release`/`build-and-stage-runtime-pair` grammars and deadlines, provenance-tool image registration plus stage-phase zero-descendant enforcement, subprocess-policy bypass and Job/audit-count mismatch rejection, unexpected child/module rejection, timeout termination, and the exact direct signal -> exit capture -> drain join -> thread/process/pipe close -> active-zero -> Job-close sequence. Test every legal command form and reject absolute/path-like/dot-dot unittest selectors, malformed dotted selectors, arbitrary discovery roots/patterns/top-levels, unknown or reordered switches, extra builder/benchmark/inspector arguments, and request paths outside the held attempt root before Python starts. Forge the primary receipt and separately supplied expected receipt hash; origin/snapshot signer/thumbprint, architecture/version, all five signed-runtime identities, and `pyvenv.cfg`; missing or extra `_pth`/zip/path entries; full snapshot/dependency tree count/bytes/digests; ready/live/after loaded modules; and before/after namespace state. Prove neither the venv redirector nor user-local base image is launched in place and missing/partial primary provenance is terminal before Python. Build an arbitrary ignored `tests/test_evil.py`, a valid timestamp/size-matched malicious `__pycache__` entry, plus poisoned `PYTHONPATH`, `PYTHONHOME`, real-home/profile variables, user-site, cwd `sitecustomize.py`, `.pyd`, and PyInstaller hook fixtures; a sentinel must never execute and `Path.home()` must stay synthetic. Task 1 cleanup fixtures supply only held HEAD-untracked cache leaves inside exact `__pycache__` parents and prove the cap, sorted receipt, before/after digests, terminal journal digest, zero Python starts, one-shot bootstrap binding, and snapshot-initializer dependency on the accepted empty-after receipt. They interrupt before and after every leaf/directory intent/result, terminal journal flush, receipt creation/rename/reopen, and final proof; require the fixed director-approved `resume_cache_cleanup` authorization and no-path recovery mode; and prove present-after-intent, deletion-before-result, wrong-prefix, replacement, extra sibling, parent drift, stale authorization, and second-normal-attempt behavior. A HEAD entry, hardlink, reparse point, non-cache file, forged/partial journal/authorization/receipt, wrong bootstrap HEAD, post-initialization call, post-manifest call, or second call is untouched and causes failure. Test a development TDD manifest containing only current-task plan-declared paths and exact identities, and prove it cannot authorize packaging/evidence, another path, or a postcommit exact-source run. Poison `PATH` plus every caller `GIT_*` redirect and config seam; forge the `cmd\git.exe` shim, effective Git image/DLL set, Program-Files placement/ACL, Authenticode state, signer/thumbprint, held identities, hardlink alias count/location, loaded module, child count, repo/gitdir/common-dir identity, replacement refs, grafts, alternates, index flags/content, HEAD full-record output, worktree blob bytes, filesystem extras, and cap boundaries. Prove `ActiveProcesses` remains nonzero while a Git process handle is open and zero is accepted only after direct handles close. No failed Git/source/runtime proof may clean a byte or launch Python.

The same PowerShell suite builds maximum/boundary fixture roots and proves the exact receipt key sets, canonical bytes including terminal LF, tree preimages, stored-ZIP entry order/timestamps/UTF-8 names, byte-exact `python313._pth`, fixed-root/DACL/no-replace/recovery rules, source-versus-snapshot identity separation, dependency exclusion policy, worktree-versus-receipt inventory authorities, namespace-shadow rejection, and lane-specific early module sets. It includes the live no-source-ZIP/no-source-`_pth` shape plus editable SGFX `.pth`, executable `distutils-precedence.pth`, `direct_url.json`, and forbidden dependency top-level fixtures. Git fixtures prove expected-commit attribute sourcing, LF/CRLF/mixed built-in clean equivalence, raw equality for each `-text` security input, and rejection of dirty worktree attributes, global attributes, `filter`, `ident`, `working-tree-encoding`, or caller EOL/config substitution. Snapshot initialization, floor initialization, cache cleanup, cache-cleanup recovery, bound-primary recovery, bound-primary recovery finalization, refresh-manifest, and Python execution parameter sets are mutually exclusive and every nonexecution mode proves zero child-Python starts.

Add an outer held-caller fixture for both destructive pre-binding modes. It supplies the independently reviewed bootstrap commit and all four exact hashes, holds the launcher without write/delete sharing, and proves the launcher records those values rather than self-selecting HEAD. A different clean commit with the same file shape, a clean cherry-pick with changed commit identity, an edited self-consistent launcher, a swapped same-path leaf, or any missing/disagreeing hash must fail before either cleanup journal, snapshot `.next`, or child process exists. Add explicit common-dir and per-worktree `info/attributes` fixtures that override `text`/`eol`; both must fail while absent protected `info` directories and an exact expected-commit attribute map pass. Add cache-recovery fixtures for the fixed authorization schema/digest, both journal prefix length/digests, chained suffix replay, repeated recovery crashes, identity-only continuation, immutable terminal cleanup journal, receipt-journal header bootstrap, and every receipt create/partial-write/flush/reopen/rename/completion crash window. Add post-binding recovery fixtures for the fixed authorization schema, held authorization digest, exact identity-only cleanup, reconstruction states, recovery receipt, disabled execution interval, fresh review, new binding commit, history archival, and terminal journal move.

Author only `.gitattributes`, `scripts/run_sgfx_isolated_python.ps1`, `scripts/sgfx_isolated_python_bootstrap.py`, and `scripts/test_sgfx_isolated_python_trust.ps1`. The attributes file marks itself, both provenance receipts, all three bootstrap files, Task 10's native-build/staging/recovery scripts, both Task 12 verifier/acceptance scripts, and `sg_preflight/data/ramses_r0/*.json` as `-text`, so Git never rewrites bytes whose canonical hashes are security inputs. Its bytes are exactly:

~~~text
.gitattributes -text
config/sgfx-primary-python-3.13-provenance.json -text
config/sgfx-r0-native-build-provenance.json -text
scripts/run_sgfx_isolated_python.ps1 -text
scripts/sgfx_isolated_python_bootstrap.py -text
scripts/test_sgfx_isolated_python_trust.ps1 -text
scripts/run_sgfx_r0_native_build.ps1 -text
scripts/test_sgfx_r0_native_build_trust.ps1 -text
scripts/recover_sgfx_r0_publication.ps1 -text
scripts/stage_ramses_runtime_pair.ps1 -text
scripts/verify_ramses_r0.ps1 -text
scripts/run_sgfx_r0_acceptance.ps1 -text
sg_preflight/data/ramses_r0/*.json -text
~~~

Do not create a receipt/snapshot, execute either script, or start Python. Parse both PowerShell files as data with `System.Management.Automation.Language.Parser`, inspect the Python bootstrap text statically, and obtain independent Python/security plus scope/release reviews of the exact four-file staged diff. With zero open Critical/Important findings, commit this no-Python trust anchor:

~~~powershell
$bootstrapFiles = @(
    'scripts\run_sgfx_isolated_python.ps1',
    'scripts\test_sgfx_isolated_python_trust.ps1'
)
foreach ($path in $bootstrapFiles) {
    $tokens = $null
    $errors = $null
    [Management.Automation.Language.Parser]::ParseFile((Resolve-Path -LiteralPath $path).Path, [ref]$tokens, [ref]$errors) | Out-Null
    if ($errors.Count -ne 0) { throw "Bootstrap PowerShell parse failed" }
}
git add .gitattributes scripts/run_sgfx_isolated_python.ps1 scripts/sgfx_isolated_python_bootstrap.py scripts/test_sgfx_isolated_python_trust.ps1
git diff --cached --check
git commit -m "build: establish isolated Python bootstrap"
~~~

Require the commit to contain exactly those four paths, `git show --check` to pass, and status to be clean. Independent Python/security and scope/release reviewers retain its exact commit plus four file sizes/digests in a protected review record outside this implementation worktree and confirm that binding to root Lexus. The ignored progress ledger may mirror but never originate or replace it. This intentionally creates a tracked, reviewed trust anchor before its first execution; no untracked or merely self-authenticated launcher is ever run.

- [ ] **Step 2: Clean ignored caches, initialize, independently review, and commit-bind the primary snapshot**

From the clean bootstrap commit, root Lexus copies the independently retained values into `$reviewedBootstrapCommit`, `$reviewedAttributesSha256`, `$reviewedLauncherSha256`, `$reviewedPythonBootstrapSha256`, and `$reviewedTrustTestSha256`; each is validated for exact lowercase length. Use one held-launcher bootstrap for both modes:

~~~powershell
$heldBootstrapLauncherPath = [IO.Path]::GetFullPath('scripts\run_sgfx_isolated_python.ps1')
$bootstrapArgs = @(
    '-DevelopmentTask', '1',
    '-ExpectedBootstrapCommit', $reviewedBootstrapCommit,
    '-ExpectedBootstrapAttributesSha256', $reviewedAttributesSha256,
    '-ExpectedBootstrapLauncherSha256', $reviewedLauncherSha256,
    '-ExpectedBootstrapPythonSha256', $reviewedPythonBootstrapSha256,
    '-ExpectedBootstrapTrustTestSha256', $reviewedTrustTestSha256
)
$launcherStream = [IO.File]::Open($heldBootstrapLauncherPath, [IO.FileMode]::Open, [IO.FileAccess]::Read, [IO.FileShare]::Read)
try {
    $sha = [Security.Cryptography.SHA256]::Create()
    try { $actual = ([BitConverter]::ToString($sha.ComputeHash($launcherStream))).Replace('-', '').ToLowerInvariant() } finally { $sha.Dispose() }
    if ($actual -cne $reviewedLauncherSha256) { throw "Bootstrap launcher is not the reviewed byte stream" }
    & $heldBootstrapLauncherPath -InitializeIgnoredCacheCleanup @bootstrapArgs
    if ($LASTEXITCODE -ne 0) { throw "Reviewed ignored-cache cleanup failed" }
    & $heldBootstrapLauncherPath -InitializePrimarySnapshot @bootstrapArgs
    if ($LASTEXITCODE -ne 0) { throw "Reviewed primary-snapshot initialization failed" }
} finally {
    $launcherStream.Dispose()
}
~~~

Require the canonical cleanup-receipt digest, terminal cleanup-journal digest, completed receipt-journal digest, complete after-source proof, and zero-child-Python result. Independently inspect `.superpowers/sdd/r0-task1-cache-cleanup-journal.jsonl`, `.superpowers/sdd/r0-task1-cache-cleanup-receipt-journal.jsonl`, and `.superpowers/sdd/r0-task1-cache-cleanup.json` against the held pre-cleanup inventory, every mutation/publication result, empty after inventory, and external four-file binding. The second invocation must authenticate that completed two-journal/final-receipt set and launch no Python. It creates the fixed ignored snapshot and the untracked canonical `config/sgfx-primary-python-3.13-provenance.json`, whose `bootstrapCommit`/`bootstrapFiles` exactly repeat the caller binding, and emits only the lowercase receipt SHA-256. No other worktree path may change.

Independent Python/security and scope/release reviewers verify the terminal cleanup journal, completed receipt journal, final receipt, empty after inventory, all five source/snapshot signed-runtime roles and signer thumbprints, exact primary receipt schema/bytes/hash, deterministic ZIP/`_pth` bytes, dependency exclusions, DACL/identities, `pyvenv.cfg`, tree preimages, lane module set, and exact allowed write set. Root Lexus then modifies only the tracked launcher to freeze that independently confirmed primary receipt digest. Statically reparse the launcher without executing it, require zero open Critical/Important findings on the exact launcher-plus-primary-receipt diff, and run:

~~~powershell
git add scripts/run_sgfx_isolated_python.ps1 config/sgfx-primary-python-3.13-provenance.json
git diff --cached --check
git commit -m "build: bind protected Python snapshot"
git show --check --stat --oneline HEAD
~~~

Require an exact two-path commit and clean status. Both reviewers reopen the exact binding commit, retained launcher and tracked receipt, protected snapshot/runtime/dependency leaves, canonical receipt bytes, and frozen launcher digest; they confirm that the commit changed only those two paths and that their candidate review still applies byte-for-byte. Root Lexus then no-replace publishes fixed external `python313-<primary-receipt-sha256>.json` with `kind=python313`, `reviewedCommit=<binding-commit>`, `reviewedDigest=<primary-receipt-sha256>`, exact sorted launcher/receipt file records, both reviewer roles, and canonical UTC. Independently revalidate that record's protected identity/DACL/bytes and exact target files. Record the bootstrap commit, cleanup-journal/receipt-journal/final-receipt digests, binding commit, primary receipt digest, external review-record digest, and snapshot tree digests in the ignored ledger.

Only from that clean binding commit, after the fixed external `python313-<receipt-sha256>.json` record passes, may the exact receipt digest be supplied as `SGFX_R0_PRIMARY_PYTHON_PROVENANCE_RECEIPT_SHA256`. The launcher derives that fixed record name from the expected digest, validates its binding commit/digest/two files/reviewers, and accepts no record path. Run the tracked `scripts/test_sgfx_isolated_python_trust.ps1 -DevelopmentTask 1` and require its complete PASS footer before authoring or running any R0 Python test. Any bootstrap/binding review finding is fixed and recommitted without Python; any snapshot/receipt mismatch follows the explicit bound-primary recovery/finalization contract and repeats review, binding, external-record publication, and history archival under new unique identities. An interrupted cache cleanup follows only `-RecoverIgnoredCacheCleanup` with its fixed reviewed authorization and journal; it never silently starts a new normal attempt.

- [ ] **Step 3: Add failing native and Python contract tests for every stable outcome, exact phase order, and both report schemas**

Now use the exact public contract:

~~~cpp
constexpr std::array<sgfx::cine::ProbePhaseId, 13> ExpectedPhaseOrder{
    sgfx::cine::ProbePhaseId::Input,
    sgfx::cine::ProbePhaseId::Metadata,
    sgfx::cine::ProbePhaseId::SceneLoad,
    sgfx::cine::ProbePhaseId::Validation,
    sgfx::cine::ProbePhaseId::Inventory,
    sgfx::cine::ProbePhaseId::Logic,
    sgfx::cine::ProbePhaseId::AuthoredContract,
    sgfx::cine::ProbePhaseId::DisplaySetup,
    sgfx::cine::ProbePhaseId::Available,
    sgfx::cine::ProbePhaseId::Ready,
    sgfx::cine::ProbePhaseId::Rendered,
    sgfx::cine::ProbePhaseId::Readback,
    sgfx::cine::ProbePhaseId::Finalize,
};

static_assert(sgfx::cine::probe_phase_count() == ExpectedPhaseOrder.size());
static_assert(sgfx::cine::probe_phase_at(0) == sgfx::cine::ProbePhaseId::Input);
static_assert(sgfx::cine::probe_phase_at(12) == sgfx::cine::ProbePhaseId::Finalize);

const std::array<std::string_view, 14> expectedCodes{
    "scene_unresolved",
    "metadata_unreadable",
    "toolchain_incompatible",
    "scene_load_failed",
    "logic_update_failed",
    "display_or_offscreen_setup_failed",
    "scene_available_timeout",
    "scene_ready_timeout",
    "scene_rendered_timeout",
    "authored_pass_or_camera_contract_missing",
    "readback_request_failed",
    "readback_timeout",
    "clear_only_frame",
    "visible_frame_observed",
};
for (const auto code : expectedCodes)
    require(sgfx::cine::is_stable_render_outcome_code(code), code);
~~~

Add separate facts for each stable code plus undetermined_after_<last-proven-phase>. Add two issues with the same empty name but distinct object IDs and assert that their native subject keys differ. The native report has exactly five sorted `loadedModules` entries: roles `process_image`, `sdl3`, `ramses_shared`, `ramses_headless`, and `ramses_renderer`, exact fixed filenames, and objects `{role,fileName,volumeSerialNumber,fileId}` where the volume is 16 lowercase hex and the 128-bit file ID is 32 lowercase hex. Paths, digests, optional/missing entries, and duplicate roles/names are forbidden; Python owns expected-digest comparison.

Use a C++17-compatible constexpr element-by-element loop plus the individual enum-count/order assertions; do not depend on C++20 `std::array::operator==`. In `tests/test_ramses_probe_schema.py`, load both packaged schema resources, require exact native and final top-level field sets, the same phase/outcome enums as C++, all documented size/list/string limits, and `additionalProperties: false` at every object boundary. Parse with the standard library only; do not add `jsonschema`.

- [ ] **Step 4: Verify the configured tree and run both new contract tests RED**

Author `cpp/tests/ramses_probe_contract_test.cpp`, `tests/test_ramses_probe_schema.py`, and the minimum CMake registration without implementing the types or schemas. The one-time ignored-cache cleanup already completed before the trust suite. Refresh the fixed Task 1 development manifest through the now-tracked receipt-bound launcher, then require both native build/link RED and assertion-level Python RED for missing R0 types/schemas:

~~~powershell
rg -n "CMAKE_HOME_DIRECTORY:INTERNAL=.*/sg-preflight-v02-qt-integration/cpp" build\cine-c0\CMakeCache.txt
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 1
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_contract_test
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 1 -Module unittest -ModuleArguments @("tests.test_ramses_probe_schema", "-v")
~~~

Expected: build/link failure because the R0 types and classifier do not exist, plus assertion-level Python failure because both report schemas/types are absent. Record both RED failures. Every source/runtime difference, including any cache created after the one-time cleanup, fails before Python.

- [ ] **Step 5: Add the minimal typed contract and schemas**

Implement the public types, deterministic pure classifier, two exact schemas, C++ contract test support, and standard-library Python schema assertions. Do not modify the now-committed attributes file, launcher, bootstrap, trust test, or primary receipt. Refresh the Task 1 development manifest after every source change before a Python test.

`run_sgfx_isolated_python.ps1` has eight mutually exclusive operation families: one `-Module`/`-ModuleArguments` or `-Script`/`-ScriptArguments` execution; PowerShell-only `-RefreshDevelopmentManifest -DevelopmentTask <1..12>`; Task-1-only `-InitializePrimarySnapshot -DevelopmentTask 1`; Task-1-only `-InitializeIgnoredCacheCleanup -DevelopmentTask 1`; Task-1-only `-RecoverIgnoredCacheCleanup -DevelopmentTask 1 -ExpectedCacheCleanupRecoveryAuthorizationSha256 <64hex>`; Task-1-only `-RecoverBoundPrimarySnapshot -DevelopmentTask 1 -ExpectedRecoveryAuthorizationSha256 <64hex>`; Task-1-only `-FinalizeBoundPrimaryRecovery -DevelopmentTask 1 -ExpectedRecoveryAuthorizationSha256 <64hex> -ExpectedNewBindingCommit <40hex> -ExpectedExternalPrimaryReviewRecordSha256 <64hex>`; or floor-preparation-only `-InitializePython310Snapshot -DevelopmentTask 12`. Every nonexecution mode launches no Python and enforces its exact fixed roots/sources/write sets. The first tracked bootstrap commit contains one exact internal `UNBOUND_PRIMARY_RECEIPT` sentinel. While it remains, normal cache cleanup is the only legal first mode when the mutation journal, receipt journal, their bootstrap leaves, recovery authorization, `.next`, and final receipt are all absent; an interrupted bootstrap or journal disables every family except the fixed-authorized cache-recovery mode; and primary initialization becomes legal only after the fixed terminal cleanup journal plus completed receipt journal and final receipt authenticate the externally supplied reviewed bootstrap commit/four-file binding and empty after inventory. Both initial modes require the held caller's explicit `-ExpectedBootstrapCommit` plus all four `-ExpectedBootstrap*Sha256` values; the launcher may not derive them from its own HEAD. The clean binding commit replaces the sentinel with the independently reviewed 64-lowercase-hex primary receipt digest; afterward initial primary initialization, normal cache cleanup, and cache recovery are permanently forbidden. Bound-primary recovery alone is legal under Global Constraints' authorization while the bound snapshot is missing/mismatched, and it disables execution/refresh/floor work through `awaiting_new_binding`; after the exact new binding commit/external record exist, only the bound-primary finalizer may archive the fixed recovery artifacts and re-enable ordinary bound modes. Cache cleanup authenticates the complete source except for the narrowly eligible pinned cache inventory, performs only the journaled identity-bound deletion and separately journaled terminal receipt publication from Global Constraints, then requires the ordinary complete source proof; every later cleanup call or cache contamination fails closed. Execution permits only the five targets and exact grammars in Global Constraints and requires the expected primary receipt SHA from exactly one of explicit `-ExpectedPrimaryPythonProvenanceReceiptSha256` or the exact development environment key. It derives the fixed receipt/runtime/dependency roots, trusted Windows/System32, and unique nonexistent cache prefix; performs the applicable authenticated HEAD plus development-manifest or exact-source proof; and executes the frozen `CreateProcessW`/Job/handle-list/handshake/quoting/cleanup sequence. A script target must be the exact HEAD regular file at its frozen workspace-relative path; only `unittest` may use `-DevelopmentTask` for Python execution. Exact-source mode requires `-ExpectedSourceCommit <40hex>`, forbids a development manifest, and performs the trusted full-record/blob/index/filesystem proof before and after the child. The default executable is the receipt-derived held snapshot `python.exe`, never the venv redirector or base installation in place; the only default dependency root is the fixed receipt-owned snapshot. An internal verifier-only interpreter selection may choose only the fixed CPython 3.10 snapshot whose complete lane-specific receipt passed Task 12 review; it must report exact CPython 3.10 plus all five signed-runtime identities and its receipt-owned dependency root. No arbitrary interpreter, receipt, snapshot, or dependency root is accepted. `sgfx_isolated_python_bootstrap.py` is standard-library-only, verifies all flags/cache/four-entry-startup-path/environment/loaded-module/receipt/handshake facts before importing any worktree/dependency module, installs the fixed recursive subprocess/audit policy, rejects any target/argument outside the fixed grammar, sets exact `sys.argv`, and calls only `runpy.run_module()` or `runpy.run_path()`. Neither launcher imports `sg_preflight`, tests, PyInstaller, or a dependency before those checks.

Every bound execution, refresh, and floor-preparation invocation also derives fixed external `python313-<expected-primary-receipt-sha256>.json`, retains it with the tracked launcher/receipt, requires `reviewedCommit` to equal the snapshot-binding commit and `reviewedDigest` to equal the expected receipt digest, and verifies its exact two file records plus required reviewer roles before any Python or generated-root mutation. Missing, forged, stale, mismatched, replaced, or caller-redirected primary review state fails before execution. Initial cache/snapshot construction remains governed by the earlier bootstrap record because the `python313` record cannot exist until after the binding commit. Task 1 trust fixtures cover every missing/forged/wrong-commit/wrong-digest/wrong-file/wrong-reviewer/fixed-name/caller-path case before the first child instruction.

Define the request and outcome facts without JSON or UI dependencies:

~~~cpp
enum class ProbeBackend { OpenGL };
enum class ProbePhaseState { NotRun, Passed, Finding, Unavailable, Failed, TimedOut };
enum class LocalTranslationApplication { NotApplied };
enum class RamsesRenderOutcome {
    SceneUnresolved,
    MetadataUnreadable,
    ToolchainIncompatible,
    SceneLoadFailed,
    LogicUpdateFailed,
    DisplayOrOffscreenSetupFailed,
    SceneAvailableTimeout,
    SceneReadyTimeout,
    SceneRenderedTimeout,
    AuthoredPassOrCameraContractMissing,
    ReadbackRequestFailed,
    ReadbackTimeout,
    ClearOnlyFrame,
    VisibleFrameObserved,
    Undetermined,
};

struct RamsesProbeRequest {
    std::filesystem::path scene_path;
    std::optional<std::filesystem::path> perspective_path;
    std::optional<std::string> perspective_id;
    AuthoredPerspectiveState authored_perspective_state{AuthoredPerspectiveState::NotSupplied};
    std::optional<RamsesAuthoredPerspective> authored_perspective;
    std::string profile_id;
    std::string scenario_id;
    std::string source_fingerprint;
    std::string run_token;
    std::uint64_t generation{};
    ProbeBackend backend{ProbeBackend::OpenGL};
    std::uint32_t width{480};
    std::uint32_t height{270};
    std::chrono::milliseconds timeout{60000};
    std::chrono::milliseconds render_timeout{30000};
    std::uint64_t parent_start_qpc{};
    std::uint64_t qpc_frequency{};
    std::uint64_t native_work_deadline_qpc{};
    std::uint64_t parent_deadline_qpc{};
};
~~~

Define `RamsesAuthoredPerspective` in this task, before any parser exists:

~~~cpp
struct RamsesAuthoredPerspective {
    std::string id;
    bool aspect_from_resolution{};
    float distance{};
    float yaw{};
    float pitch{};
    float roll{};
    float horizontal_fov{};
    float aspect_ratio{};
    float near_plane{};
    float far_plane{};
    std::array<float, 3> local_translation{};
    std::array<float, 3> origin{};
    float scale{};
    std::array<std::int32_t, 2> shift{};
    std::int32_t viewport_offset_x{};
    std::int32_t viewport_offset_y{};
    std::uint32_t viewport_width{};
    std::uint32_t viewport_height{};
    LocalTranslationApplication local_translation_application{
        LocalTranslationApplication::NotApplied};
};
~~~

`LocalTranslation` is an allowlisted, required, finite, source-provenance fact in the representative G45 `ExportedCameraCraneV1` shape; R0 never maps it to a runtime property or direct camera mutation. `LocalTranslationApplication` has the single allowed value `NotApplied`, serialized exactly as `not_applied`. `AuthoredPerspectiveState` is exactly `NotSupplied`, `Selected`, or `MissingContract`.

`validate_probe_request()` accepts profile IDs matching `[A-Z0-9][A-Z0-9_]{0,63}`, scenario IDs matching `[a-z0-9][a-z0-9-]{0,63}`, perspective IDs matching `[A-Za-z0-9][A-Za-z0-9_.-]{0,127}` when supplied, a 64-lowercase-hex adapter-supplied source fingerprint, a 32-lowercase-hex run token, an unsigned 64-bit generation, `opengl` mapped to `EDeviceType::GLES_3_0`, exact 480 x 270, and exact 60000/30000 millisecond budgets. The QPC frequency must equal live `QueryPerformanceFrequency`; start/work/parent ticks must be nonzero, overflow-safe, and exactly 58/60 seconds apart. Perspective path and ID must be supplied together or both absent. `NotSupplied` requires neither path/id nor parsed values; `Selected` requires all three; `MissingContract` requires path/id but no values. Python, not native code, proves all-root disjointness, held input identity, output-handle ancestry, and digests because native code does not know the semantic source roots.

Feature-level compatibility accepts only the concrete Ramses 28.16 public values `EFeatureLevel_01`, `EFeatureLevel_02`, `EFeatureLevel_03`, and `EFeatureLevel_04`. Record the exact enumerator and raw value. Reject an unknown raw value as `toolchain_incompatible`; do not treat `Latest` as an independent fifth level or serialize an implementation-defined enum spelling.

The canonical path-free fingerprint is SHA-256 over UTF-8 canonical JSON (`sort_keys=True`, separators `(',', ':')`, `ensure_ascii=True`) of exactly:

~~~json
{"backend":"opengl","perspective":{"id":"<id>","sha256":"<digest>","state":"included"},"probeVersion":"0.1.0","profileId":"<profile>","scenarioId":"<scenario>","sceneSha256":"<digest>","schemaVersion":1}
~~~

When no perspective is passed, `perspective` is exactly `{"state":"unavailable"}`. Paths, timestamps, generation, run token, helper digest, and worktree-proof state are deliberately excluded. Native echoes `sourceFingerprint`, `runToken`, and `generation`; it does not claim to have verified them.

Bound construction is explicit. Native serialized section budgets after JSON escaping are: metadata 32 KiB; validation 224 KiB; inventory 448 KiB; transform/camera/viewport/authored-contract facts 224 KiB; logic 224 KiB; and identity/phases/lifecycle/frame/truncation bookkeeping 256 KiB. Their 1,408 KiB sum leaves 640 KiB for document syntax and other fixed scalars under the 2 MiB cap. UTF-8 metadata text is at most 4,096 encoded bytes; validation retains at most 256 aggregates, four unique messages per aggregate, 128 escaped JSON bytes per message, and 96 encoded bytes per name; inventory has at most 32 buckets, 256 records per bucket, and 2,048 globally with 48-byte types and 96-byte names; transform/camera/viewport observations retain at most 2,048 globally; logic retains exact total/success/failure/incomplete engine counts, at most 64 sorted per-engine records, and 512 non-policy node-timing records globally with the same name/type caps; phases are exactly 13. Complete cycle/runtime classification and failed-engine counts are computed before detail caps. Each serialized section budget is an additional tighter cap: after sorting the complete typed collection, retain whole canonical records only while the next record fits, and record exact total/reported/truncated counts. Final serialized section budgets are: normalized facts/observations 1 MiB; findings 1 MiB; environment/artifacts 256 KiB; source-mutation/evidence 512 KiB, leaving 1.25 MiB for fixed scalars and syntax under 4 MiB. Final findings retain at most 100 and top subjects five.

Native code computes no cryptographic message/metadata digest; Python owns all SHA-256 claims. Native serialization re-reads its inherited report handle and compares bytes exactly with the in-memory canonical JSON rather than inventing a native hash. `BoundedText` replaces invalid UTF-8 with U+FFFD, replaces C0 controls except tab/newline/carriage-return with U+FFFD, truncates only at a Unicode scalar boundary to both its encoded-byte and escaped-JSON-byte caps, and records original byte count plus truncated. Every capped collection carries total/reported/truncated counts plus uncapped anomaly totals by exact category. Sort complete validation aggregates by `(severityRank, objectType, nullableObjectId)` before retaining 256, with `error=0` and `warning=1`; sort unique safe messages by encoded bytes before retaining four. Sort numeric observations by `(stateRank, typedIdentity)`, with `nonfinite=0`, `invalid=1`, `finite=2`, and `unsupported=3`, before per-type/global caps. Sort LogicEngines by scene object ID and retain failed/incomplete engines before successful detail; node timing detail never determines policy. If any validation issue, numeric anomaly, or failed/incomplete engine detail is omitted, emit its independent truncation fact. Final findings have `policyPriority=0` for the fixed validation/numeric/logic truncation rules and `1` for detail; retain every priority-0 finding first, then fill the remaining slots by `(severityRank, ruleId, subjectIdentity, findingId)` with `error=0`, `warning=1`, and `info=2`. Maximum-shaped and shuffled tests place the only anomaly/failure last in 257 validation aggregates, 2,049 numeric observations, 65 engines, 513 node records, and 101 findings, then crowd each same-severity truncation rule behind more than 100 lexically earlier detail findings and require all policy-meta findings, exact uncapped counts, deterministic retained bytes, and both report caps under worst-case six-byte JSON escaping.

The native contract records all design-required facts rather than reconstructing them from prose:

- scene file size as an adapter-owned final fact and scene SHA-256 as an adapter-owned final fact; native only echoes the source fingerprint;
- complete pre-truncation empty-name and duplicate-name aggregate counts per exact object type, plus bounded records after deterministic sorting;
- finite/non-finite observations for publicly readable Node translation/rotation/scaling and PerspectiveCamera frustum, exact `[1, 32768]` validity for PerspectiveCamera viewport dimensions, and nonzero validity for publicly exposed unsigned RenderBuffer/RenderTarget dimensions; each field carries `supported`, `present`, and typed value/classification so unsupported never means zero;
- explicit authored render-pass/framebuffer/camera/interface identities and pass-to-camera contract facts. A Ramses `Property` has no scene object ID, so its identity is the owning LogicNode scene object ID plus `input` or `output` direction plus a typed root-to-leaf segment array. An array segment is exactly `{kind:"array",index}`. A struct segment is exactly `{kind:"struct",rawNameRank,safeName}` where `rawNameRank` is the zero-based rank after sorting the complete sibling raw-name byte strings and `safeName` is bounded display text; the SDK's unstable struct iteration index never participates. Duplicate/empty/invalid raw struct names make that contract explicitly ambiguous and unusable rather than guessed. Shuffled struct enumeration, long names sharing a bounded prefix, arrays, and duplicate/empty names must preserve or truthfully reject identity. Weak links are reported separately and never enter the dependency-cycle graph;
- exact-bounds state `unsupported_public_api` until a public Ramses 28.16 API proves otherwise.

Validation identity is aggregate-based. The installed Ramses validation issue API contributes only Error and Warning; R0 invents no informational issue severity. Group issues by source fingerprint, exact originating object type and scene object ID (or the tagged scene-level null origin), and normalized severity. Preserve exact uncapped error/warning aggregate counts, sort severity first, then apply the record/section caps. Within each aggregate sort unique bounded safe messages by encoded bytes, retain at most four, record total/unique/reported/truncated counts, and never use name or SDK iteration order in identity. Tests inject multiple issues on one object, duplicates, null origins, reordered input, 257 aggregates with the only Error last, and a fifth message and require identical retained bytes/aggregate/finding IDs plus exact truncation facts/counts.

Task 1 freezes the complete final policy table below; `ramses_probe_report.py` later implements it without invention. `nextActionId` values are recommendation identifiers, never executable capability/action IDs. `owner` is an exact enum: `content_author`, `pipeline_owner`, `sgfx_maintainer`, or `workstation_owner`. Each row uses `SDK_NATIVE_OPPORTUNITY` and `verified` confidence except every `undetermined_after_*`, which uses `open`.

| Native fact/outcome | Rule ID | Severity | Affected gates | Next action ID | Owner | Safe message | Subject discriminator | Evidence roles |
|---|---|---|---|---|---|---|---|---|
| validation aggregate | `ramses.native_validation` | normalized native error/warning | `interface,review` | `ramses.review_validation` | `content_author` | `Native Ramses validation reported an issue.` | object type/id or scene-level + severity | `native_report,source_scene` |
| omitted validation aggregates | `ramses.validation_evidence_truncated` | error when any Error omitted, otherwise warning | `interface,review` | `ramses.review_validation` | `content_author` | `Native Ramses validation evidence exceeded the bounded detail limit.` | omitted error/warning counts | `native_report,source_scene` |
| non-finite Node transform | `ramses.transform_nonfinite` | error | `interface,review` | `ramses.inspect_transform` | `content_author` | `A readable Ramses node transform contains a non-finite value.` | node scene object ID + translation/rotation/scaling field | `native_report,source_scene` |
| non-finite camera frustum | `ramses.camera_nonfinite` | error | `visual,review` | `ramses.inspect_camera` | `content_author` | `A readable Ramses camera frustum value is non-finite.` | camera scene object ID + frustum field | `native_report,source_scene` |
| invalid camera viewport | `ramses.camera_viewport_invalid` | error | `visual,review` | `ramses.inspect_camera` | `content_author` | `A readable Ramses camera viewport is outside the supported range.` | camera scene object ID + viewport field | `native_report,source_scene` |
| invalid render-buffer/target dimension | `ramses.render_dimension_invalid` | error | `visual,review` | `ramses.inspect_render_dimensions` | `content_author` | `A readable Ramses render dimension is invalid.` | render buffer/target scene object ID + dimension field | `native_report,source_scene` |
| omitted non-finite/invalid numeric facts | `ramses.numeric_anomaly_evidence_truncated` | error | `interface,visual,review` | `ramses.inspect_numeric_anomalies` | `content_author` | `Ramses numeric anomaly evidence exceeded the bounded detail limit.` | omitted anomaly category counts | `native_report,source_scene` |
| omitted failed/incomplete LogicEngine facts | `ramses.logic_evidence_truncated` | error | `interface,review` | `ramses.repair_logic_graph` | `content_author` | `Ramses logic failure evidence exceeded the bounded detail limit.` | exact omitted failed/incomplete engine counts | `native_report,source_scene` |
| authored perspective property rejection | `ramses.authored_application` | error | `interface,visual,review` | `ramses.repair_authored_application` | `content_author` | `The validated authored perspective could not be applied through the Ramses interface.` | owning engine/interface IDs + exact rejected property identity | `native_report,source_scene,source_perspective` |
| `scene_unresolved` | `ramses.scene_input` | error | `interface,review` | `ramses.supply_scene` | `pipeline_owner` | `The canonical Ramses scene could not be resolved.` | `input` | `native_report` |
| `metadata_unreadable` | `ramses.scene_metadata` | error | `interface,review` | `ramses.inspect_scene_metadata` | `pipeline_owner` | `Ramses scene metadata could not be read.` | `metadata` | `native_report,source_scene` |
| `toolchain_incompatible` | `ramses.toolchain_compatibility` | error | `interface,review` | `ramses.align_toolchain` | `pipeline_owner` | `The scene feature level is unsupported by this Ramses runtime.` | `metadata.feature_level` | `native_report,helper_runtime,source_scene` |
| `scene_load_failed` | `ramses.scene_load` | error | `interface,review` | `ramses.inspect_scene_load` | `pipeline_owner` | `Ramses could not load the scene after readable metadata.` | `scene_load` | `native_report,source_scene` |
| `logic_update_failed` | `ramses.logic_update` | error | `interface,review` | `ramses.repair_logic_graph` | `content_author` | `A Ramses logic update failed.` | exact `default_state` or `authored_perspective` update stage + logic engine ID + cycle/runtime/unknown category | `native_report,source_scene` |
| `display_or_offscreen_setup_failed` | `ramses.renderer_setup` | error | `visual,review` | `ramses.inspect_renderer_setup` | `workstation_owner` | `The configured OpenGL display or offscreen buffer could not be prepared.` | `display_setup` | `native_report,helper_runtime` |
| `scene_available_timeout` | `ramses.lifecycle` | error | `visual,review` | `ramses.inspect_lifecycle` | `sgfx_maintainer` | `The scene did not reach Available before the bounded deadline.` | `available` | `native_report` |
| `scene_ready_timeout` | `ramses.lifecycle` | error | `visual,review` | `ramses.inspect_lifecycle` | `sgfx_maintainer` | `The scene did not reach Ready before the bounded deadline.` | `ready` | `native_report` |
| `scene_rendered_timeout` | `ramses.lifecycle` | error | `visual,review` | `ramses.inspect_lifecycle` | `sgfx_maintainer` | `The scene did not reach Rendered before the bounded deadline.` | `rendered` | `native_report` |
| `authored_pass_or_camera_contract_missing` | `ramses.authored_contract` | warning | `interface,review` | `ramses.supply_authored_contract` | `content_author` | `The exact authored pass, camera, interface, property, or perspective contract is incomplete.` | first missing typed contract element | `native_report,source_scene` plus `source_perspective` when supplied |
| `readback_request_failed` | `ramses.first_frame` | error | `visual,review` | `ramses.inspect_readback` | `sgfx_maintainer` | `The bounded authored-frame readback request was rejected.` | `readback_request` | `native_report` |
| `readback_timeout` | `ramses.first_frame` | error | `visual,review` | `ramses.inspect_readback` | `sgfx_maintainer` | `The bounded authored-frame readback did not complete.` | `readback` | `native_report` |
| `clear_only_frame` | `ramses.first_frame` | warning | `visual,review` | `ramses.review_authored_frame` | `content_author` | `The authored-frame readback matched the configured clear color only.` | `first_frame` | `native_report,first_frame` |
| `visible_frame_observed` | `ramses.first_frame` | info | `visual,review` | `ramses.review_authored_frame` | `content_author` | `The authored-frame readback contained non-clear pixels; visual approval is still required.` | `first_frame` | `native_report,first_frame` |
| every `undetermined_after_<phase>` | `ramses.render_outcome` | warning | `review` | `ramses.rerun_probe` | `sgfx_maintainer` | `The probe could not determine a stable render outcome after the last proven phase.` | exact last-proven phase | `native_report` |

Opaque artifact roles are exactly `native_report`, `final_report`, `isolated_evidence`, `source_scene`, `source_perspective`, `helper_runtime`, `first_frame`, `stdout_log`, and `stderr_log`. Artifact ID is the first 32 lowercase hexadecimal characters of SHA-256 over UTF-8 canonical JSON of `["sgfx-ramses-r0-artifact-v1", runToken, role]`. It is run-scoped, collision-checked across all emitted roles, resolves only through the protected in-memory/run-root table, expires when that run is pruned, and never encodes or exposes a path.

`ProbeSubject` is an exact tagged union rather than one generic tuple. Variants are: validation `(sourceFingerprint,objectType,nullableObjectId,severity)`; numeric `(sourceFingerprint,objectType,objectId,field)`; logic `(sourceFingerprint,engineId,updateStage,category,optionalPropertyIdentity)`; authored application `(sourceFingerprint,engineId,interfaceId,propertyIdentity)`; lifecycle `(sourceFingerprint,phase,requestOrEventStage)`; frame `(sourceFingerprint,outcome)`; phase `(sourceFingerprint,phase,stableDiscriminator)`; and truncation `(sourceFingerprint,category,exactOmittedCounts)`. Unknown/missing variant fields fail schema validation. Tests place two severities, two numeric fields, two properties, two update stages, and two lifecycle failures on the same object and require distinct native subjects and final finding IDs.

Clear/visible findings require a securely published `first_frame` artifact. A successful readback whose WIC/frame staging fails still writes a complete protected native diagnostic, but Task 7 rejects the execution before `AcceptedNativeProbe`; no final policy report with an unresolved frame reference is created.

Add both exact schema documents during GREEN. The native schema contains only helper-owned facts plus the adapter-supplied identity claims. The final schema contains the approved section-8 `RamsesProbeReport` fields and Python-owned environment, digest, artifact, and source-mutation evidence. Neither schema lets native output assert that a digest or worktree guard passed.

- [ ] **Step 6: Implement deterministic classification precedence**

classify_render_outcome() returns the first proven missing/failing phase in the design order. Native validation issues do not change the render outcome. If evidence does not prove one cause, return Undetermined and serialize undetermined_after_<last-proven-phase>.

`scene_unresolved` remains in the pure classifier because the approved design freezes that code, but the standalone adapter normally resolves and holds the canonical scene before native launch. A missing/unresolvable prelaunch scene therefore produces adapter-level unavailable with no fabricated native report; tests exercise the enum/classifier without weakening that prerequisite boundary.

Positive finite values create typed observations but no finding. A public value that cannot be read records `supported=false` and creates no non-finite/invalid finding. Camera viewport width/height are valid only in `[1, 32768]`; offsets retain their public signed types. Add 0, 1, 32768, and 32769 viewport cases plus positive, non-finite/invalid, unsupported-public-API, and anomaly-last shuffled-over-cap tests for every policy row.

Build only the exact tagged `ProbeSubject` variants frozen above. Display names never define identity.

- [ ] **Step 7: Run the contract test and existing link probe GREEN**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_contract_test sgfx_cine_ramses_link_probe
ctest --test-dir build\cine-c0 -C RelWithDebInfo -R "sgfx_cine_ramses_(probe_contract|link_probe)" --output-on-failure
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 1
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 1 -Module unittest -ModuleArguments @("tests.test_ramses_probe_schema", "-v")
~~~

Expected: CTest has a complete passing footer, every stable code is exercised, and Ramses linked OK remains present.

- [ ] **Step 8: Review and commit the pure contract**

Run git diff --check and inspect the task diff for JSON/UI/process/filesystem work that belongs to later tasks. Commit:

~~~powershell
git add sg_preflight/data/ramses_r0/ramses_probe_native_report.schema.json sg_preflight/data/ramses_r0/ramses_probe_report.schema.json cpp/include/sgfx/cine/ramses_probe.h cpp/src/ramses_probe_contract.cpp cpp/tests/ramses_probe_contract_test.cpp tests/test_ramses_probe_schema.py cpp/CMakeLists.txt
git commit -m "feat(ramses): define R0 probe contract"
~~~

---

### Task 2: Add the strict native parser and inherited-handle serializer

**Files:**
- Modify: cpp/include/sgfx/cine/ramses_probe.h
- Modify: cpp/src/ramses_probe_contract.cpp
- Create: cpp/apps/ramses_probe_cli_support.h
- Create: cpp/apps/ramses_probe_cli_support.cpp
- Create: cpp/tests/ramses_probe_cli_test.cpp
- Modify: cpp/CMakeLists.txt

**Interfaces:**
- Consumes: validate_probe_request(), `RamsesAuthoredPerspective`, and a fabricated RamsesProbeNativeReport test object.
- Produces: app-private `RamsesProbeInvocation { RamsesProbeRequest request; uintptr_t native_report_handle; uintptr_t first_frame_handle; }`, Windows `parse_ramses_probe_arguments(const std::vector<std::wstring_view>&) -> RamsesProbeInvocation` with an explicit strict UTF-8 filesystem adapter only for non-Windows tests, exact selected-view parser, app-private transport validation, and app-private write_native_probe_report_to_handle(). This task deliberately does not create the production executable; Task 4 wires the parser/serializer only after real scene and lifecycle facts exist. CLI grammar, inherited handles, and JSON do not enter the installed sgfx::cine public header.

- [ ] **Step 1: Write failing parser, path, and no-write tests**

Test the exact accepted argument set:

~~~text
--scene <regular-file>
[--perspective <regular-json-file> --perspective-id <canonical-id>]
--profile-id <canonical-id>
--scenario-id <canonical-id>
--source-fingerprint <64-lowercase-hex>
--run-token <32-lowercase-hex>
--generation <uint64>
--backend opengl
--width 480
--height 270
--timeout-ms 60000
--render-timeout-ms 30000
--parent-start-qpc <uint64>
--qpc-frequency <uint64>
--native-work-deadline-qpc <uint64>
--parent-deadline-qpc <uint64>
--native-report-handle <uintptr>
--first-frame-handle <uintptr>
~~~

Reject missing required options, duplicate/unknown options, case-mutated backend, upper-case digest/token, generation/clock arithmetic overflow, QPC frequency mismatch, deadline disagreement/already-expired parent deadline, reparse input, output-handle identity/link/type/emptiness/writability mismatch, equal or standard-stream output handles, one-sided perspective pairing, invalid identifiers, and all out-of-contract numeric values. Assert that neither inherited output handle changes after every rejection. Python alone proves each handle's ancestry beneath its pinned attempt root, every path/root disjointness rule, and source containment before launch; native has no output-root argument or semantic root identity and never claims those proofs. No-perspective and missing-selected-view requests are represented only as typed parser states here; Task 4 proves their real metadata-through-logic execution.

Assert only that the `opengl` token converts to and serializes from `EDeviceType::GLES_3_0`; no GL 4.x or Vulkan alias is accepted. Device support and execution are Task 4 renderer facts, not parser claims.

- [ ] **Step 2: Build the CLI contract test and verify RED**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_cli_test
~~~

Expected: failure because argument parsing and the CLI target do not exist.

- [ ] **Step 3: Implement the strict parser as app-private support**

On Windows the executable uses `wmain(int, wchar_t**)` and passes only `std::wstring_view` values to app-private `parse_ramses_probe_arguments()`; native never reparses a lossy active-code-page `char** argv`. The parser compares fixed option names as ASCII-wide tokens. For a numeric token it first rejects every code unit outside ASCII, copies the bounded validated code units one-for-one into `char` storage without locale/code-page conversion, then calls `std::from_chars(begin,end,...)` and accepts only `ec == std::errc{}` plus `ptr == end`; `wcstoull` and every path/Unicode narrowing are forbidden. It constructs every filesystem path directly from UTF-16, uses a seen-option set for duplicates, and performs no shell invocation. Non-Windows test support uses an explicit UTF-8-to-filesystem conversion with invalid-sequence rejection, never locale-dependent narrowing. It returns the semantic public request nested inside the app-private invocation transport, has no output pathname, and never creates an output directory. Task 2 tests empty/overflow/non-ASCII/partial numeric conversions plus direct wide-parser acceptance with legal regular-file paths containing spaces, nested separators, non-ASCII BMP characters, and supplementary characters. Empty tokens, quotes, and terminal backslashes remain arbitrary quoting-unit cases rather than impossible Windows filenames. Task 7 owns the real source `CreateProcessW` round-trip test using the reviewed CRT-compatible quoting routine and legal Unicode scene/perspective files.

The only safe process exit contract is:

~~~text
0  complete native report written to the inherited parent-owned staging handle, including classified scene findings or non-render outcomes
2  invalid_arguments
3  input_unavailable
4  internal_probe_failure_before_a_complete_report
5  report_write_failed
~~~

stderr contains exactly one safe token and stdout is empty. Metadata, compatibility, load, validation, logic, authored-contract, lifecycle, clear-frame, and readback outcomes are report data and therefore exit 0 when the complete schema-valid report was written; they are not collapsed into exit 4.

- [ ] **Step 4: Implement exact perspective parsing and bounded handle-only JSON**

When supplied, read at most 1 MiB with nlohmann_json through a duplicate-key-detecting callback. Reject duplicate keys, non-object top level, invalid UTF-8/control characters, non-finite/non-number values, depth above eight, more than 256 top-level views, key/string/property limits from Global Constraints, booleans in integer positions, and unknown fields inside the selected typed perspective. Unselected view bodies are counted and structurally bounded but not semantically interpreted. A syntactically valid top-level object with no requested ID or with an absent required property becomes typed `MissingAuthoredContract`, not invalid input, so Task 4 can complete metadata through logic while skipping rendering.

For a present selected contract, the exact allowlisted top-level fields are `AspectFromResolution_isEnabled`, `CraneGimbal`, `Frustum`, `LocalTranslation`, `Origin`, `Scale`, `ShiftXY`, and `Viewport`; any ninth field is invalid. Require exact JSON types: `AspectFromResolution_isEnabled` is Boolean; `CraneGimbal.{Distance,Yaw,Pitch,Roll}`, `Frustum.{HorizontalFOV,AspectRatio,NearPlane,FarPlane}`, `LocalTranslation[3]`, `Origin[3]`, and `Scale` are finite and representable as `float`; `ShiftXY[2]` and `Viewport.{OffsetX,OffsetY,Width,Height}` are mathematical integers representable as `int32_t`. Nested objects also reject unknown keys. Require Distance, HorizontalFOV, AspectRatio, NearPlane, and Scale greater than zero, FarPlane greater than NearPlane, and viewport Width/Height in the installed Ramses public limit `[1, 32768]`. Do not round fractional values. `LocalTranslation` is retained with `not_applied` provenance and never enters an interface property map. Test viewport values 0, 1, 32768, and 32769 independently from non-finite frustum values.

The JSON key `AspectFromResolution_isEnabled` maps only to runtime `Interface_CameraCrane/AutoAspect`, matching the two proven real paths. The C0 preview prototype's runtime `AspectFromResolution_isEnabled` property and loose top-level `CraneGimbal` fallback are not R0 aliases. If a future accepted export proves another spelling, add a separately versioned typed contract and independent tests instead of silent fallback. Preserve the source file only as an input fact; do not copy its body into the report.

Build the bounded report object first and serialize canonical UTF-8 bytes in memory. `write_native_probe_report_to_handle()` verifies the inherited parent-owned report handle again, truncates only that already-empty held identity to zero, writes all bytes, enforces the 2 MiB cap, calls `FlushFileBuffers`, seeks and reads through the same handle, and requires byte-for-byte equality with the in-memory canonical serialization. It never creates, opens, renames, hashes, or unlinks a pathname. Python retains the original non-inheritable handle and performs strict schema validation plus final no-replace publication in Task 7 after process exit. Tests duplicate parent/child handles, replace the staging directory entry while both handles are held, close the child copy, and prove the parent still validates/publishes only the original identity.

- [ ] **Step 5: Prove the parser and serializer with a fabricated bounded report**

Link the CLI-support test to `sgfx::cine` and nlohmann_json::nlohmann_json. Serialize a maximum-shaped fabricated report through duplicated parent-owned handles, reparse it through the retained parent handle, and require deterministic bytes below 2 MiB. Add a G45-shaped synthetic perspective containing exactly the eight real selected-view fields including `LocalTranslation`; require acceptance and `not_applied`. Mutate each field/type/bound, QPC relation, and output-handle property independently and add a ninth truly unknown field; each invalid case leaves both handles unchanged. No production helper target exists yet.

- [ ] **Step 6: Run parser, executable, and existing preview CLI regressions**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_cli_test sgfx_cine_ramses_preview_cli sgfx_cine_ramses_preview_contract_test
ctest --test-dir build\cine-c0 -C RelWithDebInfo -R "sgfx_cine_ramses_(probe_cli|preview_contract)" --output-on-failure
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 2
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 2 -Module unittest -ModuleArguments @("tests.test_qt_quick_preview.TestNativePreviewHelper.test_cli_rejects_unknown_missing_and_out_of_bounds_requests_without_writes", "-v")
~~~

Expected: native tests pass, the G45-shaped perspective is accepted without applying `LocalTranslation`, and the legacy preview CLI keeps its exact safe-token/no-write behavior.

- [ ] **Step 7: Review and commit the native shell**

Inspect the diff for arbitrary options, path prose on stderr, output creation before validation, or a second JSON dependency. Commit:

~~~powershell
git add cpp/include/sgfx/cine/ramses_probe.h cpp/src/ramses_probe_contract.cpp cpp/apps/ramses_probe_cli_support.h cpp/apps/ramses_probe_cli_support.cpp cpp/tests/ramses_probe_cli_test.cpp cpp/CMakeLists.txt
git commit -m "feat(ramses): parse strict R0 requests"
~~~

---

### Task 3: Collect metadata, native validation, inventory, and logic facts

**Files:**
- Create: cpp/src/ramses_probe_internal.h
- Create: cpp/src/ramses_probe_scene.cpp
- Modify: cpp/src/ramses_probe.cpp
- Create: cpp/tests/ramses_probe_scene_test.cpp
- Create: cpp/tests/ramses_probe_logic_test.cpp
- Modify: cpp/CMakeLists.txt

**Interfaces:**
- Consumes: Ramses 28.16 descriptor overloads `RamsesClient::GetMetadataFromFile(int,size_t,size_t)` and `loadSceneFromFileDescriptor(int,size_t,size_t)`, SceneMetadata, ValidationReport, SceneObjectIterator, LogicEngine::enableUpdateReport(), update(), and getLastUpdateReport().
- Produces: a private non-movable RAII `ProbeSceneSession`, returned as `std::unique_ptr<ProbeSceneSession>`, plus `ProbeSceneFacts`; one retained original Windows scene HANDLE/identity guard, an independently owned metadata descriptor, a distinct load descriptor transferred exactly once to Ramses, the metadata-through-logic portion of RamsesProbeNativeReport, and one retained loaded scene for Task 4.

- [ ] **Step 1: Write a failing generated-scene test**

Generate a temporary Ramses scene with the public SDK. Because Ramses 28.16 `Scene::saveToFile` accepts only `std::string_view`, save first to a held ASCII-only staging leaf, complete and close the SDK save, then move it with no-replace wide Windows `MoveFileExW(..., MOVEFILE_WRITE_THROUGH)` into the legal nested final path containing spaces plus BMP and supplementary Unicode characters under the test output directory. Reopen/validate the final identity before exercising `CreateFileW` metadata/load; never call `path.string()` or use the active code page. The non-Windows fixture uses its lossless native filesystem rename. Include:

- two Nodes sharing an empty or duplicate display name but different scene object IDs;
- one PerspectiveCamera;
- one RenderPass and one RenderGroup;
- one LogicEngine with deterministic executable and skipped nodes where the public SDK fixture permits; and
- metadataString equal to synthetic-r0-fixture.

The test requires:

~~~cpp
require(report.scene_metadata.readable);
require(report.scene_metadata.metadata_string == "synthetic-r0-fixture");
require(report.inventory.at("nodes").total_count == 2u);
require(report.inventory.at("nodes").records.size() == 2u);
require(report.inventory.at("nodes").records[0].object_id !=
        report.inventory.at("nodes").records[1].object_id);
require(report.logic.state == ProbePhaseState::Passed);
require(report.logic.engine_count == 1u);
require(report.logic.update_report_present);
require(report.logic.total_update_microseconds_present);
~~~

Add unreadable-metadata, every concrete `EFeatureLevel_01` through `_04`, an unknown raw feature-level value, `setRequestedRamsesShellType(None)==false`, pre-validation `scene.flush()==false`, and `loadSceneFromFileDescriptor` null cases; an injected renderer-factory counter must remain zero for the unreadable/unknown/shell/flush/null cases. Prove metadata and load receive distinct binary descriptors at offset zero with the exact checked file length, metadata retains/closes its own descriptor, Ramses receives ownership of the load descriptor exactly once even when load returns null, and the original scene identity guard remains held. Add deterministic synthetic `LogicUpdateObservation` fixtures for executed, skipped, skipped-dirty, slowest-node, cycle-category failure, and runtime/unknown-category failure. For a failed update, assert through an injected call counter that `getLastUpdateReport()` is never called because Ramses documents its contents as undefined after `update()==false`.

Also generate publicly readable numeric anomalies without serializing NaN/Infinity: inject or construct Node transform, camera frustum, camera viewport, and render-buffer/target dimension observations at the collector seam and require exact `finite`, `nonfinite`, `invalid`, or `unsupported` typed states and the matching Task 1 policy row. Frustum non-finite tests are distinct from viewport-invalid 0 and 32769 tests. Require explicit authored pass/camera/interface object IDs. Require every property identity to use Task 1's owning LogicNode ID/direction plus typed struct-name-rank or array-index path; shuffle struct enumeration and use distinct long names with one bounded prefix to prove stable collision-free identity. Exact geometry bounds remain `unsupported_public_api` rather than a guessed zero extent.

Generate a second scene with three LogicEngines in shuffled SDK iteration order. Exactly one engine owns one complete typed `Interface_CameraCrane`; the other engines have unrelated or deliberately incomplete interfaces. Require default-state updates in ascending engine object-ID order and retain the selected interface's owning engine ID. Repeat with zero and two complete exact interfaces; both are typed missing/ambiguous authored contracts and no renderer may be created. Duplicate/empty struct member names make only that property contract ambiguous and never select by iteration order.

- [ ] **Step 2: Build the scene test and verify RED**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_scene_test sgfx_cine_ramses_probe_logic_test
~~~

Expected: failure because collect_ramses_scene_facts() does not exist.

- [ ] **Step 3: Implement metadata-first load and compatibility evidence**

On Windows open the canonical scene only with `CreateFileW` as a non-inheritable regular local disk file, retain that original identity guard, obtain a checked nonnegative `GetFileSizeEx` value representable as `size_t`, and create two independent non-inheritable `DuplicateHandle` copies. Each duplicate remains owned by HANDLE RAII while `_open_osfhandle(..., _O_RDONLY | _O_BINARY)` runs; `fd == -1` leaves that HANDLE guard responsible for closure, while `fd >= 0` is the only point that releases the HANDLE guard into descriptor RAII. Complete both conversions before either Ramses SDK call. A first- or second-conversion failure closes every acquired resource and calls neither metadata nor load. After both succeed, call `RamsesClient::GetMetadataFromFile(metadataFd, 0, length)` before constructing `RamsesFramework`, then close only the metadata descriptor under local fd RAII because that overload does not transfer ownership. Pass `loadFd.release()` exactly once as the first argument of `loadSceneFromFileDescriptor(loadFd.release(), 0, length)` and relinquish it unconditionally at that call because Ramses owns and closes it, including the null-scene case. Tests inject each conversion failure independently, assert zero SDK calls, and detect double-close/leak/early-release. Never narrow the UTF-16 path or call a Ramses pathname overload. The non-Windows development path follows the same independent-descriptor/offset/length/ownership contract through its native filesystem API. Record linked Ramses version, scene Ramses version, exporter version, and the exact concrete `EFeatureLevel_01` through `_04` enumerator/raw value plus bounded metadata text/original-byte-count/truncated facts; native code does not hash metadata. Create one private `std::unique_ptr<ProbeSceneSession>` at the exact supported scene feature level with console logging Off. Check `setRequestedRamsesShellType(None)` before `RamsesFramework` construction. False records Metadata Passed, SceneLoad Failed, later substantive phases NotRun, and `undetermined_after_metadata`; it constructs no framework and still emits a normal exit-0 native report when finalization succeeds. The session owns `std::unique_ptr<RamsesFramework>` and retains non-owning client/scene pointers created by that framework; it is neither copied nor moved. It remains disconnected while the client is created, the scene is loaded, and facts are collected. Task 4 may create the renderer only after render prerequisites pass but before the session's single `connect()`. R0 does not reload the same descriptor-backed scene for Task 4 and never returns a by-value object containing `RamsesFramework`.

The concrete transfer call is exactly `loadSceneFromFileDescriptor(loadFd.release(), 0, length)`; no descriptor variable is used after release.

Compatibility is factual:

- unreadable metadata -> metadata_unreadable;
- exact unsupported Ramses feature-level signal -> toolchain_incompatible;
- readable metadata/version drift without that exact signal -> factual version evidence only;
- loadSceneFromFileDescriptor null -> scene_load_failed with unknown cause; never parse Ramses error prose into `toolchain_incompatible`;
- a successful 28.15.1 export load on 28.16.0 records both versions but does not claim universal compatibility.

- [ ] **Step 4: Implement validation and bounded inventory**

Call the recommended pre-validation `scene.flush()` and check its Boolean result before constructing an empty ValidationReport or calling `scene.validate(report)`. False records SceneLoad Passed, Validation Failed, later substantive phases NotRun, and `undetermined_after_scene_load`; it does not call `validate()`, performs bounded teardown, and emits a normal exit-0 native report when finalization succeeds. On success, aggregate only the installed SDK's native Error and Warning issue severities using Task 1's exact origin/severity identity. Preserve bounded normalized messages, their original byte lengths/truncation, object type, scene object ID when the object is a SceneObject, and name as display text only. Sort the complete aggregate set by anomaly severity and stable identity before retaining 256 and sort unique messages before retaining four. Preserve exact uncapped Error/Warning totals and emit the explicit validation-truncation fact/finding whenever omitted issues could affect policy. Do not compute native message hashes or invent an informational SDK severity. The final report receives only the fixed safe policy message; raw SDK prose stays in the protected native artifact.

Iterate the public object types required by the design. Concrete buckets count only objects whose exact `getType()` equals that bucket, so Node/MeshNode and Resource/subtype counts do not overlap. Any explicitly useful aggregate count is labelled aggregate and never summed with concrete buckets. Before truncating records, compute exact total/empty-name/duplicate-name counts per type. Each bucket records total_count, reported_count, truncated_count, and at most 256 records sorted by object ID then type. Unsupported object types are explicit unsupported buckets, not zero. Read public Node transform, PerspectiveCamera frustum/viewport, RenderPass/camera, RenderTarget/RenderBuffer dimension, and authored interface/property facts through the installed public headers and emit typed supported/present/finite states. Canonicalize struct properties by the complete raw sibling-name set and array properties by index exactly as Task 1 specifies; never persist or compare the SDK's unstable struct iteration index. Do not report the prototype's mesh-node translation extent as geometry bounds; exact bounds remain unsupported unless the public API proves them.

- [ ] **Step 5: Implement one default-state update report per sorted LogicEngine and a pure reducer**

Collect every LogicEngine pointer and scene object ID, sort by object ID, reject duplicate IDs, then for each engine in that order:

~~~cpp
engine->enableUpdateReport(true);
const bool updated = engine->update();
if (updated) {
    auto updateReport = engine->getLastUpdateReport();
    // copy counts and microsecond values before updateReport is destroyed
}
engine->enableUpdateReport(false);
~~~

Copy each successful SDK report immediately into a bounded `LogicUpdateObservation`, then reduce it into facts. Record exact total/success/failure/incomplete engine counts plus executed, skipped, skipped-and-dirty, topology-sort time, total time, link activations, and slowest executed node with explicit present/supported flags so zero never means unpopulated. Compute each engine's cycle/runtime/unknown category and complete failure counts before retaining at most 64 per-engine records; failed/incomplete records sort first by engine ID, and any omitted failed/incomplete engine emits the exact `logic_evidence_truncated` fact. The 512 capped node records are timing/detail only and cannot hide policy. A failed update records `logic_update_failed` with `update_stage=default_state` and exact engine ID, never reads `getLastUpdateReport()`, and does not become `helper_crash`. Determine `cycle` only by building an exact directed graph from public `LogicEngine::getPropertyLinks()` entries where `isWeakLink == false`, with stable typed property identities, and applying a deterministic cycle algorithm. Count weak links separately but never use them as dependency edges. If that strong-link graph is acyclic, category is `runtime` only when another typed public SDK result explicitly proves runtime failure; otherwise category is `unknown`. Never classify by console/error-message text. Select authored input only after scanning all sorted engines: exactly one complete typed `Interface_CameraCrane` retains owning engine/interface IDs; zero or multiple is missing/ambiguous contract. Add strong-cycle, weak-cycle-only, mixed-link, shuffled-link/engine/interface, 65-engine failure-last, 513-node-detail, duplicate-interface, and synthetic 28.15.1-export-on-28.16.0 regressions.

- [ ] **Step 6: Run native scene, contract, and link tests GREEN**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_scene_test sgfx_cine_ramses_probe_logic_test sgfx_cine_ramses_probe_contract_test sgfx_cine_ramses_link_probe
ctest --test-dir build\cine-c0 -C RelWithDebInfo -R "sgfx_cine_ramses_(probe_scene|probe_logic|probe_contract|link_probe)" --output-on-failure
~~~

Expected: CTest has a complete passing footer; malformed metadata exits before renderer evidence; duplicate/empty names remain distinct; executed/skipped/skipped-dirty/timing/slowest and both failure categories are populated truthfully.

- [ ] **Step 7: Review and commit scene facts**

Confirm no scene save in production sources, rename, setName, direct camera mutation, source write, renderer creation, or internal Ramses header is used. Public-SDK `saveToFile` is permitted only inside the synthetic test-fixture generator. Commit:

~~~powershell
git add cpp/src/ramses_probe_internal.h cpp/src/ramses_probe_scene.cpp cpp/src/ramses_probe.cpp cpp/tests/ramses_probe_scene_test.cpp cpp/tests/ramses_probe_logic_test.cpp cpp/CMakeLists.txt
git commit -m "feat(ramses): collect R0 scene facts"
~~~

---

### Task 4: Reuse authored rendering for lifecycle and first-frame evidence

**Files:**
- Create: cpp/src/ramses_preview_internal.h
- Modify: cpp/src/ramses_preview.cpp
- Modify: cpp/src/ramses_probe.cpp
- Create: cpp/apps/ramses_probe_cli.cpp
- Create: cpp/apps/ramses_probe_png_wic.h
- Create: cpp/apps/ramses_probe_png_wic.cpp
- Create: cpp/tests/ramses_probe_lifecycle_test.cpp
- Create: cpp/tests/ramses_probe_synthetic_test.cpp
- Modify: cpp/tests/ramses_preview_contract_test.cpp
- Modify: cpp/CMakeLists.txt
- Modify: tests/test_qt_quick_preview.py

**Interfaces:**
- Consumes: the retained `ProbeSceneSession`, optional exact parsed authored perspective values, existing hidden SDL/Ramses renderer, publication-observed Available -> requested Ready -> requested Rendered waits, versioned scene flush, and `readPixels`.
- Produces: one source-private loaded-scene renderer used by the existing `render_ramses_preview()` wrapper and R0, Task 1's one canonical `ProbeLifecycleFacts`/`ProbeFrameFacts`, one-frame R0 render, complete RamsesProbeNativeReport, stable black/non-render classification, and the production `sgfx_ramses_probe.exe` wired to Task 2's parser/serializer. No duplicate `PreviewLifecycleFacts`/`PreviewFrameFacts` type or installed preview-header expansion is permitted.

- [ ] **Step 1: Write failing lifecycle and synthetic visible-frame tests**

Add a pure absolute-QPC deadline test for success and missing publish-derived Available, requested Ready, requested Rendered, versioned flush acknowledgement, and readback events. The fake clock must prove bounded termination without sleeping and preserve the last proven phase. Cover wrong scene/version-tag callbacks, publish rejection, rejected Ready/Rendered/setup requests, every `RendererSceneControl::flush()` boundary, unsupported device, `readPixels` false, renderer flush false after an accepted readback request, callback failure, wrong RGBA8 byte count, and parent startup/resume delay before native `main`. Exact immediate-rejection mapping is: `RamsesFramework::connect()` or display/offscreen creation/clear/renderer-flush rejection -> `display_or_offscreen_setup_failed`; publish rejection or rejected initial untagged scene flush after publication -> `undetermined_after_display_setup`; `setSceneMapping()`, `setSceneDisplayBufferAssignment()`, their pre-Ready scene-control flush, Ready request, or Ready flush rejection -> `undetermined_after_available`; authored setter/update/tagged-flush/Rendered request or its required scene-control flush rejection -> `undetermined_after_ready` except the already stable `logic_update_failed`; `readPixels` rejection or the immediately following required renderer flush rejection -> `readback_request_failed`. Every non-threaded pump iteration calls exactly one checked `RamsesRenderer::doOneLoop()` before dispatching callbacks. A false `doOneLoop()`, `RamsesRenderer::dispatchEvents()`, or `RendererSceneControl::dispatchEvents()` before DisplaySetup passes is `display_or_offscreen_setup_failed`; during forward progress it is immediate `undetermined_after_<last-proven-phase>`, never a timeout; during teardown it is teardown failure and prevents lane acceptance. Add injected-false coverage for connect, mapping, assignment, the pre-Ready flush, each of those three pump calls at every setup/Available/Ready/tagged-flush/Rendered/readback lifecycle position, and every teardown wait, plus the unchanged C0 preview regression. An accepted tagged scene flush whose exact matching acknowledgement misses the authored deadline is `undetermined_after_ready`, never `scene_rendered_timeout`, because no Rendered request has occurred. `scene_available_timeout`, `scene_ready_timeout`, `scene_rendered_timeout`, and `readback_timeout` are legal only when their corresponding request/flush was accepted but the named state/readback event missed the deadline. R0 never requests initial Available; it observes the publish-derived transition.

Add an injectable session/load counter and require exactly one metadata read and one scene load for a complete R0 run. The existing preview wrapper remains separately covered and may construct its own one-load session.

Generate a synthetic scene with one authored framebuffer pass, camera, visible triangle, LogicEngine, and exact `Interface_CameraCrane` input tree whose Boolean property is `AutoAspect`. Bind that exact interface through the LogicEngine to the effective camera values. Make the untouched/default state clear-only or otherwise deterministically distinguishable. Generate perspectives A and B that differ only in finite `LocalTranslation`; both must produce byte-identical interface values, public effective camera state, and frame while recording different source values with `localTranslationApplication=not_applied`. Generate perspective C with A's `LocalTranslation` and exactly one different applied camera-crane property; C must change that exact interface property, the corresponding public effective camera state, and the frame. Instrument the synthetic camera wrapper so any direct camera mutation increments a forbidden-call counter, require zero, and add a static source scan forbidding direct camera setters in all R0 production sources. These A/B/C tests fail if `LocalTranslation` is applied, an authorized property is ignored, multiple properties are changed, or direct camera mutation replaces interface application. Require:

~~~cpp
require(report.lifecycle.available.state == ProbePhaseState::Passed);
require(report.lifecycle.ready.state == ProbePhaseState::Passed);
require(report.lifecycle.rendered.state == ProbePhaseState::Passed);
require(report.lifecycle.readback.state == ProbePhaseState::Passed);
require(report.first_frame.outcome_code == "visible_frame_observed");
require(report.first_frame.non_clear_pixel_count > 0u);
require(report.artifacts.size() == 1u);
require(report.artifacts[0].file_name == "first-frame.png");
~~~

Generate the same scene without the camera-crane interface and require `authored_pass_or_camera_contract_missing`, no PNG, and no direct-camera fallback. Repeat with no perspective pair, absent requested perspective ID, and an absent required JSON property; each must preserve completed metadata/validation/inventory/logic facts and skip renderer creation. Add a clear-only synthetic render using the exact configured clear bytes and require `clear_only_frame`; wrong-size/failed-callback/read-request cases remain their exact typed failure or `undetermined_after_readback`, never visible.

Add Windows-only WIC tests around the exact R0 encoder. Convert the Ramses bottom-left RGBA8 callback into top-left 32bpp BGRA by one vertical flip plus one R/B swap, write through an audited 1 MiB-capped in-memory `IStream`, require `IWICBitmapEncoder` PNG with an unchanged `GUID_WICPixelFormat32bppBGRA`, no caller metadata, exact 480 x 270, and checked COM/HRESULT lifetimes, then copy only the bounded encoded bytes to the inherited frame handle. Decode with Task 7's strict standard-library PNG decoder and require byte-exact top-left RGBA recovery; repeat encoding locally and require deterministic bytes. Cover COM initialization states, encoder/pixel-format substitution, stream overflow, metadata/chunk drift, write/commit failure, and channel/orientation fixtures. Link only Windows system `windowscodecs` and `ole32`; add no packaged DLL or production dependency. Existing C0 remains on its unchanged Ramses pathname saver.

- [ ] **Step 2: Build/run the lifecycle and synthetic tests RED**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_lifecycle_test sgfx_cine_ramses_probe_synthetic_test
ctest --test-dir build\cine-c0 -C RelWithDebInfo -R "sgfx_cine_ramses_probe_(lifecycle|synthetic)" --output-on-failure
~~~

Expected: both targets fail because the lifecycle reducer, exact perspective input, and typed lifecycle/frame facts do not exist.

- [ ] **Step 3: Extract the loaded-scene renderer and wire the existing typed request without changing preview defaults**

Use Task 1's `RamsesAuthoredPerspective`; do not introduce or reinterpret a second perspective type here.

Move only display/offscreen mapping, event pumping, authored camera application, and readback into a private loaded-scene function with an explicit internal contract mode. Existing C0 callers keep `C0Existing`, whose `render_ramses_preview()` wrapper performs the same one metadata read/scene load and preserves its current property spelling, loose-root fallback, finite turntable, output, and defaults. R0 uses only `ExportedCameraCraneV1`, passes Task 3's retained session, supplies the explicit perspective, requests exactly one frame, and requires `Interface_CameraCrane/AutoAspect` plus the full typed tree. The modes do not alias or fall back into each other.

- [ ] **Step 4: Record exact lifecycle and readback facts**

Task 2 proved only the token mapping. Here, call `DisplayConfig::IsSupported(EDeviceType::GLES_3_0)`, require the Boolean result from `setDeviceType`, and use the exact backend state machine: an unknown token is invalid arguments/exit 2 with no report; a missing prelaunch runtime prerequisite is adapter-level unavailable with no report; a valid `opengl` request whose public `IsSupported(GLES_3_0)` is false completes a report with exit 0, DisplaySetup `Unavailable`, stable `display_or_offscreen_setup_failed`, and all earlier facts preserved. Require the Boolean result from `setDeviceType`; other rejected display/offscreen setup requests complete the same stable outcome with phase `Failed` rather than `Unavailable`.

After all non-render prerequisites pass, create the renderer while the framework is still disconnected, then require the session's one `connect()` to return true. The checked lifecycle order is exactly: create the display/offscreen resources, set the clear color through the float-domain contract below, call checked `RamsesRenderer::flush()` after each asynchronous renderer command, and prove readiness plus the renderer-owned clear value; subscribe scene-control callbacks; accept `scene.publish(EScenePublicationMode::LocalOnly)` and the initial untagged scene flush; observe the exact scene reach Available as the publish/flush consequence while requiring every pump iteration to call exactly one checked `RamsesRenderer::doOneLoop()`, then checked renderer and scene-control `dispatchEvents()`; require `setSceneMapping()` true; require `setSceneDisplayBufferAssignment()` true; require their pre-Ready `RendererSceneControl::flush()` true; request Ready, flush, and observe Ready; start the authored-render timer; apply the authorized perspective and perform the one additional owning-engine logic update; issue one nonzero tagged scene flush and observe its matching acknowledgement; request Rendered, require its scene-control flush, and observe Rendered; issue one readback request, require the immediately following `RamsesRenderer::flush()`, and observe its callback. Check every return value and callback identity under the exact mappings above; `doOneLoop()` false follows the same setup/forward/teardown mapping and cannot degrade into a missing-callback timeout. Although the generic public state setter accepts enum values, R0 never requests initial Available; it observes the publish-derived transition. The public phase list retains `DisplaySetup` before `Available`; its passed state is emitted only after display/offscreen readiness and clear facts are complete.

Subscribe the renderer/scene-control before publishing and before issuing exactly one nonzero `sceneVersionTag_t`. Immediately before applying the authorized perspective, record `authored_render_phase_start`. Apply every allowlisted value through the one selected complete `Interface_CameraCrane` and check every setter result. The first rejected setter records the typed authored-application fact with owning engine/interface/property identity, uses approved primary outcome `undetermined_after_ready`, performs no authored-state update, tagged flush, Rendered request, or readback, and preserves all earlier facts. Only after every setter succeeds, call exactly one additional `update()` on that interface's owning LogicEngine; no other engine updates a second time. A false result records approved `logic_update_failed` with `update_stage=authored_perspective`, never calls `getLastUpdateReport()`, and performs no tagged flush, Rendered request, or readback. On success call `scene->flush(tag)` and accept only `sceneFlushed` for the exact scene and tag. Perspective application, update, flush acknowledgement, Rendered wait, and readback all remain inside the same 30-second sub-budget. Tests reject each property setter and the owning-engine second update independently, shuffle engine/interface iteration, and prove downstream/other-engine call counts remain zero.

Record request accepted/rejected, display/offscreen setup, each state request/event elapsed time, versioned flush request/acknowledgement, readback request/result elapsed time, byte count, exact-clear comparison count, channel min/max/mean, and saved PNG result. The callback copies exactly the bounded `480 * 270 * 4` RGBA8 bytes into owned storage before returning and retains no SDK pointer or span beyond callback dispatch. The setter domain is exactly `setDisplayBufferClearColor(display,buffer,vec4f{0.f,0.f,0.f,1.f})` with checked true return and checked renderer flush; Ramses 28.16 float channels are never given byte value 255. The independently tested readback comparison domain is exactly RGBA8 bytes `{0,0,0,255}`. Classify `clear_only_frame` only when every returned pixel equals those exact bytes and `visible_frame_observed` only when at least one differs. Tests independently reject a wrong float setter domain, false setter/flush, and wrong RGBA8 comparison without conflating the two domains. Do not reuse the prototype's RGB-sum brightness threshold. A successful readback means the callback, format, exact byte count, owned copy, effective clear, statistics, and stable clear/visible classification all passed. If any cannot be proven, set the readback phase to Failed, return `undetermined_after_readback`, perform no WIC encoding/frame staging/publication, and require a null frame digest. Do not store HWND, process, path, or raw Ramses error in a public summary.

All native deadlines use the parent-supplied absolute QPC contract and Global Constraints' exact integer tick formula. Stop new probe work at `nativeWorkDeadlineQpc`; finish asynchronous rollback/unpublish/offscreen/display callback pumping by parent start +58.400 seconds; disconnect by +58.550; require checked synchronous renderer/client destruction plus completion of the void framework reset call by +58.700; and write/flush the inherited native-report handle and, iff a frame was produced, the inherited frame handle by +59.000. A receipt native process then exits immediately; by +60.000 its source parent must observe the direct signal, capture exit, join native drains, close thread/process/pipe handles, observe Job `ActiveProcesses == 0`, close the Job, and release the lease in that order before performing post-process native/final/evidence validation outside the timing field. A product host has until +59.700 to validate the inner retained native/frame/log bytes, emit outer status, and begin normal exit; by +60.000 the source worker must observe the outer signal, capture exit, join outer drains, close outer thread/process/pipe handles, observe whole-Job active-zero, close the Job, and release the lease in that order. Only then may the source parent re-read original handles, revalidate source/runtime/Git, and construct final/evidence outside process timing. Available and Ready waits use the same remaining absolute work deadline. The authored deadline is `min(nativeWorkDeadlineQpc, authoredRenderStartQpc + 30 seconds)`. A disconnect, destructor/reset overrun or abnormal termination, product-host transport finalization, or process-cleanup stage that misses its boundary cannot be called complete; deadline enforcement yields execution failure with no accepted report. Boundary tests cover delayed CreateProcess/resume/CRT startup, 57.999 seconds, the 58-second work stop, every absolute finalization boundary, exact floor/ceil tick arithmetic at non-divisible QPC frequencies, missing/delayed teardown callbacks, authored start with less than 30 seconds remaining, each covered sub-phase delay, native/frame staging-handle failure, blocking disconnect/destruction/reset, packaged native transport validation, outer-status delay, direct-handle/drain/active-zero/Job-close/lease-release delay, and parent termination at 60 seconds.

- [ ] **Step 5: Assemble and classify the complete native report**

RamsesProbe first collects Task 3 scene facts and retains that same session. Native validation findings remain orthogonal. It calls the shared loaded-scene renderer once only when metadata/load/logic and exact authored perspective prerequisites permit; missing authored input/property/interface completes the report without renderer creation. The reported default-state logic work is exactly one update for every sorted LogicEngine; the renderer performs exactly one additional update only on the selected interface's owning engine to apply the authorized authored perspective before the frame. Tests pin each engine's default call count, the owning engine's total of two, every other engine's total of one, and the aggregate total. The classifier uses typed facts and never parses console text.

When the strictly defined successful readback classifies clear or visible, the app-private WIC encoder writes the exact bounded PNG bytes only to the inherited parent-owned frame staging handle and flushes it; native never writes a frame pathname. The native report records `frameStagingState=written` only after same-handle byte comparison succeeds, otherwise `write_failed`. Every pre-readback or `undetermined_after_readback` result records no frame and leaves the inherited frame handle empty. After the applicable process-completion proof Python reads both original retained parent handles, validates native JSON, strictly parses PNG signature/chunks/zlib/filter rows as 8-bit noninterlaced RGBA, requires 480 x 270 and the native statistics, computes the decoded top-left RGBA SHA-256, and only then publishes `first-frame.png` followed by `ramses-probe-native.json` by held-handle no-replace rename. It retains both final handles through final report/run completion. A successful readback plus frame staging/decoding/publication failure is execution rejection with no accepted lane or final policy publication. Tests replace/unlink staging directory entries before child write, after child exit, and immediately before parent publication and prove acceptance remains bound to the retained identities.

`ProbeSceneSession` teardown is RAII, idempotent, state-aware, and part of bounded pre-publication finalization. If Ready was reached, request exact rollback to `RendererSceneState::Available`, call checked `RendererSceneControl::flush()`, and observe it. Reassign the scene to `getDisplayFramebuffer(display)`, clear the offscreen assignment, and flush before checked `scene.unpublish()` plus publish-derived unavailable observation; no nonexistent direct unmap API is invented. Issue checked offscreen-buffer destruction, call checked `RamsesRenderer::flush()`, and pump until `offscreenBufferDestroyed` matches exact display/buffer/result; then issue checked display destruction, call checked `RamsesRenderer::flush()`, and pump until `displayDestroyed` matches exact display/result. Every teardown pump requires exactly one checked `RamsesRenderer::doOneLoop()` followed by checked `RamsesRenderer::dispatchEvents()` and `RendererSceneControl::dispatchEvents()` on every iteration; any false return is an immediate teardown failure with no lane acceptance. Both callbacks must arrive by +58.400. While the renderer and client are still alive, call checked `RamsesFramework::disconnect()` and require success by +58.550. Only while disconnected may native call checked `destroyRenderer()`, then checked `destroyClient()`, then invoke the void framework reset and require that call to return by +58.700, matching installed `RamsesFramework.h`'s may-not-destroy-while-connected contract. Any negative, missing, delayed, wrong-ID, or failed callback; false teardown loop/dispatch; rejected rollback/unpublish/scene-control/renderer flush; failed/late disconnect; failed/late renderer or client destruction; reset overrun; or abnormal termination during reset is protected diagnostic teardown evidence and prevents lane acceptance plus final policy publication. Write and flush the inherited native report by +59.000 only after every synchronous destruction succeeds and reset returns. Tests cover every acquisition boundary, rollback/unpublish/flush rejection, independent false-return injection for `doOneLoop()` and each teardown dispatch API at every teardown wait, missing/delayed/wrong-ID/failed offscreen and display destruction callbacks, repeated cleanup, disconnect-before-destroy call order, each destroy false result/overrun, failed disconnect, reset overrun/abnormal termination, and exception/early-return paths; there is no invented Boolean reset result.

Add Windows-only `sgfx_ramses_probe` now, linked to `sgfx::cine`, nlohmann_json::nlohmann_json, `windowscodecs`, and `ole32`. Reuse existing build-local Ramses/SDL copy helpers. It accepts only Task 2's grammar, calls the orchestrator once, uses WIC only for R0 frame staging, and writes the report only through Task 2's inherited-handle serializer. Do not copy it into `cpp/bin`; package staging is Task 10. Non-Windows builds leave this optional target unavailable while keeping the shared library/preview buildable.

- [ ] **Step 6: Run native synthetic, preview, and Python C0 preview regressions**

Run:

~~~powershell
cmake --build build\cine-c0 --config RelWithDebInfo --target sgfx_cine_ramses_probe_lifecycle_test sgfx_cine_ramses_probe_synthetic_test sgfx_cine_ramses_preview_contract_test sgfx_ramses_probe
ctest --test-dir build\cine-c0 -C RelWithDebInfo -R "sgfx_cine_ramses_(probe_lifecycle|probe_synthetic|preview_contract)" --output-on-failure
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 4
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 4 -Module unittest -ModuleArguments @("tests.test_qt_quick_preview", "-v")
~~~

Expected: native and Python commands have complete passing footers, with only the existing environment-gated real-scene skip when no explicit scene is supplied.

- [ ] **Step 7: Review and commit the authored-frame probe**

Inspect the diff for a new renderer, direct camera setters, a perspective fallback, infinite wait, more than one frame, or changed C0 defaults. Commit:

~~~powershell
git add cpp/src/ramses_preview_internal.h cpp/src/ramses_preview.cpp cpp/src/ramses_probe.cpp cpp/apps/ramses_probe_cli.cpp cpp/apps/ramses_probe_png_wic.h cpp/apps/ramses_probe_png_wic.cpp cpp/tests/ramses_probe_lifecycle_test.cpp cpp/tests/ramses_probe_synthetic_test.cpp cpp/tests/ramses_preview_contract_test.cpp cpp/CMakeLists.txt tests/test_qt_quick_preview.py
git commit -m "feat(ramses): add authored R0 frame evidence"
~~~

---

### Task 5: Add the shared Ramses runtime lease without changing preview behavior

**Files:**
- Create: sg_preflight/ramses_runtime_lease.py
- Modify: sg_preflight/desktop/preview_coordinator.py
- Create: tests/test_ramses_runtime_lease.py
- Modify: tests/test_qt_quick_preview.py

**Interfaces:**
- Consumes: the existing preview helper lifetime.
- Produces: a dependency-free `RamsesRuntimeLease` shared later by preview and R0; no R0 input, process, report, or policy code yet.

- [ ] **Step 1: Write failing lease and unchanged-preview tests**

Lease tests use real processes and cover `WAIT_OBJECT_0`, `WAIT_ABANDONED`, `WAIT_TIMEOUT`, and `WAIT_FAILED`, same-thread acquire/release, wrong-thread release rejection, repeated acquire/release, exception, preemption, and post-release recovery. A holder prevents preview acquisition. Busy preview produces no native cache/output and returns the existing honest unavailable/fallback result; the next preview succeeds after release. Snapshot the existing preview result/output contract and require byte-equivalent behavior when the lease is uncontended.

- [ ] **Step 2: Run the lease tests and verify RED**

Run:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 5
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 5 -Module unittest -ModuleArguments @("tests.test_ramses_runtime_lease", "tests.test_qt_quick_preview", "-v")
~~~

Expected: the new lease module/behavior is RED while the captured preview baseline remains independently green.

- [ ] **Step 3: Implement the lease and wrap only the existing preview helper lifetime**

Implement `RamsesRuntimeLease` as a named per-user-session Win32 mutex (`Local\\SGFX.RamsesRuntime.v1`) with a small POSIX advisory-lock fallback for development tests. Acquisition is nonblocking. Handle `WAIT_OBJECT_0`, `WAIT_ABANDONED`, `WAIT_TIMEOUT`, and `WAIT_FAILED` explicitly; treat abandoned ownership as acquired-with-recovery evidence, never release unless the current thread owns it, and close exactly once. Neither lock name nor native handle enters UI or report data. Acquire immediately before existing preview helper launch and release only after that helper and its pipe handling finish. Do not change preview cache, source validation, fallback, timeout, result shape, or UI wording.

- [ ] **Step 4: Run GREEN, review, and commit the lease slice**

Refresh Task 5 once after the final source change, then run the complete lease/preview set and the exact existing four-pack isolation selector. Inspect for any R0 runner/report code or changed preview semantics, then commit only the four named files:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 5
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 5 -Module unittest -ModuleArguments @("tests.test_ramses_runtime_lease", "tests.test_qt_quick_preview", "-v")
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 5 -Module unittest -ModuleArguments @("tests.test_qa_actions.TestQaActions.test_execute_sgfx_preflight_calls_only_the_four_pack_service", "-v")
git add sg_preflight/ramses_runtime_lease.py sg_preflight/desktop/preview_coordinator.py tests/test_ramses_runtime_lease.py tests/test_qt_quick_preview.py
git commit -m "feat(ramses): serialize native helper access"
~~~

---

### Task 6: Resolve immutable inputs and source provenance

**Files:**
- Create: sg_preflight/ramses_probe_adapter.py
- Create: tests/test_ramses_probe_adapter.py
- Modify: sg_preflight/bundle_manifest.py
- Modify: tests/test_bundle_manifest.py

**Interfaces:**
- Produces immutable `RamsesProbeInputs`, held `VerifiedPathHandle` identities, typed before-snapshots, safe `RamsesProbeUnavailable`/`RamsesProbeRejected`, and `resolve_ramses_probe_inputs()`; no child launch or final-policy mapping.

- [ ] **Step 1: Write failing product/dev-lane, bounds, and path-race tests**

Cover canonical `RunProfile` resolution to the final identity of `<project_root>/export/exported.ramses`; a supplied real-smoke scene must have the same final volume/file ID and digest as that canonical export. An arbitrary override is accepted only in an explicit synthetic/development test lane and can never satisfy the real-input acceptance gate. Cover optional explicit perspective/id pairing; absent perspective is valid, explicitly missing/unreadable is unavailable, and malformed/nonfinite/oversized/deep/over-wide JSON is invalid before launch.

Cover fixed helper name, exact runtime set, inactive schema-2 fixture/Release receipt provenance, each independently mutated file, missing/extra/contradictory runtime, source/output/helper overlaps, UNC/mapped-remote roots, device namespaces, ADS, reserved/trailing-dot components, component reparse points, non-regular files, hardlinks, identity replacement/restoration, output above 32 MiB, and poisoned caller environment. Add race tests for transient add/remove or replacement in every source/runtime/output parent component, a newly added ADS between validation and consumption, writable mapping/replacement attempts, and runtime-namespace changes while the Job is active. Task 7 owns generated-leaf and inherited-output publication races. Test every 256 KiB request/receipt, 1 MiB perspective, depth/member/key/string, and 4 MiB future contained-Git command-output boundary.

- [ ] **Step 2: Run adapter tests RED**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 6
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 6 -Module unittest -ModuleArguments @("tests.test_ramses_probe_adapter", "tests.test_bundle_manifest", "-v")
~~~

Require assertion-level RED beyond import failure.

- [ ] **Step 3: Implement immutable resolution and exact trust lanes**

Define schema/probe constants and immutable `RamsesRuntimeFile`/`RamsesProbeInputs` records. `RamsesProbeInputs` contains exactly lane (`product` or `receipt`), profile/scenario IDs, generation, protected run token, scene and optional perspective held identities, optional perspective ID, fixed helper held identity, expected helper/runtime/schema digests and sizes, helper provenance, protected output parent, source roots, helper runtime root, the four parent-generated absolute QPC facts, optional product-output transport handles, and path-free source fingerprint. It contains no command string or caller environment. Public verifier parameters cannot supply QPC or handle values.

In `bundle_manifest.py`, add the immutable typed schema-2 model and strict `load_bundle_manifest_v2()` now so product-lane resolution has a real trust boundary. Its input contract is the final Task 10/11 schema-2 shape: at most 256 KiB; duplicate/nonfinite/depth rejection; exact keys/types/list limits; bool-vs-int rejection; exact preview/R0 helper identities; exact runtime-pair ID, native-build receipt/provenance digests, runtime filenames/digests, and three named schema version/digest entries; no path or arbitrary extension fields. Keep all active builder, frozen entry, benchmark, verifier, and package expectations at schema 1 in this task. Add only synthetic schema-2 fixtures and loader tests; do not dispatch or authenticate a live schema-2 bundle yet. `load_bundle_manifest_v1_historical()` stays separate and no dispatcher guesses a schema.

Normal product resolution accepts only the fixed packaged helper and `load_bundle_manifest_v2()` result; it remains unreachable from the current schema-1 product until Task 11 activates all producers/consumers together. The receipt lane accepts only the final runtime-pair receipt with exact keys `schemaVersion`, `pairId`, `sourceCommit`, `configuration`, `nativeBuildReceiptSha256`, `nativeBuildProvenanceSha256`, `targets`, `helpers`, `runtimeFiles`, `schemas`, `totalBytes`, and `completedAtUtc`. Values require schema 1, current clean 40-lowercase-hex commit, `Release`, one 32-lowercase-hex pair ID, both fixed lowercase build/provenance digests, exactly both helper target names, exact fixed role/name/non-Boolean-size/lowercase-digest entries for two helpers at `bin/sgfx_cine_ramses_preview_cli.exe` and `bin/sgfx_ramses_probe.exe`, four runtime DLL entries under the same `bin/`, and three separately versioned native/final/evidence schemas under `schemas/`, canonical UTC completion, and `totalBytes` equal to the checked sum of those nine payload sizes. Unknown keys, array reordering, duplicate role/name, path outside those two exact directories, or size/digest disagreement fail. Generate `run_token = secrets.token_hex(16)`, accept a caller-owned unsigned generation, and compute Task 1's exact source fingerprint from only passed source files.

The protected runner request has exact keys `schemaVersion`, `lane`, `profileId`, `scenarioId`, `generation`, `scene`, `perspective`, `perspectiveId`, `helperReceipt`, `outputParent`, `parentStartQpc`, `qpcFrequency`, `nativeWorkDeadlineQpc`, `parentDeadlineQpc`, and `productOutputHandles`. Schema is 1; `lane` is exactly `receipt` or `product`; product requires null `helperReceipt`, canonical scene resolution, and `productOutputHandles` exactly `{nativeReport,firstFrame,stdoutLog,stderrLog}` with four distinct nonzero inherited `uintptr` values generated by the source-verifier parent. Receipt requires the protected receipt and null `productOutputHandles`. Final-report and isolated-evidence handles are source-only, non-inherited, and are created only after direct exit capture, drain join, thread/process/pipe closure, whole-Job active-zero, Job closure, lease release, and the authoritative post-process facts. The QPC values are parent-generated, overflow-safe, and exact under Global Constraints; adapter, host, runner, and native require them unchanged. Perspective/path and ID are both strings or both null. Paths remain protected and never echo. No public request field can select an executable, backend, dimensions, timeout, QPC value, handle, environment, report name, or arbitrary output filename.

Protected JSON uses duplicate-key and nonfinite rejection, depth/member/string limits, exact keys/types, bool-as-int rejection, and a 256 KiB byte cap before decode. A completely absent optional runtime is unavailable; any present-but-incomplete/corrupt/contradictory runtime is rejected and cannot affect independently valid preview evidence.

- [ ] **Step 4: Implement held-handle path/provenance guards**

Reject unsafe syntax before canonicalization. Open and retain every existing intermediate directory component with `FILE_LIST_DIRECTORY | FILE_READ_ATTRIBUTES`, `FILE_FLAG_OPEN_REPARSE_POINT | FILE_FLAG_BACKUP_SEMANTICS`, and `FILE_SHARE_READ` only. Open every immutable input with `GENERIC_READ | FILE_READ_ATTRIBUTES`, `FILE_FLAG_OPEN_REPARSE_POINT`, and `FILE_SHARE_READ` only, then hash through that held handle. Open validated output components with only the read/write/delete rights needed for publication and no caller-visible inheritance. Hold source/runtime/output-parent/root handles through acceptance and every runtime component through child completion; retain volume/file IDs, enumerate streams before and after, and revalidate identities/digests through the same handles before launch and after exit. Reject `GetDriveTypeW == DRIVE_REMOTE`, UNC, any named stream, link, changed identity, parent-entry change, or runtime-namespace mutation. Create the unique empty attempt directory only after lease acquisition and hold its handle. Never recursively delete a rejected tree.

Task 6 records only held source identities and hashes. Exact Git HEAD/blob/fresh-index/filesystem proof is deliberately deferred to Task 7 so it uses the same suspended, allowlisted-handle, Job-contained process boundary. Non-Git input, unavailable trusted Git, or failed contained Git is exactly `exact_hash_only` with a typed reason.

- [ ] **Step 5: Run GREEN, review, and commit input provenance**

Refresh the exact Task 6 manifest, then run both adapter/profile-resolution and inert bundle-manifest-v2 loader suites. Review for arbitrary command/process/policy/UI code and prove every active product/package expectation is still schema 1, then commit:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 6
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 6 -Module unittest -ModuleArguments @("tests.test_ramses_probe_adapter", "tests.test_bundle_manifest", "-v")
git add sg_preflight/ramses_probe_adapter.py sg_preflight/bundle_manifest.py tests/test_ramses_probe_adapter.py tests/test_bundle_manifest.py
git commit -m "feat(ramses): validate R0 input provenance"
~~~

---

### Task 7: Own the bounded Windows process tree and verifier-only runner

**Files:**
- Create: sg_preflight/ramses_probe_process.py
- Create: sg_preflight/ramses_probe_publication.py
- Create: sg_preflight/ramses_probe_runner.py
- Modify: sg_preflight/ramses_probe_adapter.py
- Create: tests/test_ramses_probe_process.py
- Create: tests/test_ramses_probe_publication.py
- Create: tests/test_ramses_probe_runner.py
- Modify: tests/test_ramses_probe_adapter.py

**Interfaces:**
- Consumes Task 6's held immutable inputs and Task 5's lease.
- Produces an internal `ValidatedNativePayload` containing strictly validated native facts plus retained artifact/provenance handles, and promotes it to source-side `AcceptedNativeProbe` only after the direct process signals, exit is captured, every source-owned drain joins, thread/process/pipe handles close, the source-owned containing Job reaches active-zero, the Job closes, the lease releases, and authoritative after-source/runtime/Git facts pass in that order. The product host may validate only its native/frame/log transport while it remains inside the outer Job. Only the outer source verifier constructs policy/evidence and accepts either lane. Also produces a shared secure publisher/lock boundary and path-free process facts; no policy findings.

- [ ] **Step 1: Write failing suspended-launch, Job, stream, timeout, and runner tests**

Use real short-lived fixtures to prove: the primary thread cannot execute before Job assignment; assignment failure terminates the suspended process; immediate descendants remain in the Job; breakaway is denied; one parent QPC origin includes injected request-serialization and pre-resume delay; timeout calls `TerminateJobObject`; and lease release occurs only after direct signal, exit capture, drain join, thread/process/pipe closure, exact Job active-zero, and Job closure. Prove `ActiveProcesses` remains one while a signaled direct-process handle is retained and can become zero only after it closes. The dedicated source-verifier/lease-owner worker returns timeout status to its caller after two reap-only seconds but keeps Job, drains, output identities, and mutex while a descendant remains active; a second lease acquisition must fail until `ActiveProcesses == 0`, Job closure, and lease release. Cover `TerminateJobObject` and accounting-query failure without premature release. Add the product topology explicitly: an outer host and its native child share the one source-owned Job; the host waits only for the exact inner process handle plus its own inner drains, never for whole-Job active-zero while it is itself counted; it validates only native/frame/log transport and exits. Afterward only the source worker captures outer exit, joins outer drains, closes outer direct resources, observes whole-Job active-zero, closes the Job, releases the lease, captures authoritative after-source/runtime/Git facts, creates source-only final/evidence staging, and promotes or rejects the lane. A descendant that outlives either direct process blocks source acceptance, final/evidence construction, and second-lease acquisition.

The parent securely creates and pins two unpredictable empty native staging leaves, retains non-inheritable originals, duplicates only child-writable inheritable handles, and gives a native child exactly those two plus NUL/stdout/stderr. Git receives exactly NUL/stdout/stderr and can never inherit an artifact handle. Sentinel tests prove each process sees only its own list. After child exit, replace/unlink/hardlink/reparse each staging directory entry and insert each fixed final name before publication; validation/publication must still use only the original parent identities and never modify the inserted objects. Cover exact native/final caps, 256 KiB stream retain-then-discard behavior, nonzero exit, crash, partial/oversized JSON, strict PNG decode, orientation/channel mismatch, extra outputs, frame contradictions, echo/QPC mismatch, stale generation/replay, runtime namespace races, and path-free statuses. Add contained trusted-Git success/timeout/overflow/untrusted binary/config/credential/fsmonitor/dirty-clean/mutation cases and secure publisher directory/leaf/current/completion/pruner interleavings.

- [ ] **Step 2: Run process/publication/runner tests RED**

Keep all three new test modules importable with test-local sentinels or interfaces; a missing production module/import is not accepted as RED. Refresh the exact Task 7 manifest and require named assertion failures for suspended-before-first-instruction containment, journaled publication, and protected runner dispatch before any Task 7 production implementation:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 7
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 7 -Module unittest -ModuleArguments @("tests.test_ramses_probe_process", "tests.test_ramses_probe_publication", "tests.test_ramses_probe_runner", "-v")
~~~

Record the exact failing assertion names in the progress ledger.

- [ ] **Step 3: Implement create-suspended containment before first instruction**

Use standard-library `ctypes` Win32 calls, not `subprocess.Popen`, for every Windows R0/Git child. Through `SecurePublisher`, create two empty staging leaves inside the held attempt directory, retain non-inheritable parent handles, and create inheritable duplicates only for the native child. Open NUL and separate stdout/stderr pipes. Set `STARTUPINFOEXW.StartupInfo.dwFlags = STARTF_USESTDHANDLES`, exact std fields, `bInheritHandles=TRUE`, and an initialized `PROC_THREAD_ATTRIBUTE_HANDLE_LIST`. The native list is exactly NUL, stdout write, stderr write, native-report duplicate, and frame duplicate; the Git list is exactly NUL, stdout write, and stderr write. Only the first three are standard handles. Call `CreateProcessW` with trusted `lpApplicationName`, fixed argv including the already parent-generated QPC/handle values, `CREATE_SUSPENDED | CREATE_NO_WINDOW | BELOW_NORMAL_PRIORITY_CLASS | CREATE_UNICODE_ENVIRONMENT | EXTENDED_STARTUPINFO_PRESENT`, and no shell. Exact `lpCurrentDirectory` is the held app-local `pair-<id>/bin` for receipt native, held `_internal/cpp/bin` for product native, held executable directory for the frozen host, and the held containing worktree root for each Git snapshot; no other cwd is legal. A dedicated source worker acquires the named Ramses mutex for a probe lane and owns the query-capable `KILL_ON_JOB_CLOSE` Job, process/thread/pipes, outputs, and release. Assign while suspended, start drains, resume, and close parent copies of child-only duplicates. Any pre-resume failure terminates/reaps while the worker still owns the lease. Git uses its own short-lived Job without the Ramses lease or any artifact handle.

Environment schemas are per child, encoded as one double-NUL-terminated Unicode block sorted ordinal-ignore-case by key, and caller values are ignored. `SystemRoot` and `WINDIR` both equal the trusted Windows directory; `TEMP` and `TMP` both equal the held protected attempt-temp directory. Native `PATH` is exactly its authenticated app-local `bin` directory then trusted System32. Git `PATH` is exactly verified `git.exe` directory then trusted System32 and adds exactly `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=NUL`, `GIT_OPTIONAL_LOCKS=0`, `GIT_TERMINAL_PROMPT=0`, and `GIT_NO_REPLACE_OBJECTS=1`; a source-proof command may additionally receive only one internally generated protected `GIT_INDEX_FILE`, never a caller value. Task 11's frozen host `PATH` is exactly the held executable directory, its held `_internal` directory, then trusted System32 and adds exactly `SGFX_R0_PRODUCT_REQUEST=<validated capability path>`; its inner native child returns to the five-key native schema with packaged `_internal/cpp/bin`. No inherited environment key survives. Native startup enumerates exactly its loaded process image plus SDL3 and three named Ramses modules, copies path-free volume/file identities while each module is loaded, and rejects missing/duplicate names; Python compares those reported identities to its retained expected files and requires exact held-identity/digest equality. Tests place competing same-name DLLs in every ambient search location, mutate app-local entries before/after `CreateProcessW`, and reject missing/extra/wrong loaded identities. At the absolute parent deadline the source worker calls `TerminateJobObject`. On normal signal or termination it captures direct exit, joins drains, and closes thread/process/pipe handles first. It then retains and queries the Job until exact active-zero, closes the Job, and releases its mutex. If a descendant survives the two-second caller grace, the worker returns failure to the caller but keeps that same Job/mutex barrier until the ordered cleanup completes. Original artifact and immutable input/runtime handles remain separately retained for post-process validation/publication. Termination/query failure likewise retains the barrier and forbids acceptance; an external process abort triggers only emergency KILL_ON_CLOSE/abandoned-mutex recovery.

Implement exact source capture by reusing Task 1's complete Git boundary verbatim: the held real `mingw64\bin\git.exe`, its five effective Git-root DLLs, valid signer/thumbprint, protected-root ACL and full hardlink-alias closure, empty fixed environment including the internally bound `GIT_ATTR_SOURCE=<ExpectedSourceCommit>`, explicit held `--git-dir`/`--work-tree`, `CreateProcessW` quoting, three-handle allowlist, exact loaded-module/process-count proof, and independent queryable Job outside the Ramses contained-process clock. Every object-reading command is prefixed with `--no-replace-objects --no-optional-locks --attr-source=<ExpectedSourceCommit> -c core.fsmonitor=false -c core.hooksPath=NUL -c credential.helper= -c core.attributesFile=NUL -c core.autocrlf=true -c core.eol=native -c core.safecrlf=false`, starts from its own pre-create QPC origin, has a five-second deadline and 4 MiB retained cap while draining, and uses the exact signal -> exit capture -> drain join -> thread/process/pipe close -> Job-active-zero -> Job-close sequence. Reject replacement refs, grafts, alternates, shallow ambiguity, unsafe local-config include, repository-format ambiguity, caller index and every index skip-worktree/assume-valid/fsmonitor/sparse flag before accepting source facts.

For `ExpectedSourceCommit`, capture canonical full recursive HEAD records `{mode,type,objectId,size,path}` through fixed `ls-tree`/`cat-file` operations and resolve `filter`, `ident`, `working-tree-encoding`, `text`, and `eol` only from that expected commit. Reject the first three attributes when specified. Stream every retained worktree file's exact bytes through trusted `git hash-object --stdin --path=<path>` under the frozen expected-commit built-in clean/EOL policy and require the resulting repository-algorithm object ID to equal the HEAD record; every explicit `-text` security input must additionally equal the raw blob bytes. Construct a fresh protected index from that commit under the one internal `GIT_INDEX_FILE` and compare it to the held worktree. Independently enumerate every filesystem entry outside the exact authenticated generated-root exclusions and require equality with HEAD, including ignored files; `status`, ignore metadata, the caller index, name-only trees, symbolic branch names, and replace objects are never authority. Revalidate every Git/runtime/repository/worktree/object/file identity, HEAD record, resolved attribute, clean-object result, raw security-input blob, fresh-index result, filesystem inventory, and digest before and after the snapshot. Raw records remain protected; final evidence stores only the typed state and canonical snapshot digest. Timeout, overflow, mismatch, untrusted binary/runtime, poisoned config/index/object redirection, or ambiguity yields `exact_hash_only`, never clean. The after snapshot starts only after the contained process has completed the exact direct-resource/Job/lease cleanup sequence; it is skipped on timeout and cannot extend or be reported as the 60-second child timing.

- [ ] **Step 4: Validate native outputs and expose only the protected runner**

Completion proof is mode-specific. Receipt reads native bytes only after its source worker observes the native signal, captures exit, joins drains, closes thread/process/pipe handles, observes Job active-zero, closes the Job, and releases the lease in that order. A product host cannot wait for that same whole Job while it is counted inside it; it waits for the exact inner native process handle to signal, captures inner exit, joins inner drains, closes its inner thread/process/pipe handles, validates only provisional native/frame/log transport, emits status, and exits without claiming whole-Job cleanup. Only afterward does the outer source worker observe the host signal, capture outer exit, join outer drains, close outer thread/process/pipe handles, observe whole-Job active-zero, close the Job, release the lease, validate all original handles, recompute source/helper/runtime identities and after-Git facts, and create the source-only final/evidence leaves before product acceptance. In either mode, cap native bytes at 2 MiB, require exact schema/parity/phase/order/bounds/truncation/no-bool/exact echoes/QPC/loaded-module identities, and compare declared frame staging with the retained frame handle. Strictly decode written PNG bytes with standard-library `struct`/`zlib`: exact signature; legal IHDR/ancillary/contiguous-IDAT/IEND order; checked CRC for every bounded chunk; exactly one 480 x 270 8-bit color-type-6 noninterlaced IHDR; no unknown critical chunk; exactly one terminal IEND and no trailing bytes; complete zlib-stream consumption with no unused/trailing compressed bytes; valid filters; exact decompressed length; and top-left RGBA/statistics. Compute decoded RGBA SHA-256 in Python. The source worker then securely publishes frame first and native JSON second by retained handles and retains all accepted handles through Task 8/12 publication. Any frame-write failure after successful readback, mismatch, CRC/order/zlib failure, overflow, extra staged byte, descendant-lifetime disagreement, premature final/evidence byte, or race is rejection/quarantine, never accepted.

`SecurePublisher` is the sole generated-leaf/directory/current/completion/retention and recovery byte/state implementation for R0/C0. It implements Global Constraints' exact Windows and development-fallback semantics, bounded flushed state journals, blocked-retry detection, the evidence and pair mutexes, lock order, pinning, current replacement, graph revalidation, committed-publication finalization, journal-bound tombstone rename, and restartable identity-based deletion. Its generic APIs also own Task 10's fixed publication-root initializer, native-dependency snapshot, provenance-preimage/candidate publication, fresh `release_build`, `runtime_pair` stage, and authorization-bound recovery commands; `bundle_manifest_cli` wires only their exact target schemas, and all three Task 10 PowerShell publication/build/stage/recovery scripts remain argument-only parser-free held wrappers. It exposes no cleanup-by-path API. Tests inject a crash before/after every journal intent/create/result/root-rename/provenance-stage/write/rename/completion/pointer/tombstone/delete step, every fixed leaf/directory/current/completion collision, unknown orphan, and every C0-A versus R0-B publisher/pruner interleaving. They prove that only this Python implementation emits or interprets publication bytes/states, that wrappers cannot parse or mutate them, and require `clean_one_publication_orphan` only pre-commit, `finalize_one_committed_publication` only post-commit, and `resume_one_authorized_cleanup` only for an authenticated tombstone, including crash-after-delete-before-result recovery.

The runner accepts only `--request <protected regular file>` beneath the validated R0 root, caps/holds it at 256 KiB, resolves no arbitrary executable, and invokes the adapter/process boundary. Stdout is one path-free canonical JSON status; stderr is empty for expected states; exit codes are `0 accepted`, `10 unavailable`, `20 rejected`, `30 internal_failure`. PowerShell may call only this module, never the native helper.

- [ ] **Step 5: Run GREEN, review, and commit process ownership**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 7
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 7 -Module unittest -ModuleArguments @("tests.test_ramses_probe_process", "tests.test_ramses_probe_publication", "tests.test_ramses_probe_runner", "tests.test_ramses_probe_adapter", "tests.test_ramses_runtime_lease", "-v")
git add sg_preflight/ramses_probe_process.py sg_preflight/ramses_probe_publication.py sg_preflight/ramses_probe_runner.py sg_preflight/ramses_probe_adapter.py tests/test_ramses_probe_process.py tests/test_ramses_probe_publication.py tests/test_ramses_probe_runner.py tests/test_ramses_probe_adapter.py
git commit -m "feat(ramses): contain R0 probe processes"
~~~

---

### Task 8: Build the exact final policy report

**Files:**
- Create: sg_preflight/ramses_probe_report.py
- Create: tests/test_ramses_probe_report.py

**Interfaces:**
- Consumes one Task 7 source-promoted `AcceptedNativeProbe` within the same retained lane owner, after direct exit capture, joined drains, direct-resource closure, whole-Job active-zero, Job closure, lease release, held rehashes, and after-Git capture. Both receipt and product final-report bytes are authored exclusively by that outer source owner; the frozen product host cannot invoke this mapper or inherit its output handle.
- Produces the only schema-version-1 policy-bearing `RamsesProbeReport`; neither adapter nor evidence projection maps policy.

- [ ] **Step 1: Write failing exhaustive-policy, identity, artifact, and atomic-write tests**

Parameterize every row of Task 1's policy table, including both installed validation severities, every numeric/logic/truncation fact, all 14 approved stable outcomes, and `undetermined_after_` each of the 13 phases; each must match exact rule, safe message, severity, affected gates, recommendation ID, owner, confidence, tagged subject, evidence roles, and `SDK_NATIVE_OPPORTUNITY`. Unknown values fail closed. Prove rename/message/reordering stability and every same-object multi-discriminator separation. Feed shuffled over-cap inputs and require anomaly-first retention, uncapped totals, and all priority-0 truncation findings even when more than 100 same-severity detail findings sort earlier. Test artifact-role IDs, collision rejection, protected resolution, expiry, secure-frame referential integrity, and zero path leakage. Every final section honors its budget and a maximum/worst-escaped report stays below 4 MiB.

- [ ] **Step 2: Run report tests RED**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 8
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 8 -Module unittest -ModuleArguments @("tests.test_ramses_probe_report", "-v")
~~~

- [ ] **Step 3: Implement the one policy mapper and atomic final report**

Map only Task 1's table. Finding ID is SHA-256 of canonical JSON `[schemaVersion, probeVersion, sourceFingerprint, ruleId, taggedSubjectIdentity]`; exclude display name, message, refs, gates, action, owner, severity, and confidence. Build the complete set, retain every fixed priority-0 validation/numeric/logic truncation finding, fill the remaining 100 slots by Task 1's detail tuple, and record exact total/reported/truncated counts. Clear/visible policy is legal only when the retained validated payload has the strictly decoded frame handle; publication/acceptance still waits for its owning lane's source-side cleanup gate. The final environment records generation/scenario and Python-owned scene size/digests/helper/runtime/QPC facts; the one-use run token remains protected. `visible_frame_observed` is informational manual-review evidence, never approval.

Require source mutation PASS only when every exact before/after digest and every available bounded Git snapshot matches. On failure, quarantine only pinned generated identities through `SecurePublisher`; no recursive cleanup or pathname-only unlink. Publish `ramses-probe-report.json` with the same pinned run writer that still owns native/frame handles, enforce 4 MiB, strictly reparse, and retain its handle through run completion. Tests insert regular/hardlink/reparse objects at staging/final boundaries and prove none is truncated, replaced, or unlinked. No raw SDK message, console text, path, command, URL, or environment enters the final report.

- [ ] **Step 4: Run GREEN, review, and commit policy ownership**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 8
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 8 -Module unittest -ModuleArguments @("tests.test_ramses_probe_report", "tests.test_ramses_probe_adapter", "tests.test_ramses_probe_runner", "-v")
git add sg_preflight/ramses_probe_report.py tests/test_ramses_probe_report.py
git commit -m "feat(ramses): normalize standalone R0 findings"
~~~

---

### Task 9: Project accepted R0 findings into isolated standalone evidence

**Files:**
- Create: sg_preflight/data/ramses_r0/ramses_r0_evidence.schema.json
- Create: sg_preflight/ramses_probe_evidence.py
- Create: tests/test_ramses_probe_evidence.py
- Existing regression only: sg_preflight/checker_evidence.py
- Existing regression only: tests/test_checker_evidence.py

**Interfaces:**
- Consumes: one accepted schema-version-1 final `RamsesProbeReport`, whose findings were already built by Task 8.
- Produces: `project_ramses_probe_evidence(report) -> RamsesR0Evidence` schema 1 with `source_kind`, `checkers`, `subject_findings`, `top_subjects`, `manual_followups`, `summary_only`, and an opaque final-report artifact ID. It does not merge into or change shared checker evidence before the separate integration plan.

- [ ] **Step 1: Write failing exact-projection and sanitization tests**

Require the projection to preserve each final finding's ID, rule, provenance class, severity, stable subject fields, evidence refs, affected gates, next action, owner, and confidence without reinterpreting native facts. Keep at most 100 subject findings and five top subjects, ordered by severity, phase, rule, object type, and object ID. Expose only bounded display name, stable ID/type/property/phase, safe message, reviewed owner, and opaque artifact ID. Drop paths, URLs, commands, credentials, exceptions, console text, and raw SDK messages.

The isolated schema accepts only `SDK_NATIVE_OPPORTUNITY` for standalone findings, exact error/warning/info severities, verified/open confidence, the four reviewed owners, and Task 1's recommendation IDs. R0 may affect interface, visual, and review only. It never marks asset, variants, delivery, overall approval, or any containing gate passed. `visible_frame_observed` remains informational and never approval.

Add an exact existing checker-evidence fixture and assert its serialized version, keys, counts, paths, and merge behavior remain unchanged. A source scan rejects any Task 9 edit to `checker_evidence.py`; shared subject-evidence evolution belongs to the integration plan.

- [ ] **Step 2: Run isolated evidence tests and verify RED**

Run:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 9
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 9 -Module unittest -ModuleArguments @("tests.test_ramses_probe_evidence", "-v")
~~~

Expected: independent failure because the isolated schema/projector does not exist.

- [ ] **Step 3: Implement the bounded lossless projection**

Validate the final report with the Task 8 strict validator first. Copy policy-bearing fields exactly; do not map native facts a second time. Summarize checker counts by exact `ruleId`, retain explicit truncation counts, set `summary_only` truthfully, and use only the report's opaque artifact IDs. Unknown report/evidence versions or policy enums fail closed.

- [ ] **Step 4: Run evidence and unchanged-checker regressions GREEN**

Run:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 9
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 9 -Module unittest -ModuleArguments @("tests.test_ramses_probe_evidence", "tests.test_checker_evidence", "-v")
~~~

Expected: all tests pass; standalone subject evidence is exact and bounded, while existing file-backed checker evidence is byte-for-byte semantically unchanged.

- [ ] **Step 5: Review and commit isolated evidence**

Inspect for path leaks, duplicate policy mapping, invented BMW policy, gate-level pass inference, shared evidence migration, or external transport. Commit:

~~~powershell
git add sg_preflight/data/ramses_r0/ramses_r0_evidence.schema.json sg_preflight/ramses_probe_evidence.py tests/test_ramses_probe_evidence.py
git commit -m "feat(ramses): project standalone R0 evidence"
~~~

---

### Task 10: Define one atomic sanitized Release runtime pair and inactive bundle schema 2

**Files:**
- Modify: .gitignore
- Modify: cpp/CMakeLists.txt
- Create: scripts/run_sgfx_r0_native_build.ps1
- Create: scripts/test_sgfx_r0_native_build_trust.ps1
- Create: scripts/recover_sgfx_r0_publication.ps1
- Create: scripts/stage_ramses_runtime_pair.ps1
- Create: config/sgfx-r0-native-build-provenance.json
- Modify: sg_preflight/bundle_manifest.py
- Create: sg_preflight/bundle_manifest_cli.py
- Modify: tests/test_bundle_manifest.py
- Modify: tests/test_native_scaffold.py

**Interfaces:**
- Consumes Task 6's inert canonical `load_bundle_manifest_v2()` trust boundary.
- Produces one same-clean-HEAD Release preview/R0 runtime-pair publication contract plus an inactive schema-2 creator/merger and path-free helper/runtime/schema identities; no live package producer or C0 consumer changes yet.
- Produces the final byte-complete `sg_preflight/bundle_manifest.py` implementation needed by Task 11; its inactive state in this task is solely a call-path property. The Task 10 tooling/provenance reviews freeze those bytes, and Task 11 may not edit them while reusing that provenance digest.
- Applies the Global Constraints two-stage outer-Job protocol: the coordinator proves `coordinator_self_only_quiescence` with exact `ActiveProcesses == 1` and sole PID/self before receipt finalization, while only the outer launcher proves active-zero after coordinator exit. Native-build receipts contain the former proof and no post-exit zero claim.

- [ ] **Step 1: Write failing exact-runtime, strict-loader, and binary-privacy tests**

Where this task's fixture wording below says direct handles, active-zero, or retained handles through CMake/MSBuild/CTest active-zero, require the split protocol above: descendant direct resources close first; the live coordinator proves exact self-only quiescence; the outer launcher alone proves zero after coordinator exit. Fixture and receipt schemas must distinguish those two facts and reject substitution in either direction.

Cover preview-only, R0-only, both with identical shared DLL digests and pair IDs, neither, every missing/extra/mutated file, conflicting DLL/pair digests, partial pair publication, stale/malformed pointer or receipt, wrong helper/schema role/name/version/size/hash/total, 128 MiB boundary/overflow, link/junction/hardlink, duplicate/nonfinite/oversized/deep manifest JSON, bool-as-int, uppercase hash, unknown/missing key, and unavailable objects other than exactly `{"state":"unavailable"}`. Exercise pair publisher/pruner concurrency under the pair mutex and every fixed leaf/directory/current insertion race. Schema 1 is accepted only by an explicitly invoked historical reader, never automatic schema-2 dispatch. Prove the active builder/frozen entry/benchmark/C0 verifier still require schema 1 throughout this task.

Add PowerShell-only native-build trust fixtures for an absent fresh root, pre-existing/mutated generated project, exact externally reviewed tooling commit/digests, journaled protected dependency snapshot construction, provenance-receipt canonical bytes, CMake/Visual Studio/Windows SDK image/DLL identity and signer drift, missing/extra tool process, response-file/source/output escape, CMake fetch/update attempt, inherited environment, stdout/stderr/deadline caps, suspended Job assignment, and direct-handles-to-active-zero cleanup. Inject ambient SDL3/RmlUi/Freetype packages and registries and require the mandatory protected-dependency mode to ignore them; remove or redirect each of the four exact source overrides and require configure failure. Compile a capability fixture with `/experimental:deterministic` before every `/pathmap` and fail on D9007, unknown/ignored-option diagnostics, or a mapped payload retaining a real root. Require the tracked `pathMappingPolicy` to contain only static role/prefix/authority/order/overlap/trace-token rules and prove it contains no future project/build identity, tree digest, or actual trace instance. A real build must instantiate mappings for project, all four dependencies, build, Ramses install, Windows SDK, Visual Studio/toolchain, CMake install, and every additional compiler-visible root in the retained trace; its receipt must bind every per-build identity/before-after tree digest and trace-closure result. Reject duplicate identity/prefix, alias-equivalent or ambiguous overlap, caller-derived roots, wrong ordering, missing role, unclassified root, dynamic placeholder in tracked provenance, or static/dynamic disagreement. Require all two payload and eight test targets plus the complete eight-test CTest footer. Assert that raw CMake/project/response/log metadata stays protected and unpublished while OBJ/PDB/PE/DLL, staged payloads, and sanitized receipts contain no real root in raw, alternate-slash, ordinal-case, URI-encoded, JSON-escaped, UTF-8, or UTF-16LE form across every split boundary. Pin `offlineDependencyResolution=PASS` and `osEgressContainment=not_claimed`; no test may promote direct-socket denial from this contract. A same-shaped launcher on another clean commit must fail before dependency copy or process creation. Cover first-root initialization from absent state, planted root, pre-intent insertion, every header-bootstrap offset, durable create intent, create-before-result, result, reconciliation, completed rerun, and repeated recovery interruption. Require a digest-addressed authorization created only beneath the distinct protected external authority root to leave the bound `.superpowers/sdd` inventory unchanged; an internal authorization sibling, external-root replacement, wrong digest/name, or caller path fails before recovery mutation. Prove `SecurePublisher` is the only publication encoder/state machine, `bundle_manifest_cli` only validates the exact initialize/recover/build/build-and-stage arguments and orchestrates it, and none of the three PowerShell wrappers contains a journal, build-root, pair, pointer, retention, or recovery mutation codec. Exercise ordinary journals and recovery across native-dependency snapshot, pair, package-stage, release-build, R0, C0, pointer-temp, runtime-pair retention, R0 retention, and C0 retention crash points; for native dependency provenance inject crashes before/after dependency-root rename, deterministic preimage derivation, `provenance_preimage_bound` append/flush/reopen, candidate-stage creation, every partial write, flush/reopen, write result, rename intent, rename-before-result, final reopen, reconciliation, completion, and completed retry. For `build-native-release` and `build-and-stage-runtime-pair`, prove exact grammar/deadlines/write sets, outer-Job image registration, journal/mutex/root handles retained by the same Python process through CMake/MSBuild/CTest active-zero, no descendant in the stage phase, build-to-pair identity transfer without a caller path, wrapper argument forwarding, byte-exact accepted/unavailable/rejected/internal output envelopes and exit/null rules, and recovery at every build-root/pair/pointer boundary. Prove journal-bootstrap recovery, pre-commit cleanup, post-commit finalization including mandatory preimage/candidate roll-forward, restartable tombstone deletion including root-delete-before-result, and blocked unknown or unapproved orphans.

Require the candidate bundle to contain no `.ramses`, perspective JSON, generated frame/report/log, editable-install `*.dist-info/direct_url.json`, unlisted executable/DLL/schema, or unknown regular file in either dedicated Ramses subtree. Exact inventory equality applies to `_internal/cpp/bin` and `_internal/sg_preflight/data/ramses_r0`; the rest of the real PyInstaller bundle is not falsely constrained to a tiny allowlist. Enumerate every directory/file through held no-write/no-delete identities and scan every regular file plus every decoded archive member under Global Constraints' physical/logical caps. The nonexecuting reader accepts only the exact reviewed PyInstaller archive generation emitted by the authenticated builder: it validates the executable CArchive cookie/TOC/ranges/compression and terminal boundary, recursively validates embedded PYZ and ZIP/base-library containers, scans each member's complete decompressed bytes, and never imports or executes archived code. Reject malformed/truncated/overlapping/trailing/encrypted entries, unsupported compression, CRC/declared-size disagreement, duplicate or ordinal-ignore-case-colliding logical names, depth/member/expanded-byte overflow, decompression that exceeds its declared bound, or an unrecognized archive version. Bundle and logical paths are held-root-relative NFC strings with `/` separators; reject a name that is not already NFC, unsafe run-manifest syntax, invalid UTF-8, or any ordinal-ignore-case collision. Include every physical regular file beneath the bundle root—including `bundle-manifest.json`—and no directory entry in the physical tree digest. For each, hash complete held bytes, sort tuples by UTF-8 path bytes, serialize exactly `json.dumps(["sgfx-bundle-tree-v1", [[path,size,sha256], ...]], ensure_ascii=True, separators=(",", ":"))`, encode UTF-8, and SHA-256 those bytes as `bundleTreeSha256`. Re-enumerate/reconcile before each product execution and under the publication lock immediately before evidence completion; hold all executable/runtime/schema/file identities through use. Add/remove/replace/ADS, Unicode-normalization, case-collision, manifest-inclusion, tuple-serialization, empty-directory, CArchive/PYZ/ZIP range, and decompression races during enumeration, scan, launch, and final reconciliation fail closed. Forbidden content cannot hide behind an extension or compressed archive.

Inject dynamically derived home/worktree/source/build/dependency/install markers into `.exe`, `.dll`, `.json`, extensionless fixtures, ZIP/base-library members, CArchive members, and compressed PYZ members using case variants, both slash directions, URI encoding, JSON escaping, UTF-8, and UTF-16LE; place each longest encoded representation across every possible physical and decompressed streaming split offset, including UTF-16LE code-unit boundaries, and require every case to fail. Include the real preview helper, R0 helper, SDL3, three Ramses DLLs, schemas, manifest, unrelated bundle binary, PyInstaller executable CArchive, PYZ, and base-library ZIP in the scan. Add malformed cookies/TOCs, duplicate logical members, encrypted/overlapping/trailing records, compression bombs, 64 MiB member, 10,000-member, 2 GiB aggregate, and depth-three boundaries. Extension allowlists, raw-container-only scanning, and printable-text-only scanning are test failures.

- [ ] **Step 2: Run package tests RED**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 10
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 10 -Module unittest -ModuleArguments @("tests.test_bundle_manifest", "tests.test_native_scaffold", "-v")
& scripts\test_sgfx_r0_native_build_trust.ps1 -FixtureOnly
~~~

The Python tests and PowerShell fixture remain importable/parseable and must fail named assertions for absent native-build trust, provenance, and recovery behavior rather than fail because the test script cannot be loaded.

- [ ] **Step 3: Make Release builds reproducibly path-sanitized**

For MSVC, first compile a held capability fixture with `/experimental:deterministic` followed by every `/pathmap` option and reject D9007, any unknown/ignored-option diagnostic, or an unmapped marker in its OBJ/PDB/PE payload. Before every dependency subdirectory, instantiate the tracked static `pathMappingPolicy` in deterministic longest-real-root-first order for the current build: project, four protected dependencies, fresh build, Ramses install, Windows SDK, Visual Studio instance/toolchain, CMake install, and every extra compiler include/library root from the retained capability/process trace. Add linker `/PDBALTPATH:%_PDB%` for produced executables/DLLs. The isolated Python native-build coordinator derives every real root only from retained provenance/receipt identities; rejects duplicate, alias-equivalent, ambiguously overlapping, caller-supplied, or unclassified roots; records the actual ordered mapping instances, project/build identities, before/after tree digests, and trace-closure result only in the per-build receipt; and scans every produced OBJ/PDB/PE/DLL, staged artifact, and sanitized receipt for all raw, slash/case, URI, JSON, UTF-8, and UTF-16LE representations at every streaming split offset. Raw CMakeCache, generated project, response, compiler-ID, and configure/build log files remain protected path-bearing ephemeral control evidence; authenticate and digest them, but never stage, publish, or claim them path-free. Development tests remain `RelWithDebInfo`.

Add mandatory `SGFX_R0_PROTECTED_DEPENDENCIES` production handling to `cpp/CMakeLists.txt`: before dependency resolution it disables user/system package registries, sets `FETCHCONTENT_TRY_FIND_PACKAGE_MODE=NEVER` and fully disconnected mode, skips ambient `find_package(SDL3)`, `find_package(RmlUi)`, and `find_package(Freetype)`, and requires exact retained source overrides for `nlohmann_json`, `SDL3`, `freetype`, and `RmlUi`. It validates each populated source root against its receipt before adding that dependency. Ramses remains separately fixed to the held exact config root; no caller-selected package root is accepted. Implement Global Constraints' fresh protected configure/build contract in the isolated Python `SecurePublisher`/`bundle_manifest_cli` coordinator. `scripts/run_sgfx_r0_native_build.ps1` is a held argument-only wrapper with exactly two production modes and no parameter for an existing build directory, generator, tool path, dependency path, network mode, package fallback, journal, or output root. Its pre-binding `-InitializeNativeBuildProvenance` mode forwards the held externally reviewed tooling commit/digests, expected clean source commit, primary-Python provenance receipt digest, and native-dependency source-binding digest to the exact `initialize-native-provenance` grammar. That Python operation launches no CMake, compiler, project-generated code, package builder, native helper, or network client; it first completes or read-only validates `publication_journal_root`, then constructs the protected dependency snapshot and, after `root_renamed`, derives and binds the identity-complete canonical provenance preimage/candidate through the one `native_dependency_snapshot` transaction and explicit no-path recovery. The bound wrapper mode `-BuildReleasePairInputs -ExpectedSourceCommit <40hex> -ExpectedPrimaryPythonProvenanceReceiptSha256 <64hex> -ExpectedNativeBuildProvenanceSha256 <64hex>` forwards only to exact `bundle_manifest_cli build-native-release`; that same isolated Python process retains the `release_build` journal/mutex/build-root handles, configures one fresh nonce root, runs the two fixed payload and eight fixed test targets plus their exact Release CTests under the outer Job/audit contract, emits one protected build receipt with their ordered results/log digest and dynamic mapping/trace facts, reconciles/commits the root, and returns only canonical path-free receipt identity/digest output. `scripts/stage_ramses_runtime_pair.ps1` is a second argument-only wrapper whose production form requires exactly expected source commit, primary-Python receipt digest, and native-build-provenance digest and invokes only `bundle_manifest_cli build-and-stage-runtime-pair`. That one isolated process first performs a fresh `release_build`, retains its final root/receipt identities through the subsequent `runtime_pair` journal/mutex transaction, copies only the two helper payloads/four runtime DLLs/three schemas through `SecurePublisher`, and publishes the pair/current/retention state with no caller receipt or path and no descendant during staging. Both wrappers have fixture-only seams for precommit tests, never invoke `build/cine-c0` or a free-form CMake command, and contain no publication or build-root codec.

The build coordinators emit at most 4 KiB canonical UTF-8/no-BOM/one-LF JSON and empty stderr for expected states. `build-native-release` has exact key order `schemaVersion,state,sourceCommit,nativeBuildReceiptSha256`; `build-and-stage-runtime-pair` has exact key order `schemaVersion,state,sourceCommit,nativeBuildReceiptSha256,pairId,pairReceiptSha256`. Schema is 1; state/exit pairs are `accepted/0`, `unavailable/10`, `rejected/20`, and `internal_failure/30`; commit is exact lowercase 40-hex, accepted digests are lowercase 64-hex, accepted pair ID is lowercase 32-hex, and every unavailable/rejected/internal field after `sourceCommit` is null. The held PowerShell wrappers enforce only the outer stdout/stderr caps and allowed exit set, pass through bytes/exit unchanged, never parse a JSON field, and never use a returned value as a path or publication authority. The combined coordinator transfers the still-held build identities internally, and the subsequent independent `inspect-pair` is the only consumer-facing pair trust decision.

The tracked native-build provenance candidate is canonical UTF-8 without BOM plus one LF, schema 1, and has exact top-level keys `schemaVersion`, `toolingCommit`, `toolingFiles`, `cmake`, `visualStudio`, `windowsSdk`, `tools`, `ramses`, `dependencies`, `pathMappingPolicy`, and `processPolicy`. `toolingFiles` binds exact size/SHA-256 in this order for the native-build launcher, native-build trust test, publication-recovery wrapper, pair-stage script, `cpp/CMakeLists.txt`, `sg_preflight/ramses_probe_publication.py`, `sg_preflight/bundle_manifest.py`, and `sg_preflight/bundle_manifest_cli.py`. Every tool record is exact `{role,fileName,size,sha256,volumeSerialNumber,fileId,signerSubject,signerThumbprint}` and the sorted role set must equal the independently observed fixture trace; it includes at least CMake, MSBuild, `cl`, `link`, `lib`, `rc`, `mt`, `cvtres`, and `mspdbsrv`, and permits no wildcard directory/image. `dependencies` is exact role order nlohmann_json, SDL3, FreeType, RmlUi with `{role,origin,version,licenseSha256,fileCount,totalBytes,treeSha256,rootVolumeSerialNumber,rootFileId}`. `pathMappingPolicy` is exactly `{schemaVersion,baseRoles,orderingPolicy,overlapPolicy,traceRootPolicy}`. Its schema is 1; `baseRoles` is the fixed ordered array of `{role,logicalPrefix,identityAuthority}` records for project, the four dependencies, build, Ramses, Windows SDK, Visual Studio/toolchain, and CMake, where each authority is an exact per-build or immutable receipt-role reference rather than a live identity. `orderingPolicy` is exactly `longest_real_root_first`; `overlapPolicy` fixes duplicate/alias/ambiguous-overlap rejection plus declared-strict-ancestor handling; and `traceRootPolicy` fixes the authenticated ancestor roles, deterministic sanitized token derivation, and provenance-amendment requirement for an outside root. The tracked candidate contains no current project/build volume or file ID, project/build tree digest, actual future trace-root instance, or dynamic placeholder. `processPolicy` freezes generator/platform, empty-environment keys, image roles, output/deadline caps, `/experimental:deterministic`-then-`/pathmap` capability result, protected-dependency mode, four exact source-override roles, disabled package registries, `FETCHCONTENT_TRY_FIND_PACKAGE_MODE=NEVER`, `FETCHCONTENT_FULLY_DISCONNECTED=ON`, `offlineDependencyResolution=PASS`, and `osEgressContainment=not_claimed`. Unknown/reordered/missing policy roles, mapping-policy duplication/ambiguity, unclassifiable trace policy, dynamic build facts in provenance, or caller substitution fail before process creation. The plan makes no stronger direct-socket-denial claim.

Add `/cpp/runtime-pairs/` and `/build/ramses-r0-release/` to `.gitignore`; retain existing ignored `/cpp/bin/` only for the historical preview path until Task 11 switches consumers. Under the pair mutex and creation-journal contract, securely build/pin `cpp/runtime-pairs/pair-<32hex>.next` with one app-local `bin/` containing exactly `SDL3.dll`, the three named Ramses DLLs, `sgfx_cine_ramses_preview_cli.exe`, and `sgfx_ramses_probe.exe`; `schemas/` containing the three exact R0 schemas; and `pair-receipt.json`. No sibling runtime/helper directory or duplicate DLL is permitted. The receipt is at most 256 KiB and has exactly `schemaVersion`, `pairId`, `sourceCommit`, `configuration`, `nativeBuildReceiptSha256`, `nativeBuildProvenanceSha256`, `targets`, `helpers`, `runtimeFiles`, `schemas`, `totalBytes`, and `completedAtUtc`; schema is 1, configuration is `Release`, the two build digests bind the fresh protected build and tracked provenance, targets are the two fixed sorted names, and every helper/runtime/schema entry has the exact Task 6 role/name/non-Boolean-size/lowercase-digest shape and order with canonical names rooted at `bin/` or `schemas/`. `totalBytes` is the checked sum of exactly nine payload files—four runtime DLLs, two helpers, and three schemas—and excludes the receipt. Reject any cardinality mismatch before summation. Reject before rename unless `totalBytes <= 128 MiB - 256 KiB` and the complete pair including receipt is <=128 MiB. Copy every payload through `SecurePublisher`, rescan held source/staged bytes, write the receipt securely, and rename the pinned complete directory by handle with no replace. Maximum-shaped tests prove all nine leaves plus the receipt stay inside the cap; launch tests prove app-local DLL discovery and loaded-module identity without ambient search.

Publish `cpp/runtime-pairs/current.json` last while still holding the pair mutex. It is at most 4 KiB and has exactly `schemaVersion`, `pairId`, `sourceCommit`, `receiptRelativePath`, `receiptSha256`, `totalBytes`, and `completedAtUtc`; schema is exactly 1, `receiptRelativePath` is exactly `pair-<pairId>/pair-receipt.json`, IDs/commit/time/hash are strict, and non-Boolean `totalBytes` equals the authenticated complete-pair byte size including receipt. Pointer and receipt pair ID, commit, completion time, sizes, inventory, and digest must match exactly. Use the shared locked pointer replacer, then re-open/reconcile every field. A failure before verified replace leaves the prior pointer valid. Consumers reject partial/stale/oversize pairs and an older pair whose commit is not the required clean HEAD.

Retain at most four authenticated <=128 MiB pair directories, no noncurrent pair older than 14 days, and at most 512 MiB total. Under the same pair mutex, pin/revalidate current and the candidate receipt/tree immediately before mutation and always preserve current. Order only authenticated noncurrent receipts; unknown entry/link/digest/size/inventory/incomplete `.next` disagreement stops pruning. For each eligible pair, create and flush a `runtime_pair_retention` journal, flush its tombstone-rename intent, atomically no-replace rename the whole pair root to the journal-bound same-parent tombstone, and flush the observed result before deleting anything. Dispose only the tombstoned receipt-listed leaves, receipt, empty listed directories, and root through the restartable per-entry delete intent/result protocol. A crash blocks later pair publication/pruning until `resume_one_authorized_cleanup` completes that journal. Never recursively delete or rename/delete `current.json`.

- [ ] **Step 4: Implement the inactive schema-2 creator, inspector, and exact merger**

Complete the creator, merger, strict loader, inspector projection, publication hooks, and every active-call-path behavior now; “inactive” means only that Task 10's current builder and C0 consumers do not invoke them. No later edit to `sg_preflight/bundle_manifest.py` is permitted under the reviewed Task 10 provenance. Tests must prove Task 11 activation can occur solely by changing its listed builders/consumers.

Reuse Task 6's `load_bundle_manifest_v2()` without a second parser. Add an inactive `create_bundle_manifest_v2_candidate()` and pair merger plus `bundle_manifest_cli.py inspect-v2 --path <fixed manifest>`; the CLI exposes only canonical sanitized JSON and accepts no arbitrary code/command. Both inspectors emit canonical UTF-8 without BOM and one terminal LF, write empty stderr, and map states exactly to exits `accepted=0`, `unavailable=10`, `rejected=20`, and `internal_failure=30`; any extra stdout byte fails the caller.

`inspect-pair --pointer <path>` accepts only the exact `current.json` child of a validated local `runtime-pairs` root and performs the strict locked pointer/receipt/tree/size/digest reconciliation above. Its object has exactly `schemaVersion`, `state`, `code`, `pairId`, `sourceCommit`, `configuration`, `nativeBuildReceiptSha256`, `nativeBuildProvenanceSha256`, `totalBytes`, `completedAtUtc`, `helpers`, `runtimeFiles`, and `schemas`. Schema is 1. Accepted code is `ACCEPTED`; scalar identities and both build digests are strict/non-null; emitted `totalBytes` is exactly the pointer's authenticated complete-pair size including `pair-receipt.json`, while the validator separately proves the receipt's payload-only `totalBytes` equals the nine nested payload sizes. `helpers` is exactly two sorted `{role,name,size,sha256}` objects, `runtimeFiles` exactly four sorted `{name,size,sha256}` objects, and `schemas` exactly three sorted `{role,name,schemaVersion,size,sha256}` objects. Unavailable/rejected/internal codes are respectively `PAIR_UNAVAILABLE`, `PAIR_REJECTED`, and `INTERNAL_FAILURE`, with null identity/build scalar proof fields and empty arrays. Unknown keys, Boolean sizes, ordering, role/name/cardinality, either total, or pointer/receipt/build disagreement fail.

`inspect-v2 --path <fixed manifest>` accepts only the exact `bundle-manifest.json` child of a held local bundle root, calls the strict schema-2 loader, held-tree scanner, and runtime-pair reconciler, and emits exactly `schemaVersion`, `state`, `code`, `sgfxVersion`, `sourceCommit`, `pairId`, `nativeBuildReceiptSha256`, `nativeBuildProvenanceSha256`, `bundleManifestSha256`, `bundleTreeSha256`, `pythonVersion`, `pyside6Version`, `qtVersion`, `qmlContractVersion`, `qaHubSchemaVersion`, `uiCapabilityCount`, `surfaceDescriptorCount`, `controlCenterQmlPresent`, `productFontsLicensed`, `presentationModes`, `qmlImports`, `helpers`, `runtimeFiles`, and `schemas`. Schema is 1. Accepted code is `ACCEPTED`; fixed versions/counts/Booleans and both native-build digests are non-null and presentation modes plus `{module,plugin}` QML imports retain exact validated order. `helpers` is the sorted nonempty subset of preview/R0 `{role,name,size,sha256}` entries; `runtimeFiles` is exactly the four sorted entries whenever a helper exists; `schemas` is exactly the three sorted versioned entries iff R0 exists and otherwise empty. C0 acceptance requires preview; R0 acceptance requires both helpers and all schemas. Unavailable/rejected/internal codes are `MANIFEST_UNAVAILABLE`, `MANIFEST_REJECTED`, and `INTERNAL_FAILURE`, with null scalar proof fields and empty arrays. The R0 verifier's `product-manifest-inspection.json` is byte-for-byte this accepted output; C0 trusts the same typed bytes. No active package or C0 code calls the schema-2 creator/merger in Task 10. `load_bundle_manifest_v1_historical()` remains separate and cannot authenticate a current candidate.

The inactive package merger consumes only one authenticated pair pointer/receipt, requires identical pair IDs, native-build receipt/provenance digests, and shared DLL digests, stages one shared runtime plus included helpers into `_internal/cpp/bin`, includes the three schemas only in `_internal/sg_preflight/data/ramses_r0`, and publishes atomically. `ramses_r0_helper` names `native_report_schema_version`, `final_report_schema_version`, and `evidence_schema_version` separately; each schema entry has exact role/name/lowercase SHA-256. Shared runtime exists iff at least one helper exists. A present corrupt/partial optional pair aborts rather than degrading. No absolute path enters the manifest.

The schema-2 R0 addition is exactly:

~~~json
{
  "ramses_runtime": {
    "state": "included",
    "pair_id": "<32-lowercase-hex>",
    "native_build_receipt_sha256": "<64-lowercase-hex>",
    "native_build_provenance_sha256": "<64-lowercase-hex>",
    "ramses_version": "28.16.0",
    "files": [
      {"name": "SDL3.dll", "sha256": "<64-lowercase-hex>"},
      {"name": "ramses-shared-lib-headless.dll", "sha256": "<64-lowercase-hex>"},
      {"name": "ramses-shared-lib-renderer.dll", "sha256": "<64-lowercase-hex>"},
      {"name": "ramses-shared-lib.dll", "sha256": "<64-lowercase-hex>"}
    ]
  },
  "ramses_preview_helper": {
    "state": "included",
    "pair_id": "<32-lowercase-hex>",
    "file": {"name": "sgfx_cine_ramses_preview_cli.exe", "sha256": "<64-lowercase-hex>"}
  },
  "ramses_r0_helper": {
    "state": "included",
    "pair_id": "<32-lowercase-hex>",
    "probe_version": "0.1.0",
    "native_report_schema_version": 1,
    "final_report_schema_version": 1,
    "evidence_schema_version": 1,
    "ramses_version": "28.16.0",
    "backend": "opengl",
    "file": {"name": "sgfx_ramses_probe.exe", "sha256": "<64-lowercase-hex>"},
    "schemas": [
      {"role": "native_report", "name": "ramses_probe_native_report.schema.json", "sha256": "<64-lowercase-hex>"},
      {"role": "final_report", "name": "ramses_probe_report.schema.json", "sha256": "<64-lowercase-hex>"},
      {"role": "isolated_evidence", "name": "ramses_r0_evidence.schema.json", "sha256": "<64-lowercase-hex>"}
    ]
  }
}
~~~

Those three objects are part of the existing exact schema-2 top-level manifest, not a second adjacent file. Array order is the order shown. Any unavailable one of the three objects is exactly `{"state":"unavailable"}`.

- [ ] **Step 5: Run fixture GREEN, review, and commit the inactive pair/schema contract**

Run the complete fixtures, require independent Python/security and scope/release review of the exact native-build/staging/recovery tooling, and keep production provenance initialization disabled by the unbound sentinel:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 10
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 10 -Module unittest -ModuleArguments @("tests.test_bundle_manifest", "tests.test_native_scaffold", "-v")
& scripts\test_sgfx_r0_native_build_trust.ps1 -FixtureOnly
git add .gitignore cpp/CMakeLists.txt scripts/run_sgfx_r0_native_build.ps1 scripts/test_sgfx_r0_native_build_trust.ps1 scripts/recover_sgfx_r0_publication.ps1 scripts/stage_ramses_runtime_pair.ps1 sg_preflight/bundle_manifest.py sg_preflight/bundle_manifest_cli.py tests/test_bundle_manifest.py tests/test_native_scaffold.py
git diff --cached --check
git commit -m "build: establish native release trust"
~~~

- [ ] **Step 6: Initialize, independently review, and commit-bind native build provenance**

Require Step 5's clean tooling commit to contain every executable rule but no provenance receipt or protected dependency snapshot. Independent native/spec, Python/security/package, and scope/release reviewers retain that commit and exact sizes/SHA-256 for all eight `toolingFiles` outside the mutable worktree, and native/spec plus Python/security approve the separate digest-addressed four-source binding. Root invokes the held native-build launcher with those explicit values plus the primary-Python receipt and source-binding digests. The wrapper only validates/forwards those exact scalar bindings to Task 1's isolated `bundle_manifest_cli initialize-native-provenance` operation. That Python operation may inspect signed installed tools and copy held lawful dependency source bytes through the `native_dependency_snapshot` transaction and its final-root/preimage/recovery contract; it launches no CMake, compiler, project-generated code, package builder, native helper, or network client, and the wrapper contains no independent publication parser or writer:

~~~powershell
$heldNativeBuildPath = [IO.Path]::GetFullPath('scripts\run_sgfx_r0_native_build.ps1')
$nativeBuildStream = [IO.File]::Open($heldNativeBuildPath, [IO.FileMode]::Open, [IO.FileAccess]::Read, [IO.FileShare]::Read)
try {
    $sha = [Security.Cryptography.SHA256]::Create()
    try { $actualNativeBuildSha256 = ([BitConverter]::ToString($sha.ComputeHash($nativeBuildStream))).Replace('-', '').ToLowerInvariant() } finally { $sha.Dispose() }
    if ($actualNativeBuildSha256 -cne $reviewedNativeBuildLauncherSha256) { throw "Native-build launcher is not the reviewed byte stream" }
    & $heldNativeBuildPath -InitializeNativeBuildProvenance -ExpectedSourceCommit $task10ToolingCommit -ExpectedToolingCommit $task10ToolingCommit -ExpectedPrimaryPythonProvenanceReceiptSha256 $expectedPrimaryPythonProvenanceReceiptSha256 -ExpectedNativeDependencySourceBindingSha256 $reviewedNativeDependencySourceBindingSha256 -ExpectedNativeBuildLauncherSha256 $reviewedNativeBuildLauncherSha256 -ExpectedNativeBuildTrustTestSha256 $reviewedNativeBuildTrustTestSha256 -ExpectedPublicationRecoverySha256 $reviewedPublicationRecoverySha256 -ExpectedStagePairSha256 $reviewedStagePairSha256 -ExpectedCMakeListsSha256 $reviewedCMakeListsSha256 -ExpectedPublicationModuleSha256 $reviewedPublicationModuleSha256 -ExpectedBundleManifestSha256 $reviewedBundleManifestSha256 -ExpectedBundleManifestCliSha256 $reviewedBundleManifestCliSha256
    if ($LASTEXITCODE -ne 0) { throw "Native-build provenance initialization failed" }
} finally {
    $nativeBuildStream.Dispose()
}
~~~

On the first successful invocation, the mode produces only the completed fixed publication-root-initialization journal/root, one completed `native_dependency_snapshot` publication journal containing `provenance_preimage_bound`, protected `.superpowers/sdd/r0-native-dependencies-v1`, and the final untracked `config/sgfx-r0-native-build-provenance.json`; its journal-owned staging candidate is absent. A completed rerun validates the complete listed artifact set and returns only the existing candidate digest without a write. Independent reviewers verify both publication transactions/recovery states, the single Python ownership boundary, every source/license/tree/tool/signature/static-path-mapping-policy/process-policy field, absence of per-build dynamic mapping facts, and the exact allowed write set. With zero Critical/Important findings, stage only the canonical receipt, inspect the full diff, and commit:

~~~powershell
git add config/sgfx-r0-native-build-provenance.json
git diff --cached --check
git commit -m "build: bind native build provenance"
git show --check --stat --oneline HEAD
~~~

Record the distinct tooling commit, provenance-binding commit, protected dependency source-binding digest, primary-Python receipt digest, protected dependency tree digests, publication-root-initialization journal digest, `native_dependency_snapshot` journal digest, and tracked provenance SHA-256. A finding requires a new tooling commit and complete repeat; a root-initializer, journal bootstrap, snapshot, or candidate orphan follows the explicit publication-recovery contract.

- [ ] **Step 7: Stage and authenticate the first real pair from the clean Task 10 binding commit**

Run only after Step 6 committed the reviewed provenance and Task 1's complete expected-HEAD/blob/fresh-index/filesystem proof passes. Root Lexus copies the exact 40-lowercase-hex provenance-binding commit into `$task10Commit`, its independently reviewed 64-hex provenance digest into `$nativeBuildProvenanceSha256`, and the already bound primary-Python receipt digest into `$expectedPrimaryPythonProvenanceReceiptSha256`; this step never derives or substitutes a binding:

~~~powershell
if ($task10Commit -cnotmatch '^[0-9a-f]{40}$') { throw "Task 10 commit binding is invalid" }
if ($nativeBuildProvenanceSha256 -cnotmatch '^[0-9a-f]{64}$') { throw "Native-build provenance binding is invalid" }
if ($expectedPrimaryPythonProvenanceReceiptSha256 -cnotmatch '^[0-9a-f]{64}$') { throw "Primary-Python provenance binding is invalid" }
& scripts\stage_ramses_runtime_pair.ps1 -ExpectedSourceCommit $task10Commit -ExpectedPrimaryPythonProvenanceReceiptSha256 $expectedPrimaryPythonProvenanceReceiptSha256 -ExpectedNativeBuildProvenanceSha256 $nativeBuildProvenanceSha256
git check-ignore -v cpp/runtime-pairs/current.json
& scripts\run_sgfx_isolated_python.ps1 -ExpectedSourceCommit $task10Commit -Module sg_preflight.bundle_manifest_cli -ModuleArguments @("inspect-pair", "--pointer", "cpp/runtime-pairs/current.json")
~~~

Require a same-HEAD fresh-configure Release build receipt whose project/source and build-root identities, before/after tree digests, actual ordered mapping instances, and trace-closure result instantiate the tracked static `pathMappingPolicy`; require exact native-build provenance/tool/dependency/process audit, pair inventory/digest reconciliation, journal completion, and all privacy variants clean. Prove the build never opened `build/cine-c0`. This ignored pair is build evidence, not a tracked task change. If production staging reveals a source defect, fix it in a new tracked commit, regain a clean status, and repeat this step with a new fresh root; if any `toolingFiles` byte or tool/dependency/process/mapping policy changes, repeat Steps 5-7 and create a new reviewed provenance-binding commit first. Never authenticate a pair against a dirty tree or reuse a configured build.

---

### Task 11: Migrate C0 consumers without changing the default or historical evidence

**Files:**
- Modify: scripts/build_sgfx_exe.py
- Modify: sg_preflight/exe_entry.py
- Modify: scripts/verify_control_center_c0.ps1
- Modify: scripts/benchmark_qt_quick.py
- Modify: tests/test_qt_quick_benchmark.py
- Modify: tests/test_cli.py
- Modify: tests/test_bundle_manifest.py

**Interfaces:**
- Consumes only Task 10's canonical schema-2 loader/inspection output and authenticated atomic runtime pair.
- Consumes Task 10's byte-final schema-2 creator/merger from `sg_preflight/bundle_manifest.py` without editing that provenance-bound module.
- Preserves C0 verification schema, Clean no-argument default, eight capabilities, 19 surfaces, preview behavior, and original C0 evidence directory.

- [ ] **Step 1: Write failing C0 schema-2, isolated-output, and default tests**

Require the package builder and every C0 Python consumer to use `load_bundle_manifest_v2()` and PowerShell to use only `bundle_manifest_cli inspect-v2`; direct `json.loads`/`ConvertFrom-Json` trust decisions are forbidden. Add required `verify_control_center_c0.ps1 -ExpectedPrimaryPythonProvenanceReceiptSha256 <64hex>` in both historical and isolated modes, plus isolated `-OutputRoot <validated workspace child> -RunId <32hex> -RelatedR0RunId <32hex> -ExpectedSourceCommit <40hex>`. The primary digest is never read from the environment by this verifier and is passed explicitly to every isolated Python edge. Preserve the historical current output root only when `OutputRoot`, `RunId`, `RelatedR0RunId`, and `ExpectedSourceCommit` are all omitted; those four remain an all-or-none set independent of the always-required primary digest. Isolated mode uses Task 7's trusted contained Git lane and requires the complete expected-commit HEAD-record/blob/fresh-index/filesystem proof before work, after every build, and under the evidence-publication lock immediately before completion; ignored authenticated build/evidence roots are only the exact exclusions named by that proof, never inferred from ignore metadata. The terminal directory is exactly `c0-<RunId>`, and the related R0 ID is accepted only after strict `current.json`/completion/manifest reconciliation proves the named completed R0 run on that exact commit. Tests reject missing, environment-only, duplicate-source, uppercase, wrong, or unforwarded primary bindings in both modes.

Build first as sibling `c0-<RunId>.next` through the shared `SecurePublisher`; C0 never path-opens a generated directory or leaf for trust or writes JSON/Markdown directly by path. Under the per-workspace evidence mutex, revalidate and pin the related R0 completion, manifest, exact tree, and commit, then publish the C0 JSON, Markdown, and exact-tree `run-manifest.json`, rename the pinned directory with no replace, exclusively publish `out/ramses-r0/completions/c0-<RunId>.json`, and atomically publish digest-bound `out/ramses-r0/c0-current.json` last. Retain every generated leaf/directory and related-R0 identity until the pointer is durable. Reject output escape, links, nonempty/current collisions, arbitrary terminal names, dangling/mismatched R0 relations, publication-name insertion, commit drift, and overwrite of historical C0 or published R0 evidence.

Without `-PreviewScene`, allow only the exact preview-input gate `OPEN/PREVIEW_INPUTS_UNAVAILABLE` while all deterministic C0 gates pass; do not call the full run PASS. With an approved explicit `-PreviewScene`, require the preview gate to adjudicate normally. Preserve the C0 record's own schema version and add the exact no-argument frozen default assertion.

- [ ] **Step 2: Run C0 migration tests RED**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 11
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 11 -Module unittest -ModuleArguments @("tests.test_qt_quick_benchmark", "tests.test_cli", "tests.test_bundle_manifest", "-v")
~~~

- [ ] **Step 3: Migrate consumers and preserve exact behavior**

Activate Task 10's already complete producer/merger only by changing `scripts/build_sgfx_exe.py` and the listed frozen entry, benchmark, CLI, test, and C0-verifier consumers. `sg_preflight/bundle_manifest.py` must remain byte-identical to its Task 10 tooling commit and reviewed native-build provenance record, must be absent from the Task 11 staged diff, and is revalidated before commit and before every pair/product build. If activation exposes a required change in that module, stop and execute Task 10's new-tooling/new-provenance binding cycle; reusing the old digest is forbidden.

In one tracked commit, activate the package manifest producer/merger and every frozen entry, benchmark, CLI, and C0 verifier expectation at schema 2. No intermediate active schema mismatch is permitted. Every PowerShell-to-Python edge in the package, benchmark, R0 verifier, and C0 verifier uses Task 1's isolated launcher and rejects every forbidden import-capable source artifact regardless of ignore/index state before import/build; no direct `python.exe`, Store stub, or ambient module execution remains. `build_sgfx_exe.py --staged-only` creates its package `.next` root and every child through `SecurePublisher` transaction kind `package_stage`, completes the flushed journal only after the exact staged tree/manifest is durable, and blocks on any interrupted/unknown package-stage orphan pending Task 10's authorized no-path recovery. No-argument `DEFAULT_DOUBLE_CLICK_ARGS` remains exactly `dashboard run --ui-mode clean`; no QML/action/capability/surface code changes. C0 isolated verification delegates every generated leaf, directory, completion, current-pointer, and relation-safe retention operation to the shared `SecurePublisher`; it publishes only to its separately retained `c0-<RunId>` root and external completion/current records so Task 13 can produce same-HEAD evidence without replacing `out/control-center-c0` or appending beneath an R0 run.

Freeze one product-only R0 host entry in `exe_entry.py`: the staged executable accepts exactly argv `--internal-ramses-r0-product` together with one protected `SGFX_R0_PRODUCT_REQUEST` environment value and requires `sys.frozen`. That value is exactly one UTF-16 Windows absolute path of at most 32,767 characters, with no NUL, to a source-verifier-created regular request leaf beneath the held lane-attempt root. The canonical request is at most 256 KiB; the source verifier retains its exact volume/file identity with `FILE_SHARE_READ` only, and the host opens the same leaf without write/delete sharing, revalidates its complete component chain and identity, holds it through consumption, then removes the environment key before normal CLI dispatch. The host rejects a nonlocal/device/ADS/reparse/hardlinked file, a different identity, extra environment key, partial/oversized document, or request path outside the exact attempt root.

Before the one QPC origin, the source verifier securely creates and pins exactly four empty product-transport staging leaves for native report, optional frame, inner stdout, and inner stderr, retaining non-inheritable originals. It creates child-writable inheritable duplicates and puts their numeric values only in the protected request. The suspended outer host is assigned to the source-verifier-owned queryable Job before resume with `bInheritHandles=TRUE` and `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` exactly NUL, outer-stdout write, outer-stderr write, and those four product-output duplicates—seven handles total. No request, runtime, final-report, or isolated-evidence handle is inherited accidentally. The source-verifier worker, not the host, owns the Ramses lease, Job, outer pipes, and all four original transport handles through outer signal, exit capture, outer drain join, outer thread/process/pipe closure, Job active-zero, Job closure, and lease release in that order. It creates source-only final-report/evidence staging leaves after that ordered cleanup and authoritative retained-handle/source/runtime/Git validation, never before or inside the host.

The host proves it is already contained in the inherited no-breakaway Job, never acquires/releases the Ramses lease, never creates another Job, and never rebases the four QPC facts. It invokes only Task 7's contained inner-native transport and resolves the manifest only as `<exe-dir>/bundle-manifest.json`, the native helper only as `<exe-dir>/_internal/cpp/bin/sgfx_ramses_probe.exe`, and the three schemas only beneath `<exe-dir>/_internal/sg_preflight/data/ramses_r0`; every item and the complete staged product tree must reconcile through retained identities and the adjacent schema-2 manifest before launch. For the inner native child the host creates NUL and inner stdout/stderr pipes, passes exactly those plus duplicates of the inherited native-report/frame handles through its own five-handle `PROC_THREAD_ATTRIBUTE_HANDLE_LIST`, and lets the child inherit the outer Job. Because the host itself keeps that Job nonzero, it waits only for the exact inner process handle, captures its exit, joins inner drains, closes inner thread/process/pipe handles, validates provisional native/frame transport, flushes/re-reads the four transport handles, emits the canonical outer status, and begins normal exit by parent start +59.700 seconds; it never invokes Tasks 8/9, inherits or writes final/evidence bytes, queries or claims whole-Job active-zero, or creates, opens for output, publishes, renames, or unlinks an artifact path. By +60.000 the source worker must observe the outer host signal, capture outer exit, join outer drains, close outer thread/process/pipe handles, observe whole-Job active-zero, close the Job, and release the Ramses lease in that order for process PASS. Only afterward, outside process timing, does the outer source verifier validate its original retained native/frame/log identities plus authoritative after-source/runtime/Git facts, exclusively execute Tasks 8/9, and securely publish final/evidence from source-only handles. Packaged Task 8/9 code is exercised through frozen-import tests and manifest-bound byte-exact schema fixtures, never through pre-after-Git product bytes. Missing any host +59.700 or source cleanup +60.000 obligation is execution rejection; at +60.000 the source worker terminates any still-active process in the complete Job.

The request cannot choose or override any executable, manifest, bundle root, schema, backend, dimensions, timeout, QPC value, environment, or output filename. Outer stdout is exactly one canonical UTF-8-with-terminal-LF object capped at 16 KiB with keys `schemaVersion`, `state`, `code`, `runToken`, and `generation`; stderr is empty. Schema is 1. Exact state/code/exit pairs are `accepted/ACCEPTED/0`, `unavailable/INPUT_UNAVAILABLE/10`, `rejected/EXECUTION_REJECTED/20`, and `internal_failure/INTERNAL_FAILURE/30`. Here `accepted/ACCEPTED` means only that the host completed and flushed its provisional native/frame/log transport; it is never product-lane acceptance. That requires the outer source cleanup, authoritative post-zero validations, and source-only Tasks 8/9 above. The accepted transport requires the request's 32-hex run token and non-Boolean uint64 generation; every other state uses null token/generation. The source verifier parses, validates, and discards outer transport bytes; only the separately inherited inner logs become lane evidence. Any other argv/environment/handle/status combination follows the existing CLI/default path or fails closed. Tests cover moved/missing/extra/mutated adjacency and bundle-tree races, request identity/path/environment/size/NUL races, every missing/extra/inheritable handle across the exact seven/five contracts, attempted final/evidence inheritance or pre-zero write, host crash/timeout with active descendant, QPC rebasing, inner artifact write failure, replacement after host flush/before source validation, outer stream overflow/extra bytes/raw path/state/code/exit mismatch, premature lane promotion, frozen Task 8/9 import/fixture drift, source-Python use of the internal token, and byte-for-byte Clean no-argument behavior.

- [ ] **Step 4: Run fixture GREEN, review, and commit the atomic schema-2 activation**

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 11
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 11 -Module unittest -ModuleArguments @("tests.test_qt_quick_benchmark", "tests.test_cli", "tests.test_bundle_manifest", "tests.test_qt_quick_preview", "-v")
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 11 -Module unittest -ModuleArguments @("tests.test_cli.TestCLI.test_frozen_exe_entry_defaults_to_clean_dashboard_when_double_clicked", "-v")
git add scripts/build_sgfx_exe.py sg_preflight/exe_entry.py scripts/verify_control_center_c0.ps1 scripts/benchmark_qt_quick.py tests/test_qt_quick_benchmark.py tests/test_cli.py tests/test_bundle_manifest.py
git diff --cached --check
git commit -m "build: migrate C0 bundle verification"
~~~

- [ ] **Step 5: Rebuild the pair and exact staged product from the clean activation commit**

Before reusing the Task 10 provenance digest, reopen the reviewed native-build record and require the current `sg_preflight/bundle_manifest.py` identity, size, and SHA-256 to equal its frozen `toolingFiles` entry and the Task 10 commit. A mismatch is a provenance failure requiring the complete rebind cycle, not an activation-build input.

Run only from the clean Step 4 commit. Root Lexus copies the exact 40-lowercase-hex commit returned by the successful Step 4 commit into `$task11Commit`, reuses the independently reviewed Task 10 tracked provenance digest as `$nativeBuildProvenanceSha256`, and supplies the already bound primary-Python receipt digest as `$expectedPrimaryPythonProvenanceReceiptSha256`; this step never derives or substitutes a binding:

~~~powershell
if ($task11Commit -cnotmatch '^[0-9a-f]{40}$') { throw "Task 11 commit binding is invalid" }
if ($nativeBuildProvenanceSha256 -cnotmatch '^[0-9a-f]{64}$') { throw "Native-build provenance binding is invalid" }
if ($expectedPrimaryPythonProvenanceReceiptSha256 -cnotmatch '^[0-9a-f]{64}$') { throw "Primary-Python provenance binding is invalid" }
& scripts\stage_ramses_runtime_pair.ps1 -ExpectedSourceCommit $task11Commit -ExpectedPrimaryPythonProvenanceReceiptSha256 $expectedPrimaryPythonProvenanceReceiptSha256 -ExpectedNativeBuildProvenanceSha256 $nativeBuildProvenanceSha256
& scripts\run_sgfx_isolated_python.ps1 -ExpectedSourceCommit $task11Commit -Script scripts/build_sgfx_exe.py -ScriptArguments @("--staged-only")
& scripts\run_sgfx_isolated_python.ps1 -ExpectedSourceCommit $task11Commit -Module sg_preflight.bundle_manifest_cli -ModuleArguments @("inspect-v2", "--path", "build/b/sgfx-preflight/bundle-manifest.json")
~~~

Require the runtime-pair receipt, staged product manifest, `_internal/cpp/bin`, and `_internal/sg_preflight/data/ramses_r0` to reconcile to the same clean activation commit, pair ID, and digests. Open every staged directory/file through retained no-write/no-delete identities, compute the deterministic whole-tree digest, run the global bounded physical plus recursive CArchive/PYZ/ZIP logical-member privacy scan through those handles, and re-enumerate/reconcile the held tree before accepting it. Require nonzero logical-member/decompressed-byte counts for the real PyInstaller executable and prove `PYZ.pyz` plus `base_library.zip` were decoded rather than raw-scanned only. If the strict real build exposes a source defect, commit the fix and repeat the entire clean postcommit stage; never reuse the pre-activation pair or authenticate a dirty tree.

---

### Task 12: Add the deterministic R0 verifier and license record

**Files:**
- Create: scripts/verify_ramses_r0.ps1
- Create: scripts/run_sgfx_r0_acceptance.ps1
- Create: tests/test_ramses_r0_verifier.py
- Modify: sg_preflight/ramses_probe_process.py
- Modify: sg_preflight/ramses_probe_runner.py
- Modify: tests/test_ramses_probe_process.py
- Modify: tests/test_ramses_probe_runner.py
- Modify: NOTICE.md

**Interfaces:**
- Produces atomic per-run `ramses-r0-verification.json`/`.md`, a current pointer, exact package/privacy/process/source evidence, and PASS/OPEN/FAIL gates; no acceptance claim before Task 13 review.

- [ ] **Step 1: Write failing verifier, publication, retention, and Python-floor tests**

Static tests require: caller-supplied reviewed 40-hex expected source commit; the complete authenticated HEAD-record/blob/fresh-index/filesystem proof at every named boundary independent of ignore/caller-index state; isolated Task 1 Python bootstrap with zero forbidden import-capable artifacts in active source roots regardless of ignore/index state; mandatory separately reviewer-bound CPython 3.13 primary receipt digest and complete protected runtime/dependency snapshots; caller-supplied or securely generated 32-hex run ID; independently reviewed native-build provenance plus completed native-dependency-snapshot publication and a fresh protected disconnected configure/Release build of both helpers with no `build/cine-c0` read; safe journaled app-local pair stage followed immediately by a fresh journaled staged-product build on every verifier invocation; CTest/native synthetic and Python gates; protected runner only; no production synthetic scene execution; product-manifest inspection without private inputs; matched real receipt/product execution only when approved canonical inputs exist; canonical-profile scene identity; source before/after proof; one-origin 60/30 contained-process timing; exact Ramses subtree inventories plus held-tree whole-bundle digest and recursive physical/CArchive/PYZ/ZIP privacy scan; per-lane ordered process cleanup; receipt-bound CPython 3.10 lane; shared-secure-publisher journals plus `.next`/JSON/Markdown/run-manifest/directory/external-completion/current-pointer publication; separate C0 hierarchy; relation-aware locked retention; blocked-orphan/explicit-recovery behavior; and no Jira/Clockodo/SVN/BMW network/write/push/direct native launch. Test interrupted run, failed rerun after prior PASS, duplicate/colliding run IDs, one-sided perspective, unknown output, stale/malformed pointer, completion/manifest digest mismatch, run-manifest extra/missing/duplicate role-case-lane entries, every pruning guard, whole-bundle physical/logical file/byte/depth overflow or mutation, every privacy-marker representation at every possible raw/decompressed chunk split, malformed/duplicate/encrypted/overlapping/trailing/archive-bomb members, evidence/pair lock ordering, publication journal/authorization/name insertion races, arbitrary ignored module/schema/hook contamination, valid timestamp-forged `.pyc`/`.pyd`/`.pth`/sitecustomize/PyInstaller-hook contamination under every ignore/index state, poisoned Python/Git environment, forged primary receipt/snapshot/expected digest, malicious PEP-514 registration/provenance/signature/runtime-tree cases, forged native tool/dependency/build receipts or mutable generated projects, product-host seven/five-handle leaks/timeout/crash/inner-wait-versus-whole-Job cycles, attempted pre-zero final/evidence creation, and host-flush-to-parent-validation replacement attempts.

The static and fixture suite also requires fixed-name lookup and canonical validation of external `python313-<ExpectedPrimaryPythonProvenanceReceiptSha256>.json`, with no caller path. It mutates the record filename, kind, binding commit, reviewed receipt digest, launcher/receipt file records, reviewer set, DACL/identity, and target bytes independently and requires failure before Python. Its Release gate builds both payload helpers plus all eight fixed native test targets and requires the complete eight-test CTest footer. Here “disconnected” means only reviewed local dependency inputs and no CMake fetch/update path; the receipt explicitly records `osEgressContainment=not_claimed`, and neither verifier nor acceptance may claim OS-enforced outbound denial. Publication fixtures exercise every restart window after journal-bootstrap publication, final-root rename, completion publication, pointer replacement, journal completion, runtime-pair tombstone rename, each R0/C0 retention intent before mutation, the mandatory both-intents-durable gate, completion-tombstone rename/result while the run intent is already durable, run-root tombstone rename/result, every descendant deletion, and both file/directory root-delete-before-result cases, distinguishing pre-pointer preservation of the old pointer from post-pointer roll-forward of the new one.

- [ ] **Step 2: Run verifier/process/runner tests RED**

Keep the static verifier module and the two extended production-test modules importable with test-local sentinels. Before implementing Task 12 production behavior, refresh the exact manifest and require named assertion failures for the missing verifier contract, publication recovery enforcement, reviewed floor binding, and acceptance-launcher checks; missing-file or import errors do not satisfy RED:

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 12
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 12 -Module unittest -ModuleArguments @("tests.test_ramses_r0_verifier", "tests.test_ramses_probe_process", "tests.test_ramses_probe_runner", "-v")
~~~

Record the exact assertion failures in the progress ledger.

- [ ] **Step 3: Implement exact verifier and factual licenses**

Before any gate, the verifier derives fixed external `python313-<ExpectedPrimaryPythonProvenanceReceiptSha256>.json` beneath the canonical review root, accepts no caller path, retains its identity with the tracked launcher/receipt, and requires its `reviewedCommit`, `reviewedDigest`, exact two file records, and reviewer set to match the Task 1 binding. Missing or mismatched external primary review evidence is terminal before Python. Task 13 revalidates the same fixed record on the final implementation commit; that later review confirms the original binding remains exact rather than replacing its `reviewedCommit`.

Parameters are required `ExpectedSourceCommit`, `ExpectedPrimaryPythonProvenanceReceiptSha256`, and `ExpectedNativeBuildProvenanceSha256` plus optional `RunId`, `Scene`, `Perspective`, `PerspectiveId`, `ProfileId`, `ScenarioId`, and `ExpectedReviewedPython310ProvenanceReceiptSha256`, with internal test-only clock/root seams. The primary and native-build digests are exactly 64 lowercase hex, must equal their independently reviewed Task 1/Task 10 values and the frozen launcher/receipt values, and cannot be derived from mutable receipts by this verifier. The optional 3.10 digest is exactly one separately reviewer-bound 64-lowercase-hex value; there is no caller-supplied floor receipt path. When present, the verifier derives only fixed `.superpowers/sdd/r0-python-floor-310-v1/receipt.json`, requires its canonical bytes to equal the separately reviewed digest and protected review record, and revalidates the entire fixed snapshot. When absent, it never executes a discovered/candidate floor and records exactly `PYTHON_3_10_INTERPRETER_UNAVAILABLE` or `PYTHON_3_10_PROVENANCE_REQUIRED`. `ExpectedSourceCommit` must be exactly 40 lowercase hex and is never derived or replaced inside the verifier. `RunId`, when supplied, must be exactly 32 lowercase hex; when omitted, generate it with cryptographic randomness and expose it only through sanitized status/current inspection. Real execution requires all of `Scene`, `Perspective`, `PerspectiveId`, `ProfileId`, and `ScenarioId`; the supplied scene final file identity/digest must equal the scene resolved from that live `RunProfile`, and Task 13 must source the nonempty scenario ID from the explicit protected local approval binding rather than hard-code a vehicle/scenario token. Arbitrary dev/synthetic overrides cannot satisfy the real gate. Any partial real-input set is FAIL. With the set entirely absent, CTest/native generated-scene tests still run, but neither production lane launches or creates a request/log/report directory; the two exact execution gates are `OPEN/REAL_RECEIPT_INPUTS_REQUIRED` and `OPEN/REAL_PRODUCT_INPUTS_REQUIRED`.

Create `scripts/run_sgfx_r0_acceptance.ps1` as the sole Task 13 entry point with modes `NoPrivate`, `Real`, and `Regression`. It requires `ExpectedLauncherSha256`, `ExpectedSourceCommit`, `ExpectedPrimaryPythonProvenanceReceiptSha256`, and `ExpectedNativeBuildProvenanceSha256`; the optional reviewed 3.10 digest has no caller path, and `Real` additionally requires the complete approved five-field input set. The launcher is canonical UTF-8 without BOM and one terminal LF. Task 13 Step 1 records its digest outside the mutable source tree as part of the independent review disposition. Before any mutable source or dependency executes, the launcher opens its own regular local file with read sharing only, validates ancestry/identity/no-reparse/no-ADS/link-count-one, hashes the held bytes, and requires the separately reviewer-bound caller digest; the caller independently retains the same no-write/no-delete identity from hash through invocation. It then implements Task 1's complete authenticated real-Git/runtime/source proof directly and holds every tracked PowerShell/Python/native-build script leaf it will execute without write/delete sharing. Each held stream must reproduce its `ExpectedSourceCommit` object ID through the frozen expected-commit built-in Git-clean transform; the explicit `-text` acceptance launcher and verifier scripts must additionally equal their raw blobs. It never calls ambient `git`, never starts a nested shell, never derives expected commit or receipt hashes, and never invokes an unheld script path.

Each mode executes in the current authenticated PowerShell process through only held exact script identities. `NoPrivate` runs the R0 and related C0 deterministic verifiers, performs both strict current-pointer inspections, and emits one canonical path-free summary. `Real` generates distinct cryptographic R0/C0 IDs, runs matched receipt/product and related C0 real-input verification, performs both strict inspections, and emits the same summary shape. `Regression` runs the affected matrix, exact frozen no-argument default test, and full discovery solely through Task 1's held isolated Python launcher. Before and after every individual build, verifier, inspector, or test gate—and again while holding the evidence mutex before publication—the launcher reruns the full expected-commit HEAD-record/blob/fresh-index/filesystem proof and rehashes every retained script/runtime leaf; exact generated roots are the only explicit exclusions. A gate may write only its declared ignored roots. Any source, script, runtime, launcher, generated-root, or proof drift terminates the sequence before the next gate. Tests mutate each launcher/script/source byte between proof and execution, forge the separately supplied launcher/primary-receipt digests, poison ambient Git/PowerShell/PATH, and prove no unreviewed byte executes.

Create `runs/run-<RunId>.next` with the shared `SecurePublisher`, capture run ID/source commit/UTC timestamps, and count every real receipt/product child against this attempt's 32 MiB cap. Publish every directory and leaf—including JSON, Markdown, exact-tree `run-manifest.json`, external completion, and current pointer—only through that publisher and its evidence mutex; no verifier component path-opens a generated artifact for writing or later trust. After flush/reparse/hash reconciliation, rename the complete pinned `.next` directory to `run-<RunId>` with no replace, exclusively publish `completions/r0-<RunId>.json`, and atomically publish the strict digest-bound `current.json` last without invalidating a prior completed pointer at start. Revalidate the complete `ExpectedSourceCommit` HEAD-record/blob/fresh-index/filesystem proof, every retained script/runtime/artifact identity, and the complete relation graph while holding the evidence mutex immediately before completion/current publication.

Add `ramses_probe_runner --inspect-current <fixed validated pointer>` as the sole strict R0/C0 pointer, external-completion, manifest, and exact-tree reader. It accepts only the exact validated `out/ramses-r0/current.json` or `out/ramses-r0/c0-current.json` shape, emits one canonical path-free object with exactly `schemaVersion`, `kind`, `state`, `runId`, `relatedR0RunId`, `sourceCommit`, `runManifestSha256`, `completionRecordSha256`, and `completedAtUtc`, and rejects every pointer/completion/manifest/tree/relation disagreement. Apply Global Constraints locking and retention exactly across the separate R0/C0 hierarchies. Canonical handoff data stores only the fixed resolver token `R0_CURRENT` or `C0_CURRENT`, sanitized run/related-run IDs, source commit, roles, state, and manifest/completion/verifier digests. It never stores a resolved current-pointer, verifier JSON/Markdown, real lane, frame, log, private input/output path, or frame digest.

Freeze `ramses-r0-verification.json` as canonical UTF-8 without BOM, one terminal LF, schema 1, and exactly these top-level keys in this order: `schemaVersion`, `kind`, `state`, `runId`, `sourceCommit`, `startedAtUtc`, `completedAtUtc`, `realInputMode`, `gates`, `lanes`, `timing`, `package`, `privacy`, `sourceIntegrity`, `processCleanup`, `requirements`, `openCodes`, and `artifactRoles`. `kind` is `ramses_r0_verification`; state is `PASS`, `PASS_WITH_OPEN`, or `FAIL`; timestamps are UTC with exactly six fractional digits; IDs/hashes are strict lowercase; `realInputMode` is `absent` or `complete`; unknown keys, duplicate keys, Boolean integers, unsorted arrays, and noncanonical encoding fail.

`gates` is an array in this exact ID order: `native_contract_tests`, `python_primary`, `python_floor_3_10`, `runtime_pair`, `product_manifest`, `real_receipt_execution`, `real_product_execution`, `lane_fact_parity`, `package_inventory`, `privacy`, `source_integrity`, `process_lifecycle_tests`, `bundle_schema_2`, `no_default_flip`, and `timing`. Each object is exactly `{id,state,code}` with state `PASS|OPEN|FAIL`. A PASS code is exactly `PASS`; a FAIL code is exactly `FAIL_` plus the uppercase gate ID; OPEN codes are limited to `PYTHON_3_10_INTERPRETER_UNAVAILABLE`, `PYTHON_3_10_PROVENANCE_REQUIRED`, `REAL_RECEIPT_INPUTS_REQUIRED`, `REAL_PRODUCT_INPUTS_REQUIRED`, `REAL_LANE_FACTS_REQUIRED`, and `REPRESENTATIVE_TIMING_REQUIRED`, each legal only for its matching gate.

`lanes` is exactly receipt then product, each object exactly `{lane,executionState,executionCode,renderOutcome,sourceFingerprint,nativeReportSha256,finalReportSha256,isolatedEvidenceSha256,firstFrameSha256}`. `executionState` is `PASS|OPEN|FAIL`. Receipt codes are respectively `PASS`, `REAL_RECEIPT_INPUTS_REQUIRED`, or `FAIL_REAL_RECEIPT_EXECUTION`; product codes are `PASS`, `REAL_PRODUCT_INPUTS_REQUIRED`, or `FAIL_REAL_PRODUCT_EXECUTION`. OPEN and FAIL require `renderOutcome`, source fingerprint, and all four artifact hashes null; any partial failure artifacts are represented only by the exact manifest prefix. PASS requires a 64-hex source fingerprint plus native/final/evidence hashes and `renderOutcome` exactly one of the 14 stable codes or `undetermined_after_<one exact phase>`. `firstFrameSha256` is nonnull exactly for `clear_only_frame` or `visible_frame_observed` and null otherwise.

`timing` is exactly `{evidenceState,requirement27State,lanes}`. `evidenceState` is `PASS_EXACT_INPUT|OPEN`, requirement 27 is `OPEN`, and lanes are receipt then product objects `{lane,state,containedProcessMilliseconds,authoredRenderMilliseconds}` with `state=NOT_RUN|PARTIAL|MEASURED|EXCEEDED`. Contained-process time is the design section-22 child budget measured from the one parent QPC origin through direct signal and exit capture, joined source-owned drains, thread/process/pipe closure, whole-Job active-zero observation, Job closure, and source-worker Ramses lease release in that exact order; it deliberately excludes later retained artifact/source re-read, after-Git, policy verification, and secure publication. OPEN execution requires NOT_RUN and both times null. PASS execution requires a non-Boolean nonnegative integer contained-process value within 60,000: PARTIAL has null authored time because that phase was not reached, while MEASURED has both integers within 60,000/30,000; PASS can never be EXCEEDED. Failed execution may be NOT_RUN before launch, PARTIAL when only an in-budget prefix was observed, MEASURED when both clocks completed within budget but a later acceptance gate failed, or EXCEEDED when any observed value crossed its budget. Each failed-execution value is null exactly when that clock or phase was never observed, so a contained-process timeout before authored rendering is EXCEEDED with a nonnull contained-process value and null authored value. `PASS_EXACT_INPUT` is legal only after both lane executions and cleanup gates PASS, both timing lanes are MEASURED within budget, post-process acceptance passes, and facts match; otherwise it is OPEN. Post-process acceptance failure leaves timing facts factual but cannot produce `PASS_EXACT_INPUT`.

`package` is exactly `{pairId,configuration,runtimePairReceiptSha256,productManifestInspectionSha256,bundleTreeSha256}`; every field is null only before its proof in terminal FAIL, while every non-FAIL record has a 32-hex pair ID, `Release`, and three lowercase hashes. `privacy` is exactly `{state,physicalFilesScanned,physicalBytesScanned,archiveMembersScanned,decompressedBytesScanned,markerVariantCount}` with state `PASS|FAIL` and non-Boolean nonnegative counts, or all proof values null before that terminal-FAIL stage. A successful real PyInstaller scan requires nonzero archive-member and decompressed-byte counts and separately proves CArchive, PYZ, and base-library ZIP traversal; a raw-only scan cannot pass. `sourceIntegrity` is exactly `{state,code,mode,sceneBeforeSha256,sceneAfterSha256,perspectiveBeforeSha256,perspectiveAfterSha256,worktreeBefore,worktreeAfter}`. State is `NOT_RUN|PARTIAL|PASS|FAIL`; code is respectively `SOURCE_INTEGRITY_NOT_RUN`, `SOURCE_INTEGRITY_PARTIAL`, one of `NO_REAL_INPUTS|PASS_EXACT_HASH_ONLY|PASS_CONTAINED_WORKTREE`, or `FAIL_SOURCE_INTEGRITY`; and mode is `unknown|absent|exact_hash_only|contained_worktree`. `PASS/NO_REAL_INPUTS/absent` has all six proof fields null. A passing real `exact_hash_only` record has equal nonnull scene hashes, both worktree fields null, and perspective hashes either both null when no perspective was supplied or equal/non-null. A passing `contained_worktree` record has the same input rule plus equal lowercase SHA-256 worktree snapshots derived from the complete authenticated HEAD/blob/fresh-index/filesystem proof. `NOT_RUN` has all proof fields null and mode `unknown`; `PARTIAL` preserves every observed before/after field, permits skipped after fields to remain null, and never invents equality; `FAIL` preserves unequal hashes/snapshots and every other successfully observed field rather than normalizing them away. For a no-real-input record, `source_integrity` gate is PASS exactly with `PASS/NO_REAL_INPUTS`; for a successful real record it is PASS exactly when this object is PASS in the matching mode; and for real-input `NOT_RUN`, `PARTIAL`, or `FAIL` it is `FAIL/FAIL_SOURCE_INTEGRITY`. Timeout-after-before-hash, timeout-after-before-Git, byte mutation, Git mutation, and early-failure fixtures pin the exact nullability, retained mismatch facts, object code, gate cross-link, and top-level FAIL derivation.

`processCleanup` is exactly `{contractTestsState,lanes}`. Contract state is `PASS|FAIL` and refers only to deterministic Job/lease/process tests. Lanes are receipt then product objects `{lane,state,directProcessSignaled,exitCaptured,drainsJoined,directResourcesClosed,activeProcesses,jobClosed,leaseReleased}`. Absent execution is exactly `NOT_RUN` with seven null proofs. A completed lane is PASS only with all six Booleans true and non-Boolean active count zero, proven in the field order shown. `directResourcesClosed` covers thread/process/pipe handles and must precede the active-zero observation; `jobClosed` is separate and must follow active-zero. Retained artifact/input/runtime handles remain live under their distinct acceptance owner. FAIL permits only actually observed typed proofs, using null after the first unproven stage; execution PASS requires cleanup PASS. Tests reject reordered facts, active zero while the direct process handle is retained, Job closure before the zero query, or lease release before Job closure. `requirements` contains exactly IDs 1 through 28 in numeric order and objects `{id,state,evidenceCodes}`, where state is `PASS_STANDALONE|DEFERRED_TO_INTEGRATION|OPEN|FAIL` and evidence codes are sorted unique uppercase tokens. Component aggregation precedence is exact: any failure -> `FAIL`; otherwise any unresolved standalone prerequisite -> `OPEN`; otherwise any unfinished integration half -> `DEFERRED_TO_INTEGRATION`; otherwise all standalone components passed -> `PASS_STANDALONE`. Evidence codes retain each component result, but one requirement object never has two states. Task 13's `REQUIREMENT_MATRIX_V1` is compiled into the generator and validator as the only legal ID/order, proof-owner, successful code set, OPEN alternative, FAIL alternative, and aggregate mapping. The validator independently derives all 28 rows from gates/artifacts/test receipts; it rejects a missing/extra/reordered row, unknown/missing/extra/reordered/wrong code, a code legal for another requirement, wrong alternative for the observed proof state, or a supplied aggregate that differs from precedence. `openCodes` is independently derived as the sorted unique union of every OPEN gate code plus exactly the matrix-classified deferred/open component codes from OPEN or DEFERRED requirements. A passed standalone component code never enters `openCodes` merely because its sibling integration component is deferred. Tests pin the complete 28-row successful-standalone, no-private-OPEN, floor-OPEN, real-smoke-OPEN, timing-OPEN, every-one-row-FAIL, and every-one-code mutation fixtures plus byte-exact `openCodes` derivation. `artifactRoles` is the sorted unique array of exact `{role,case,lane}` triples present in the run manifest.

Top-level state is derived, never assigned independently: any FAIL gate or requirement yields `FAIL`; otherwise any OPEN gate or OPEN/DEFERRED requirement yields `PASS_WITH_OPEN`; only a record with none yields `PASS`. The validated state must equal manifest, completion, and pointer state. Because representative timing and integration halves remain OPEN/DEFERRED in this standalone plan, every otherwise successful no-private or real record is exactly `PASS_WITH_OPEN`, never PASS.

Validate that model with one standard-library manual validator shared by generation and `--inspect-current`; generation must serialize, reparse, compare the typed model, and derive top-level state before publication. Derive `ramses-r0-verification.md` only from the accepted typed model using frozen section order `Identity`, `Gates`, `Lanes`, `Timing`, `Package and privacy`, `Source and process`, `Requirements`, `Open items`, and `Artifacts`, fixed headings/table columns, LF line endings, and no locale-dependent text, path, or timestamp formatting. Tests pin byte-exact JSON/Markdown fixtures for absent, accepted-complete, failed-before-proof, timeout-partial, source-hash-mismatch, and Git-mismatch modes and prove every field/type/enum/order/state/code/nullability/cross-record mutation is rejected.

On every invocation, use the trusted contained Git lane's complete real-Git/runtime/HEAD-record/blob/fresh-index/filesystem proof and require exact equality to `ExpectedSourceCommit` before building; exact authenticated generated roots are the only exclusions and ignore metadata is never authority. Retain the argument-only `scripts/run_sgfx_r0_native_build.ps1` as bound tooling, but invoke only held `scripts/stage_ramses_runtime_pair.ps1` with the explicit primary-Python and native-build provenance bindings so its exact isolated `build-and-stage-runtime-pair` coordinator creates one fresh protected configure/build root and stages one exact-commit `Release` preview/R0 pair while the Python publisher owns every journal/mutex/root handle; `build/cine-c0` and every prior configured/generated project are forbidden inputs. Repeat the complete source/tool/dependency/process proof; immediately run `scripts/build_sgfx_exe.py --staged-only`; then repeat the proof again before any execution. Issue one protected pair receipt containing both targets/configuration, native-build receipt/provenance digests, pair ID, all nine sizes/digests, and total. Open and retain the complete staged-product tree through no-write/no-delete identities, reconcile the exact manifest and pair, compute its deterministic whole-tree digest, perform the bounded physical plus recursive CArchive/PYZ/ZIP logical-member privacy scan, and re-enumerate it before each product launch and again under the evidence mutex. Any source, Git runtime, toolchain, protected dependency, build process audit, pair, product, archive, namespace, identity, digest, HEAD record, blob, fresh-index, or filesystem-inventory drift is terminal FAIL; a fresh pair always requires a fresh protected configure/build and product rebuild, and no prior generated project or staged product is reusable.

With no approved real inputs, perform no receipt/product probe execution and create no execution directory; strictly inspect and record the trusted pair receipt and staged product manifest/tree only, while retaining the two exact real-input OPEN gates and NOT_RUN timing/cleanup lanes. With the complete approved real set, execute both `real/receipt` through the receipt and `real/product` only through Task 11's exact adjacent frozen host (`sgfx-preflight.exe --internal-ramses-r0-product` plus the protected request-path capability). No PowerShell path directly launches the native helper.

For each real lane, one source-verifier process executes a complete retained-handle transaction with two explicit phases. The contained-process phase is adapter resolution -> parent-generated QPC/request/transport handles -> contained process -> direct signal -> exit capture -> drain join -> thread/process/pipe closure -> whole-Job active-zero -> Job closure -> lease release. The post-process acceptance phase is retained-handle native/module/source/runtime/after-Git validation -> source-only Task 8 policy report -> source-only Task 9 isolated evidence -> strict reparse -> secure publication. Receipt and product both construct policy/evidence exclusively in their source parent after the entire ordered process cleanup and authoritative after-state. Product uses Task 11's seven-handle frozen-host contract: the host can validate and flush only native/frame/inner-log transport after its inner signal/exit/drain/direct-resource sequence; the source worker owns the one Job/lease/deadline and original handles and is the sole Tasks 8/9 writer after the outer host and all descendants are gone, direct resources are closed, Job active-zero is observed, the Job is closed, and the lease is released. The source accepts only the exact outer transport status plus every ordered cleanup proof and original handle/schema/digest, then publishes request, inner logs, frame iff present, native report, final report, and isolated evidence in the manifest's exact longest-prefix order. No accepted final report exists without validated post-zero native/source/runtime/Git facts and no accepted evidence exists without an accepted final report. Timeout/crash/mutation yields a truthful terminal prefix and no pathname adoption.

For matched real execution, first reconcile exact source/helper/runtime/schema/pair/profile/scenario identities and require each lane's raw loaded-module volume/file IDs to equal that lane's retained pair/manifest identities. Then build and byte-compare one versioned canonical `nativePolicyParityV1` projection. It retains every policy- or evidence-bearing native fact: scene metadata and compatibility; validation aggregates, every sorted typed issue identity/severity/message/source class, uncapped totals, reported counts, and truncation flags/counts; every inventory bucket, sorted typed object identity/record, total/reported count, and truncation fact; all transform, camera, frustum, viewport, and render-dimension numeric state/identity/value observations plus numeric-list truncation; every sorted LogicEngine identity, count/category/link/executed/skipped/authored-application fact and truncation fact except its microsecond timing fields; authored-contract/interface/property presence and applied/not-applied values; every phase/lifecycle state, request/acknowledgement identity, stable failure stage, version tag, callback/result fact, and teardown result except raw QPC/timestamps/elapsed/duration; render outcome; and first-frame dimensions, clear/non-clear counts, channel min/max/mean, staging/encoding state, and decoded top-left RGBA SHA-256 when present. `loadedModules` is normalized only after the lane-specific identity proof into sorted exact objects `{role,fileName,authenticatedExpectedSha256}`; raw lane-specific volume/file IDs are replaced by, never added to, that normalized record. The only excluded fields are lane name, run token, generation, raw QPC/timestamps, elapsed/duration/microseconds, artifact IDs, protected paths, console bytes, and the raw module volume/file IDs replaced by the normalized module records. No other recursive key dropping, numeric omission, truncation omission, path substitution, or one-lane substitution is legal. Mutation tests change every retained scalar/list/object category—including each numeric observation, every reported/truncated count or flag, and normalized module role/name/digest—and require exact `FAIL/LANE_FACT_MISMATCH`; exclusion tests change only one explicitly excluded field at a time and require parity to remain unchanged.

Run every default Python 3.13 gate only after Task 1's fixed primary receipt, separately supplied expected receipt digest, complete protected runtime snapshot, and separate protected dependency snapshot all pass again. Recompute and reconcile the full receipt/snapshot/dependency tree records, fixed present `python313.zip` and `_pth`, exact PSF/Microsoft signer subjects/thumbprints/digests for `python.exe`, `python3.dll`, `python313.dll`, `VCRUNTIME140.dll`, and `VCRUNTIME140_1.dll`, fixed early loaded image plus `python313.dll`/`VCRUNTIME140.dll`, receipt-bound later `python3.dll`/`VCRUNTIME140_1.dll` if an extension loads them, no absent startup entry, no unexpected child/module, nonce handshake, and ordered Python Job cleanup before setting `python_primary=PASS`. The verifier and Task 13 launcher pass the expected digest explicitly to every isolated invocation; neither may derive, replace, relax, or omit it. Any primary mismatch is terminal FAIL and executes no workspace Python.

Discover CPython-3.10 candidates without caller `PATH` by reading the exact 32/64-bit HKCU/HKLM PEP-514 `PythonCore\3.10` views as untrusted data, rejecting `ExecutableArguments`, unsafe paths, reparse/remote/nonregular entries, and ambiguity; registry tags never authorize execution. If no candidate exists, record `PYTHON_3_10_INTERPRETER_UNAVAILABLE`. A floor snapshot may be prepared only through Task 1's already-reviewed PowerShell-only mode `-InitializePython310Snapshot -DevelopmentTask 12`, from that authenticated candidate plus exact held sibling `<worktree-parent>/sg-preflight/.venv310` whose `pyvenv.cfg` must resolve to the same candidate and whose selected dependencies satisfy `third_party_no_site_v1`. The mode uses the primary lane's fixed `.next`/DACL/no-replace/recovery rules, constructs deterministic `runtime/python310.zip` and exact ASCII `runtime/python310._pth` bytes `python310.zip\nDLLs\n..\..\..\..\n..\dependencies\n`, writes canonical `.superpowers/sdd/r0-python-floor-310-v1/receipt.json` only before the final no-replace rename, launches no Python, and emits only its digest. If candidate or compatible sibling dependency source is absent, it writes nothing and reports unavailable/provenance-required.

Task 12 Step 4 invokes that fixed mode exactly once after GREEN and before commit. The exact result is one of: no candidate and no write with `PYTHON_3_10_INTERPRETER_UNAVAILABLE`; candidate/dependency prerequisite not reviewable and no accepted snapshot with `PYTHON_3_10_PROVENANCE_REQUIRED`; or one protected candidate that immediately enters the independent review checkpoint below. A second initialization or an orphan never auto-cleans/retries.

The floor receipt uses exactly the primary schema, canonical serialization, signed/generated/tree record shapes, digest preimages, caps, and dependency policy. Its only lane substitutions are `snapshotId=r0-python-floor-310-v1`, the candidate's exact `3.10.x` patch version, `python310.dll`, `python310.zip`, `python310._pth`, and startup array `["python310.zip","DLLs","..\\..\\..\\..","..\\dependencies"]`; the stable-ABI leaf remains `python3.dll`. Both source and snapshot signed-file arrays still contain exact ordered roles `python`, `python_abi_dll`, `python_dll`, `vcruntime`, and `vcruntime_1`; they pin the PSF-signed executable, stable-ABI DLL, versioned DLL, and Microsoft-signed `VCRUNTIME140.dll`/`VCRUNTIME140_1.dll` identities, sizes, digests, subjects, and thumbprints. After `-InitializePython310Snapshot` creates a candidate, no floor execution occurs. Independent Python/security and scope/release reviewers open and retain the fixed receipt/runtime/dependency leaves, verify canonical receipt bytes, DACL/identities, source provenance, signer chains/thumbprints, five signed-runtime roles, ZIP/`_pth`, dependency closure, tree preimages, and module policy, then freeze the exact receipt digest in a protected review record outside mutable source. Task 13's three whole-branch reviewers must reconfirm that same digest and unchanged fixed-root identities before acceptance. Without that record and explicit reviewed digest, the only legal result is `PYTHON_3_10_PROVENANCE_REQUIRED`; no environment value or arbitrary receipt path can promote it. Before workspace import, require lane-specific early modules exactly held `python.exe`, `python310.dll`, and `VCRUNTIME140.dll` outside SystemRoot; later `python3.dll`/`VCRUNTIME140_1.dll` loads are permitted only from receipt-owned identities. Open/retain and revalidate every receipt/runtime/dependency leaf and tree preimage before and after execution; reject absent startup entries, unsafe links/streams/collisions, receipt/root overlap, signature/digest/tree/identity drift, or dependency shadowing. Only then launch through the same suspended Job/nonce/module/cleanup contract and require `sys.implementation.name == "cpython"`, `sys.version_info[:2] == (3, 10)`, architecture, executable, stable-ABI DLL if loaded, versioned DLL, and both VCRUNTIME identities if loaded. First compile/import every changed standard-library production module and schema consumer, then run the complete focused schema/adapter/process/publication/runner/lease/report/evidence/bundle/CLI/default tests. A candidate with missing/incompatible prepared dependencies or any receipt/runtime mismatch is FAIL, not unavailable/provenance-required; a smaller pure-data subset cannot satisfy the floor. Tests exercise exact 3.10 `python3.dll`/versioned-DLL/both-VCRUNTIME signer, digest, and module rejection and forbid the hard-coded 3.13 module set in the floor lane.

Update NOTICE.md from the installed Ramses license and existing SDL3/nlohmann sources, naming observed internal runtime roles without claiming external distribution approval.

- [ ] **Step 4: Run verifier contract tests GREEN and commit**

Run GREEN, then execute the one fixed floor-preparation probe. If it creates a candidate, pause this commit only long enough for the two independent floor reviewers to complete the fixed-root checkpoint and freeze its digest outside source; if it does not, record the exact OPEN code. The floor result does not permit changing a test expectation or inferring coverage.

~~~powershell
& scripts\run_sgfx_isolated_python.ps1 -RefreshDevelopmentManifest -DevelopmentTask 12
& scripts\run_sgfx_isolated_python.ps1 -DevelopmentTask 12 -Module unittest -ModuleArguments @("tests.test_ramses_r0_verifier", "tests.test_ramses_probe_process", "tests.test_ramses_probe_runner", "-v")
& scripts\run_sgfx_isolated_python.ps1 -InitializePython310Snapshot -DevelopmentTask 12
git add scripts/verify_ramses_r0.ps1 scripts/run_sgfx_r0_acceptance.ps1 tests/test_ramses_r0_verifier.py sg_preflight/ramses_probe_process.py sg_preflight/ramses_probe_runner.py tests/test_ramses_probe_process.py tests/test_ramses_probe_runner.py NOTICE.md
git diff --cached --check
git commit -m "build: verify standalone Ramses R0"
~~~

Do not run or claim commit-bound acceptance yet. Task 13 first obtains whole-branch reviews on a clean implementation HEAD; only a reviewed clean HEAD may produce exact acceptance evidence.

---

### Task 13: Execute standalone acceptance, real smoke, review, and delivery records

**Files:**
- Create: docs/tickets/sgfx-ramses-r0-standalone.md
- Create: docs/worklogs/sgfx-ramses-r0-standalone.md
- Modify: CHANGELOG.md
- Modify: plans/2026-07-13-sgfx-ramses-r0-standalone-implementation-plan.md
- Append only after commit: C:\Users\DavidErikGarciaArena\Downloads\sg-preflight\out\agent-control\STATE.md
- Append only after commit: C:\Users\DavidErikGarciaArena\Downloads\sg-preflight\out\agent-handoffs\codex_to_claude.md

**Interfaces:**
- Consumes: all Task 1-12 commits, the deterministic verifier, an explicitly supplied compatible local scene/perspective when authorized, and independent task/whole-branch reviews.
- Produces: accepted standalone R0 evidence or a truthful OPEN/FAIL record; no integrated action.

- [ ] **Step 1: Obtain clean-HEAD whole-branch reviews before exact evidence**

All three reviewers also reopen the fixed external `python313-<primary-receipt-sha256>.json`, its exact Task 1 binding commit, the retained launcher/receipt file records, and the protected snapshot closure. They confirm its bytes and target identities remain unchanged on the final implementation tree and record that reconfirmation in the implementation review binding; Task 13 does not create a replacement primary record or alter its original binding commit.

Require a clean Task 12 implementation HEAD and independent native/spec, Python/security/package, and scope/release reviews over the complete plan-to-HEAD diff. Fix every Critical or Important finding, run the covering RED/GREEN regressions, commit the fix, and repeat all affected reviews until none has an open Critical or Important finding. Record the reviewed implementation SHA, exact `scripts/run_sgfx_r0_acceptance.ps1` SHA-256, exact primary Python receipt SHA-256, and exact tracked native-build provenance SHA-256 separately outside the mutable source tree; all three reviewers confirm the launcher, primary binding, native tool/dependency/build contract, and provenance digest they reviewed. If the fixed CPython 3.10 floor exists, all three also reopen/revalidate its receipt/runtime/dependency closure and confirm the exact already reviewer-bound floor receipt digest; if it is absent or cannot be reconfirmed, no floor digest is supplied and the verifier must emit the applicable OPEN code. Exact evidence cannot begin on an unreviewed or dirty tree.

- [ ] **Step 2: Run both deterministic verifiers on the reviewed exact HEAD**

Run without private real inputs first. The separately reviewer-bound launcher generates distinct R0/C0 run IDs, performs every exact-source check, invokes both deterministic verifiers in-process through retained script identities, and reconciles both published pointers. The caller pins the launcher identity while PowerShell reads it: open the canonical leaf with read sharing only, compute its SHA-256 from the retained stream, require the independently reviewed digest, keep the stream open through invocation, and pass the same digest so the launcher performs its own regular-local-file/reparse/link/ADS/identity checks before work. Use the same bootstrap for Steps 2-4, changing only the mode and its declared inputs:

~~~powershell
$heldReviewedLauncherPath = [IO.Path]::GetFullPath('scripts\run_sgfx_r0_acceptance.ps1')
$allowedBindingNames = @(
    'SGFX_R0_REVIEWED_ACCEPTANCE_LAUNCHER_SHA256',
    'SGFX_R0_REVIEWED_IMPLEMENTATION_SHA',
    'SGFX_R0_PRIMARY_PYTHON_PROVENANCE_RECEIPT_SHA256',
    'SGFX_R0_REVIEWED_NATIVE_BUILD_PROVENANCE_SHA256',
    'SGFX_R0_REVIEWED_PYTHON310_PROVENANCE_RECEIPT_SHA256',
    'SGFX_R0_APPROVED_SCENE',
    'SGFX_R0_APPROVED_PERSPECTIVE',
    'SGFX_R0_APPROVED_PERSPECTIVE_ID',
    'SGFX_R0_CANONICAL_PROFILE_ID',
    'SGFX_R0_APPROVED_SCENARIO_ID'
)
$unexpectedBindings = @(
    [Environment]::GetEnvironmentVariables([EnvironmentVariableTarget]::Process).Keys |
        Where-Object { $_ -like 'SGFX_R0_*' -and $_ -notin $allowedBindingNames }
)
if ($unexpectedBindings.Count -ne 0) { throw "Unexpected SGFX R0 acceptance binding" }
$reviewedLauncherSha256 = $env:SGFX_R0_REVIEWED_ACCEPTANCE_LAUNCHER_SHA256
$reviewedImplementationSha = $env:SGFX_R0_REVIEWED_IMPLEMENTATION_SHA
$reviewedPrimaryPythonReceiptSha256 = $env:SGFX_R0_PRIMARY_PYTHON_PROVENANCE_RECEIPT_SHA256
$reviewedNativeBuildProvenanceSha256 = $env:SGFX_R0_REVIEWED_NATIVE_BUILD_PROVENANCE_SHA256
$reviewedPython310ReceiptSha256 = $env:SGFX_R0_REVIEWED_PYTHON310_PROVENANCE_RECEIPT_SHA256
$approvedScene = $env:SGFX_R0_APPROVED_SCENE
$approvedPerspective = $env:SGFX_R0_APPROVED_PERSPECTIVE
$approvedPerspectiveId = $env:SGFX_R0_APPROVED_PERSPECTIVE_ID
$canonicalProfile = $env:SGFX_R0_CANONICAL_PROFILE_ID
$approvedScenario = $env:SGFX_R0_APPROVED_SCENARIO_ID
foreach ($name in $allowedBindingNames) {
    [Environment]::SetEnvironmentVariable($name, $null, [EnvironmentVariableTarget]::Process)
}
if ($reviewedLauncherSha256 -cnotmatch '^[0-9a-f]{64}$' -or $reviewedImplementationSha -cnotmatch '^[0-9a-f]{40}$' -or $reviewedPrimaryPythonReceiptSha256 -cnotmatch '^[0-9a-f]{64}$' -or $reviewedNativeBuildProvenanceSha256 -cnotmatch '^[0-9a-f]{64}$') { throw "Reviewed acceptance bindings are invalid" }
$python310Args = @()
if (-not [string]::IsNullOrWhiteSpace($reviewedPython310ReceiptSha256)) {
    if ($reviewedPython310ReceiptSha256 -cnotmatch '^[0-9a-f]{64}$') { throw "Reviewed CPython 3.10 binding is invalid" }
    $python310Args = @('-ExpectedReviewedPython310ProvenanceReceiptSha256', $reviewedPython310ReceiptSha256)
}
$realInputValues = @($approvedScene, $approvedPerspective, $approvedPerspectiveId, $canonicalProfile, $approvedScenario)
$realInputCount = @($realInputValues | Where-Object { -not [string]::IsNullOrWhiteSpace($_) }).Count
if ($realInputCount -ne 0 -and $realInputCount -ne 5) { throw "Approved real-input binding is partial" }
$launcherStream = [IO.File]::Open($heldReviewedLauncherPath, [IO.FileMode]::Open, [IO.FileAccess]::Read, [IO.FileShare]::Read)
try {
    $sha = [Security.Cryptography.SHA256]::Create()
    try { $actualLauncherSha256 = ([BitConverter]::ToString($sha.ComputeHash($launcherStream))).Replace('-', '').ToLowerInvariant() } finally { $sha.Dispose() }
    if ($actualLauncherSha256 -cne $reviewedLauncherSha256) { throw "Acceptance launcher is not the reviewed byte stream" }
    & $heldReviewedLauncherPath -Mode NoPrivate -ExpectedLauncherSha256 $reviewedLauncherSha256 -ExpectedSourceCommit $reviewedImplementationSha -ExpectedPrimaryPythonProvenanceReceiptSha256 $reviewedPrimaryPythonReceiptSha256 -ExpectedNativeBuildProvenanceSha256 $reviewedNativeBuildProvenanceSha256 @python310Args
    if ($LASTEXITCODE -ne 0) { throw "Reviewed R0/C0 no-private acceptance failed" }
} finally {
    $launcherStream.Dispose()
}
~~~

The four required values are independent review bindings, never derived by the launcher or read from repository files. The bootstrap permits only the ten named caller bindings, captures them once, clears every one from the process environment before invoking the launcher, and then supplies provenance and approved-input values only as explicit parameters; the wrapper can never observe both environment and explicit receipt sources. The launcher rejects every residual or unknown `SGFX_R0_*` caller key and builds all child environments from scratch. The worklog records the launcher digest and protected provenance-binding presence, never receipt or private-input paths. Task 12 tests this exact PowerShell-5.1-compatible .NET hash/bootstrap text and the launcher's stricter self-checks.

Require deterministic native/CTest synthetic tests, Python, process-lifecycle tests, runtime-pair trust, product-manifest inspection, package, privacy, source-integrity, bundle-schema-2, and no-default-flip gates to pass. Per-run receipt/product cleanup is exactly NOT_RUN with null proofs because no production probe execution occurs. Receipt execution is exactly `OPEN/REAL_RECEIPT_INPUTS_REQUIRED`, product execution is exactly `OPEN/REAL_PRODUCT_INPUTS_REQUIRED`, and there is no synthetic request/log/report directory. Inspect and digest-reconcile the R0 JSON/Markdown/run manifest/external completion/current pointer, separate C0 JSON/Markdown/run manifest/external completion/current pointer, runtime-pair receipt, staged bundle manifest, and held whole-bundle digest. Require each source commit to equal the explicitly supplied reviewed implementation SHA; receipt and inspected-product helper/runtime/schema/pair/tree digests must reconcile exactly; every optional runtime object/digest set must be exact; the C0 artifact retains its own schema version and related R0 digest-bound identity; and the no-argument default remains Clean. The no-private R0 overall is exactly `PASS_WITH_OPEN`. The no-private C0 deterministic gates pass, preview is exactly `OPEN/PREVIEW_INPUTS_UNAVAILABLE`, timing is exactly `OPEN/OPEN_LOADED_WORKSTATION`, and overall is exactly `PASS_WITH_OPEN`; neither record is a full PASS. Python 3.10, R0 program-level timing, and real smoke may be OPEN only under their exact approved codes, and any standalone-acceptance consequence remains explicit.

- [ ] **Step 3: Run the approved real smoke and adjudicate 60/30 timing**

After confirming the explicit local files are approved for local read-only use, bind the complete five-field real-input set plus the optional 3.10 pair through the held-file bootstrap. `$approvedScene`, `$approvedPerspective`, `$approvedPerspectiveId`, `$canonicalProfile`, and `$approvedScenario` come respectively from the protected `SGFX_R0_APPROVED_SCENE`, `SGFX_R0_APPROVED_PERSPECTIVE`, `SGFX_R0_APPROVED_PERSPECTIVE_ID`, `SGFX_R0_CANONICAL_PROFILE_ID`, and `SGFX_R0_APPROVED_SCENARIO_ID` values; the launcher validates syntax, completeness, final identities, and approval binding before use. The same independently reviewed launcher identity generates distinct exact IDs, proves the source before and after every gate, runs both real verifiers, and reconciles both pointers. Repeat Step 2's complete held-stream bootstrap and replace only its final invocation with:

~~~powershell
if ($realInputCount -ne 5) { throw "Complete approved real-input binding is required" }
& $heldReviewedLauncherPath -Mode Real -ExpectedLauncherSha256 $reviewedLauncherSha256 -ExpectedSourceCommit $reviewedImplementationSha -ExpectedPrimaryPythonProvenanceReceiptSha256 $reviewedPrimaryPythonReceiptSha256 -ExpectedNativeBuildProvenanceSha256 $reviewedNativeBuildProvenanceSha256 -Scene $approvedScene -Perspective $approvedPerspective -PerspectiveId $approvedPerspectiveId -ProfileId $canonicalProfile -ScenarioId $approvedScenario @python310Args
if ($LASTEXITCODE -ne 0) { throw "Reviewed R0/C0 real-input acceptance failed" }
~~~

Require the scene's held final identity and digest to equal the live `RunProfile` canonical export, identical before/after SHA-256 for each passed input, matching available contained worktree snapshots or explicit `exact_hash_only`, complete receipt-lane and product-lane typed reports with reconciled digests/facts, and a truthful outcome code. All real-lane execution/evidence output stays beneath ignored `out/ramses-r0`; the only other generated writes are the exact authenticated protected fresh `build/ramses-r0-release`, `build/pyinstaller`, `cpp/runtime-pairs`, `build/b/sgfx-preflight`, native-dependency, and publication-journal roots, temporary test outputs are removed through their exact journaled identities, and every other write fails. `build/cine-c0` may be exercised only by development CTest regression and is never Release provenance. Require `first-frame.png` if and only if Task 4's strictly defined readback succeeded with stable clear/visible classification and the report declares it; a truthful pre-readback, missing-contract, or `undetermined_after_readback` outcome must have no PNG and may still satisfy the design's real-smoke classifier gate. On the same explicitly bound reviewed implementation SHA, the C0 real-input rerun must have deterministic and compatible-preview gates PASS, timing exactly `OPEN/OPEN_LOADED_WORKSTATION`, and overall exactly `PASS_WITH_OPEN`; a later separately authorized `-ReferenceReady` run is required to close C0 timing. A visible frame is not visual approval.

Record the exact per-lane contained-process and authored-render-phase values under `timing.lanes`. If both receipt and product executions/cleanup are PASS, both timing lanes are MEASURED within 60/30, and canonical facts match, set `timing.evidenceState=PASS_EXACT_INPUT` for that exact profile/scenario/source fingerprint. A pre-authored classified outcome is PARTIAL and cannot claim the timing pass. Post-process validation time is not folded into or mislabeled as the section-22 child-process budget, while any post-process failure still prevents the evidence state from passing. With no approved inputs, both lanes are NOT_RUN, timing evidence is OPEN, and no production timing sample exists. Requirement 27 and program-level timing remain `OPEN` until the separately approved representative input set is measured on a reference-ready workstation. If an exact run exceeds either budget, set timing evidence OPEN and amend the design through a separate evidence-backed decision; do not increase constants inside this task.

- [ ] **Step 4: Run affected and full regression gates on the same exact HEAD**

Run the same held independently reviewed launcher in regression mode. It performs a complete source proof before and after each of the three commands rather than trusting one preflight status. Repeat Step 2's complete held-stream bootstrap and replace only its final invocation with:

~~~powershell
& $heldReviewedLauncherPath -Mode Regression -ExpectedLauncherSha256 $reviewedLauncherSha256 -ExpectedSourceCommit $reviewedImplementationSha -ExpectedPrimaryPythonProvenanceReceiptSha256 $reviewedPrimaryPythonReceiptSha256 -ExpectedNativeBuildProvenanceSha256 $reviewedNativeBuildProvenanceSha256 @python310Args
if ($LASTEXITCODE -ne 0) { throw "Reviewed R0/C0 regression acceptance failed" }
~~~

The affected gate, exact default test, and full discovery each require their own complete `Ran ... / OK` footer. A timeout remains OPEN, is not promoted, and prevents standalone acceptance until resolved or the accepted design is amended with evidence.

- [ ] **Step 5: Adjudicate all 28 R0 requirements with scoped statuses**

Create the checklist from this frozen `REQUIREMENT_MATRIX_V1`; do not author codes ad hoc. Braced code sets are exact sorted `evidenceCodes`. “None” means an unavailable deterministic proof is FAIL, not OPEN. Each proof owner names the command family plus the exact test/artifact that independently drives the row.

| ID | Successful standalone code set -> aggregate | Exact proof owner | OPEN alternative | FAIL alternative |
|---:|---|---|---|---|
| 1 | `{CONTROL_CENTER_AND_FOUR_PACK_GREEN}` -> `PASS_STANDALONE` | Task 13 Regression affected/full commands; `tests.test_qa_actions`, `tests.test_qa_hub`; C0 deterministic/current inspection | `{CONTROL_CENTER_AND_FOUR_PACK_REGRESSION_OPEN}` only for a missing complete regression footer | `{CONTROL_CENTER_AND_FOUR_PACK_REGRESSION_FAIL}` |
| 2 | `{EIGHT_CAPABILITY_INVENTORY_UNCHANGED_PASS}` -> `PASS_STANDALONE` | `tests.test_qa_hub`, `tests.test_qt_quick_benchmark`; accepted manifest `uiCapabilityCount=8` | None | `{EIGHT_CAPABILITY_INVENTORY_UNCHANGED_FAIL}` |
| 3 | `{FOCUSED_NATIVE_EXTRACTION_PASS}` -> `PASS_STANDALONE` | Release CTests `sgfx_cine_ramses_probe_contract`, `sgfx_cine_ramses_probe_scene`, `sgfx_cine_ramses_probe_logic`, `sgfx_cine_ramses_probe_lifecycle`, `sgfx_cine_ramses_probe_synthetic`, `sgfx_cine_ramses_link_probe`, and `sgfx_cine_ramses_preview_contract` plus complete diff review | None | `{FOCUSED_NATIVE_EXTRACTION_FAIL}` |
| 4 | `{STRICT_TYPED_CLI_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_cli` and `tests.test_ramses_probe_adapter` | None | `{STRICT_TYPED_CLI_FAIL}` |
| 5 | `{METADATA_FAILURE_SKIPS_RENDERER_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_scene` and `sgfx_cine_ramses_probe_synthetic` renderer/load counters | None | `{METADATA_FAILURE_SKIPS_RENDERER_FAIL}` |
| 6 | `{NATIVE_VALIDATION_IDENTITY_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_scene`; `tests.test_ramses_probe_schema` | None | `{NATIVE_VALIDATION_IDENTITY_FAIL}` |
| 7 | `{FINDING_IDENTITY_COLLISION_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_contract`; `tests.test_ramses_probe_report` | None | `{FINDING_IDENTITY_COLLISION_FAIL}` |
| 8 | `{VERSIONED_SCHEMA_PHASE_IDENTITY_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_contract`; `tests.test_ramses_probe_schema`, `tests.test_ramses_probe_report`; native/final schema artifacts | None | `{VERSIONED_SCHEMA_PHASE_IDENTITY_FAIL}` |
| 9 | `{LOGIC_EVIDENCE_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_logic` and `sgfx_cine_ramses_probe_synthetic`; final-report logic facts | None | `{LOGIC_EVIDENCE_FAIL}` |
| 10 | `{BOUNDED_LIFECYCLE_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_lifecycle`; `tests.test_ramses_probe_process`; `process_lifecycle_tests` gate | None | `{BOUNDED_LIFECYCLE_FAIL}` |
| 11 | `{NON_RENDER_CLASSIFICATION_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_contract` and `sgfx_cine_ramses_probe_synthetic`; both lane render outcomes | None | `{NON_RENDER_CLASSIFICATION_FAIL}` |
| 12 | `{VALIDATION_NOT_HELPER_CRASH_PASS}` -> `PASS_STANDALONE` | `tests.test_ramses_probe_process`, `tests.test_ramses_probe_report`; typed lane execution/render status | None | `{VALIDATION_NOT_HELPER_CRASH_FAIL}` |
| 13 | `{VISIBLE_FRAME_NOT_APPROVAL_PASS}` -> `PASS_STANDALONE` | `tests.test_ramses_probe_report`, `tests.test_ramses_probe_evidence`; reviewed ticket wording | None | `{VISIBLE_FRAME_NOT_APPROVAL_FAIL}` |
| 14 | `{AUTHORED_CAMERA_CONTRACT_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_synthetic`; static no-direct-camera-setter scan | None | `{AUTHORED_CAMERA_CONTRACT_FAIL}` |
| 15 | `{ATOMIC_OUTPUT_CONTAINMENT_PASS}` -> `PASS_STANDALONE` | CTest `sgfx_cine_ramses_probe_cli`; `tests.test_ramses_probe_publication`, `tests.test_ramses_probe_runner`; accepted run manifest | None | `{ATOMIC_OUTPUT_CONTAINMENT_FAIL}` |
| 16 | `{SOURCE_IMMUTABILITY_PASS}` -> `PASS_STANDALONE` | `tests.test_ramses_probe_adapter`, `tests.test_ramses_r0_verifier`; verification `sourceIntegrity` plus held before/after facts | None | `{SOURCE_IMMUTABILITY_FAIL}` |
| 17 | `{UNTRUSTED_REPORT_REJECTION_PASS}` -> `PASS_STANDALONE` | `tests.test_ramses_probe_schema`, `tests.test_ramses_probe_process`, `tests.test_ramses_probe_runner`, `tests.test_ramses_r0_verifier` | None | `{UNTRUSTED_REPORT_REJECTION_FAIL}` |
| 18 | `{INTEGRATED_QML_TRANSPORT_DEFERRED,STANDALONE_REDACTION_PASS}` -> `DEFERRED_TO_INTEGRATION` | `tests.test_ramses_probe_evidence`, `tests.test_ramses_probe_runner`; zero-QML-transport diff scan | `{INTEGRATED_QML_TRANSPORT_DEFERRED,STANDALONE_REDACTION_OPEN}` | `{INTEGRATED_QML_TRANSPORT_DEFERRED,STANDALONE_REDACTION_FAIL}` |
| 19 | `{HOME_ROW_AND_OPAQUE_HANDLE_TRANSPORT_DEFERRED,STANDALONE_SOURCE_CONTRACT_PASS}` -> `DEFERRED_TO_INTEGRATION` | `tests.test_ramses_probe_report`, `tests.test_ramses_probe_evidence`; final/evidence schema artifacts | `{HOME_ROW_AND_OPAQUE_HANDLE_TRANSPORT_DEFERRED,STANDALONE_SOURCE_CONTRACT_OPEN}` | `{HOME_ROW_AND_OPAQUE_HANDLE_TRANSPORT_DEFERRED,STANDALONE_SOURCE_CONTRACT_FAIL}` |
| 20 | `{INTEGRATED_PARENT_STAGE_FAILURE_PRESERVATION_DEFERRED,STANDALONE_FOUR_PACK_FAILURE_ISOLATION_PASS}` -> `DEFERRED_TO_INTEGRATION` | exact `TestQaActions.test_execute_sgfx_preflight_calls_only_the_four_pack_service`; `tests.test_ramses_probe_runner` absence/failure cases | `{INTEGRATED_PARENT_STAGE_FAILURE_PRESERVATION_DEFERRED,STANDALONE_FOUR_PACK_FAILURE_ISOLATION_OPEN}` | `{INTEGRATED_PARENT_STAGE_FAILURE_PRESERVATION_DEFERRED,STANDALONE_FOUR_PACK_FAILURE_ISOLATION_FAIL}` |
| 21 | `{INTEGRATION_ORDER_PRESERVED_PASS}` -> `PASS_STANDALONE` | clean plan-to-HEAD diff proves no `qa_actions.py`, `qa_hub.py`, QML, capability, or action integration | None | `{INTEGRATION_ORDER_PRESERVED_FAIL}` |
| 22 | `{RUNTIME_LEASE_PROCESS_ISOLATION_PASS}` -> `PASS_STANDALONE` | `tests.test_ramses_runtime_lease`, `tests.test_ramses_probe_process`; verification `processCleanup` | None | `{RUNTIME_LEASE_PROCESS_ISOLATION_FAIL}` |
| 23 | `{CURRENT_PROFILE_UI_REFRESH_DEFERRED,STANDALONE_STALE_REPLAY_REJECTION_PASS}` -> `DEFERRED_TO_INTEGRATION` | `tests.test_ramses_probe_runner`, `tests.test_ramses_probe_publication`; generation/current-pointer fixtures | `{CURRENT_PROFILE_UI_REFRESH_DEFERRED,STANDALONE_STALE_REPLAY_REJECTION_OPEN}` | `{CURRENT_PROFILE_UI_REFRESH_DEFERRED,STANDALONE_STALE_REPLAY_REJECTION_FAIL}` |
| 24 | `{SYNTHETIC_PACKAGE_PRIVACY_PASS}` -> `PASS_STANDALONE` | `tests.test_bundle_manifest`, `tests.test_native_scaffold`, `tests.test_ramses_r0_verifier`; package/privacy gate and scan counts | None | `{SYNTHETIC_PACKAGE_PRIVACY_FAIL}` |
| 25 | `{R0_ACTION_QML_INTEGRATION_DEFERRED,STANDALONE_BUILD_TEST_PACKAGE_PROCESS_EXACT_TREE_PASS}` -> `DEFERRED_TO_INTEGRATION` only when reviewed 3.13 and 3.10 lanes both pass | fresh native-build receipt; CMake/CTest; complete focused Python 3.13/3.10 suites; runtime pair/product manifest/package/process/exact-tree gates | exactly `{PYTHON_3_10_INTERPRETER_UNAVAILABLE,R0_ACTION_QML_INTEGRATION_DEFERRED,STANDALONE_NONFLOOR_BUILD_TEST_PACKAGE_PROCESS_EXACT_TREE_PASS}` or `{PYTHON_3_10_PROVENANCE_REQUIRED,R0_ACTION_QML_INTEGRATION_DEFERRED,STANDALONE_NONFLOOR_BUILD_TEST_PACKAGE_PROCESS_EXACT_TREE_PASS}` | `{FAIL_STANDALONE_BUILD_TEST_PACKAGE_PROCESS_EXACT_TREE,R0_ACTION_QML_INTEGRATION_DEFERRED}` |
| 26 | `{APPROVED_REAL_LOCAL_SMOKE_PASS}` -> `PASS_STANDALONE` | Task 13 Real command; receipt/product lanes, `nativePolicyParityV1`, source integrity, classifier artifact rules | `{APPROVED_REAL_LOCAL_SMOKE_REQUIRED}` | `{APPROVED_REAL_LOCAL_SMOKE_FAIL}` |
| 27 | `{REPRESENTATIVE_TIMING_REQUIRED}` -> `OPEN`; add `EXACT_INPUT_TIMING_PASS` before it when exact-input timing passes | verification `timing`, both lane QPC facts, reference-ready representative run | `{EXACT_INPUT_TIMING_EXCEEDED,REPRESENTATIVE_TIMING_REQUIRED}` or `{REPRESENTATIVE_TIMING_REQUIRED}` | `{TIMING_EVIDENCE_FAIL}` only for invalid/contradictory timing proof |
| 28 | `{DIFF_SCOPE_AND_DEPENDENCY_PASS}` -> `PASS_STANDALONE` | complete plan-to-HEAD diff, native-build provenance, package inventory, dependency/license review | None | `{DIFF_SCOPE_AND_DEPENDENCY_FAIL}` |

For every row, the validator chooses exactly one table alternative from the owning proof; it does not retain a PASS code when that component is OPEN/FAIL. Deferred sibling codes remain present under precedence. Requirement 27 code order is exact lexicographic order when two codes exist. The complete section-24 program remains OPEN until all deferred integration halves are implemented and representative timing is closed or spec-amended; do not mislabel scoped standalone success as integrated acceptance.

- [ ] **Step 6: Create and independently review factual delivery records**

The ticket distinguishes VERIFIED, INFERRED, and OPEN. Without matched real lanes, it says only that the staged package/manifest candidate is authenticated and product execution remains OPEN. It may call the executable product path verified only after matched `real/receipt` and `real/product` execution plus facts parity pass. In either case it never calls the artifact an accepted executable/default, installed bundle, distributed release, or integrated action. It says:

~~~text
C0 provides a finite authored turntable and the existing provenance-gated inspection handoff. R0 is a noninteractive metadata, validation, lifecycle, and bounded authored-frame probe. Evidence-linked guided 3D review belongs to R4.
~~~

The worklog uses only observed local timestamps and durations. The changelog describes standalone local behavior and explicit degradation; it does not claim one-button integration, default cutover, universal compatibility, visual approval, or external release. Independently review the ticket, worklog, changelog, acceptance matrix, and machine evidence. If that review exposes any production/test/verifier defect, commit the fix, repeat Step 1 reviews, and rerun every exact command in Steps 2-4 on the new clean HEAD. Documentation-only corrections may be re-reviewed without invalidating otherwise unchanged exact evidence.

- [ ] **Step 7: Verify records, mark plan checkboxes, and commit delivery**

Run bounded whole-bundle marker/private-path scans, exact Ramses-subtree inventories, JSON parse checks, SHA-256 reconciliation, timestamp arithmetic, R0/C0 run-manifest/external-completion/current-pointer reconciliation, `git diff --check`, and a staged diff review. Mark Tasks 1-12 and Task 13 Steps 1-7 only when their evidence exists; leave Step 8 unchecked for the delivery commit.

Commit:

~~~powershell
git add docs/tickets/sgfx-ramses-r0-standalone.md docs/worklogs/sgfx-ramses-r0-standalone.md CHANGELOG.md plans/2026-07-13-sgfx-ramses-r0-standalone-implementation-plan.md
if ($LASTEXITCODE -ne 0) { throw "Staging standalone R0 evidence failed" }
git diff --cached --check
if ($LASTEXITCODE -ne 0) { throw "Staged delivery diff check failed" }
git commit -m "docs: record standalone Ramses R0 evidence"
if ($LASTEXITCODE -ne 0) { throw "Standalone R0 evidence commit failed" }
git show --check --stat --oneline HEAD
if ($LASTEXITCODE -ne 0) { throw "Standalone R0 evidence commit inspection failed" }
~~~

Verify the shown commit contains only expected paths and require a clean status. Record the distinct reviewed implementation SHA and evidence-delivery SHA.

- [ ] **Step 8: Append the canonical recovery correction and next action**

Append a physically last block to both canonical coordination files. Do not rewrite history. Record:

- active worktree, branch, reviewed implementation HEAD, exact verifier HEAD, and evidence-delivery HEAD;
- native, Python, package, real-smoke, timing, source-integrity, and review states;
- only fixed resolver tokens `R0_CURRENT`/`C0_CURRENT`, sanitized run IDs/roles, and verifier, run-manifest, and external-completion digests; never a resolved pointer/JSON/Markdown, real lane, scene/perspective/frame/log/private path, or frame digest;
- remaining OPEN items;
- explicit R0 noninteractive versus R4 guided-review boundary;
- C0 candidate versus default/distribution boundary; and
- acceptance state `PENDING_PLAN_CLOSURE`, with NEXT exactly the plan-closure commit; do not select integration yet.

End each appended beat with the real Europe/Berlin timestamp and exactly:

~~~text
Wagata, Yondaime! Signed sincerely by your dear Lexus
~~~

After that append is physically last, mark only this Step 8 checked, run `git add plans/2026-07-13-sgfx-ramses-r0-standalone-implementation-plan.md`, require `git diff --cached --check` and its `$LASTEXITCODE` to be clean, inspect the complete cached plan-only diff, and commit with `git commit -m "docs: close standalone Ramses R0 plan"`; fail immediately on a nonzero commit status. Run `git show --check --stat --oneline HEAD`, fail immediately on a nonzero status, and require a clean status. Only now evaluate the complete Standalone Acceptance Gate. Append one final physically-last canonical closure block naming the closure SHA, final `PASS|OPEN|FAIL`, and exact NEXT: integration ExecPlan only for PASS, otherwise the first concrete OPEN/FAIL closure action. This final external coordination append is the post-closure record, not another unchecked repository task; it prevents an infinite self-referential commit loop. Keep implementation, evidence-delivery, and plan-closure SHAs distinct.

---

## Standalone Acceptance Gate

Standalone R0 is accepted only when:

1. Tasks 1-13 are checked from fresh evidence and the distinct plan-closure commit is clean.
2. The native probe builds through the reviewed fresh protected offline-input Release toolchain/dependency contract with `osEgressContainment=not_claimed`, never the development configured tree, and all eight fixed contract/scene/logic/lifecycle/synthetic/link/preview/CLI CTests pass, including checked `doOneLoop()` failure injection.
3. Native/final/isolated-evidence schemas, enums, phase order, finding identity, source fingerprint, generation/replay, and path policy are exact.
4. An approved real local smoke reaches a truthful classified outcome with unchanged hashes for every passed input and matching available complete worktree HEAD/blob/fresh-index/filesystem proofs. A PNG exists iff Task 4's strictly validated readback succeeded and classified clear or visible; `undetermined_after_readback` has no PNG. If approved inputs are unavailable, standalone R0 remains OPEN and the integration slice is not authorized.
5. The 60/30-second evidence substatus is `PASS_EXACT_INPUT` for the named canonical profile/scenario/source fingerprint or explicitly OPEN. Requirement 27 and program-level representative timing remain OPEN until the distinct representative set is approved and measured; no exact-input pass authorizes release/default cutover.
6. The affected Python/QML/package matrix, exact default test, and full unittest discovery each have a complete OK footer under the independently receipt-bound protected CPython 3.13 primary runtime/dependency snapshots. A separately receipt-bound actual CPython 3.10 snapshot lane also passes; if that interpreter is unavailable or lacks approved provenance, standalone acceptance remains OPEN and the integration slice is not authorized. Any primary 3.13 mismatch is FAIL.
7. R0's matched real receipt and staged-product lanes pass the exact canonical facts projection and reconcile on the same explicitly bound reviewed clean implementation HEAD. The earlier no-private record retains native synthetic tests PASS, runtime-pair and product-manifest trust PASS, and both real execution gates OPEN under their exact input-required codes; it contains no production execution payload. The real-input C0 deterministic and compatible-preview gates pass there, while its exact timing OPEN and overall `PASS_WITH_OPEN` remain truthful; the initial no-private preview OPEN is preserved separately. The staged schema-2 package has exact audited Ramses helper/runtime/schema subtrees and passes the held-tree bounded whole-bundle physical plus recursive CArchive/PYZ/ZIP logical-member privacy scan.
8. Existing C0 preview, four-pack action, eight capabilities, 19 surfaces, profile neutrality, and Clean default remain unchanged.
9. Shared checker evidence remains unchanged until integration; the isolated R0 projection cannot mark containing gates or approval passed.
10. No BMW/private fixture, derived frame, raw path, external mutation, source write, direct camera mutation, interactive R4 feature, or new mandatory dependency appears in the diff; every pair/package/R0/C0/pointer/build publication journal is completed and no unknown orphan is adopted or auto-cleaned.
11. Independent native/spec, Python/security/package, and scope/release reviews have no open Critical or Important findings on the implementation HEAD that produced evidence; later code/script fixes trigger the full review/evidence loop again.
12. Canonical recovery records distinguish reviewed implementation, evidence-delivery, and plan-closure SHAs and are physically last with the exact signoff.

Passing this gate authorizes planning the separate one-button integration slice. It does not graduate the R0 program phase. Phase graduation requires separately reviewed integration acceptance, every section-24 OPEN/DEFERRED item adjudicated including representative timing closed or spec-amended, and then a teammate pilot of the accepted integrated one-button workflow; standalone or pre-integration feedback cannot substitute. It does not authorize integrating R0 automatically, flipping the Qt default, pushing, distributing a package, posting to Jira/Clockodo, writing BMW/SVN data, running rack/vehicle systems, or claiming stakeholder/visual/delivery approval.

## Plan Self-Review

- Spec coverage: all R0 component, data-flow, check, classification, lifecycle, safety, package, and standalone acceptance requirements are assigned to Tasks 1-13 with explicit deferred integration halves.
- Scope boundary: this plan intentionally stops before action/QaHub/QML integration. R1-R5 and R4 guided 3D review are excluded.
- Type consistency: native/final/isolated-evidence contracts use schema 1, the bundle uses schema 2 only after atomic Task 11 activation, probe version is 0.1.0, lanes are only `receipt` and `product`, `opengl` maps to `EDeviceType::GLES_3_0`, JSON `AspectFromResolution_isEnabled` maps only to runtime `AutoAspect`, `LocalTranslationApplication::NotApplied` serializes only as `not_applied`, viewport dimensions are `[1, 32768]`, the code stays C++17, and every layer uses at most one 480 x 270 readback with a 58-second native-work/60-second parent/30-second authored-render-phase contract and exact output names.
- Ownership consistency: native owns bounded facts; Tasks 6/7 own trust/process/freshness; `ramses_probe_report.py` alone owns final policy findings; Task 9 only projects accepted findings; shared checker evidence is untouched.
- Sequencing consistency: the reviewed plan precedes the statically reviewed no-Python launcher commit; externally bound cleanup/snapshot construction and the separate snapshot-binding commit precede the first Python RED; feature RED precedes domain implementation; Task 10's reviewed tooling commit precedes protected dependency/toolchain construction and its provenance-binding commit; whole-branch reviews precede exact evidence; and any later implementation fix invalidates and reruns the affected binding/review/evidence set.
- Placeholder scan: no TBD, TODO, guessed threshold, unowned implementation step, or automatic external action remains.
- Baseline evidence before edits: the fresh combined command for `tests.test_qt_quick_preview`, `tests.test_qa_actions`, `tests.test_qa_hub`, `tests.test_checker_evidence`, and `tests.test_bundle_manifest` completed `Ran 57 tests ... OK (skipped=1)`; the skip was the existing explicit real-scene environment gate.
- Live Python closure evidence before edits: the signed base contains `python3.dll` and `VCRUNTIME140_1.dll`; the installed PySide6 `QtCore.pyd` imports the stable-ABI DLL, and base `_wmi.pyd` imports the secondary VCRUNTIME. Both are therefore first-class signed receipt roles rather than ambient DLL-search assumptions.
- Live Git materialization evidence before edits: `core.autocrlf=true`; among 514 tracked paths, 378 worktree files are CRLF, 63 are LF, and several are mixed while 493 index entries are LF text. Source proof therefore authenticates held bytes through the frozen built-in Git-clean transform and reserves raw blob equality for explicit `-text` security inputs instead of asserting false universal worktree/blob byte equality.

## Execution Mode

Execute continuously task by task with RED/GREEN evidence, one implementation task at a time, independent review after each task, and a durable progress ledger. Root/Lexus remains the only writer of this plan and canonical state/handoff files.
